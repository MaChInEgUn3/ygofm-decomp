External naming leads — UNVERIFIED
==================================

Same contract as the `UNVERIFIED datacrystal/` annotations in variables.h and
the `ygofm-community` RAG collection: everything here is a hypothesis to check
against a listing, never evidence, and it must never be ranked beside a
measured paragraph. Nothing below has been used to justify a line of C.

Source
------
https://github.com/Unchiga/YuGiOhForbiddenMemoriesRecomp — a static
recompilation of the same binary (SLUS-01411 = our SLUS_014.11), built on
PSXRecomp. Different technique entirely: the MIPS is translated to C ahead of
time and compiled native, with no byte-exactness requirement anywhere. It
therefore cannot help a single codegen decision. What it has is ~300 KB of
hand-written enhancement code that reads the game's live memory, commented.

Its LICENSE is NOASSERTION. **Do not copy its code or its prose.** Addresses
and strides of a third party's game are facts, not their authorship; their
sentences are theirs. Everything below is our own restatement, and every
structural claim was re-derived from our own listings before being written.

What was actually new, honestly
-------------------------------
Less than it first looked. The two structural claims worth checking both
turned out to be things this repo already had:

  * "the weight table is tiered, `0x8017878C + tier*1460`" — our own
    PARKED.txt entry for func_80021810 already said "walks 0x2D2 halfwords of
    a 1460-byte record". Re-derived independently anyway: the shift chain at
    the head of func_80021810 is 8a → 9a → 72a → 73a → 292a → 365a → 1460a,
    so the stride is 1460 exactly, and D_8017878C − D_801781D8 = 1460.
  * their "walks 722" is CORRECT and was mis-checked here at first: 0x2D2 is
    func_80021810's bound. func_800243F4's is 0x2D0. Two functions, two
    bounds. The error was ours, not theirs.

The one genuinely new thing is a cross-reference we had never written down:
**func_80021810 and func_800243F4 run the same weighted pick over the same
table family** — both `(func_8008E590() & 0x7FF) + 1` against a running total
of halfword weights — at different tiers. Recorded in variables.h beside
D_801781D8 and in both park entries.

Function-name leads
-------------------
Their names, our status. Names are guesses about intent; none has been checked
against behaviour.

  func_80021480  MATCHED   drop: page-apply
  func_80021810  parked    drop: roll  (confirmed by our own listing above)
  func_80021894  MATCHED   drop: award
  func_800218F0  open      drop: results-state / nav
  func_800170C8  MATCHED   (prose only — called by the card-list walk)
  func_800323F8  open      chest: build
  func_80032C48  open      chest: sort
  func_80036C14  MATCHED   glyph draw ("NP")
  func_80037DA4  open      text draw ("NP")
  func_800393B0  open      drop: widget draw
  func_80040410  MATCHED   drop: hide

Interior addresses they annotate, mapped to our functions:

  0x800175D0 in func_800175A0 MATCHED  |  0x800214A0 in func_80021480 MATCHED
  0x80021C44 in func_800218F0 open     |  0x80024A94 in func_800249E0 open
  0x8002DC70 in func_8002DC38 MATCHED  |  0x80031A7C in func_80031874 open
  0x800324B4 in func_800323F8 open     |  0x80033004 in func_80032C48 open
  0x800393CC in func_800393B0 open

Data addresses: they reference 64, of which 20 we already declare. The other
44 are mostly code addresses, not data.

How to use this
---------------
Only for comprehension, and only on the big remaining functions — knowing a
loop walks the fusion table tells you its bounds before you write a line. If a
name here ever turns out to be worth keeping, verify it against behaviour
first and then annotate the symbol in the usual place; do not promote anything
out of this file by citation alone.

# ---------------------------------------------------------------------------
# COMMUNITY CHEAT OFFSETS -- a fourth source, and the only one that is
# self-verifying. Added 2026-09-01.
#
# krystalgamer's observation: the Brazilian/Spanish/Vietnamese FM scene has
# been sharing cheats and patches for years and every one states a FILE OFFSET
# into SLUS_014.11, never a virtual address. That is why the corpus has never
# been usable by a decomp -- and it is one addition away from being usable,
# because this project already knows the delta (0x8000F800).
#
# `tools_src/offset.py` does the conversion and prints the containing function
# and the instruction the byte belongs to.
#
# WHY THIS SOURCE IS BETTER THAN THE OTHER THREE: a cheat is a claim that
# changing ONE byte produces ONE observable effect in the running game. It
# names a behaviour and points at the instruction that implements it, and it
# has been tested by everyone who ever used it. That is closer to evidence
# than a wiki row or a name in someone's database.
#
# WORKED EXAMPLE (credited to Markin Marcal): "Olho do Milenio, Offset 8864 no
# SLUS ... o codigo FF, substitua por 00 e voce vera a mao do NPC, substitua
# por 01 e voce vera as cartas ocultas."
#
#   8864 is ambiguous between decimal and hex and the tutorials never say
#   which. The stated byte settles it: decimal 8864 holds 0x00, hex 0x8864
#   holds 0xFF. So it is hex -> vram 0x80018064, inside func_80018004:
#
#       lb    $v0, 0x1F($v0)     signed byte out of the card record
#       bgez  $v0, .L8001806C    if >= 0, skip
#       addiu $v0, $zero, 0xFF   <- the cheat patches this immediate's low half
#       sb    $v0, 0x67($a0)     store into +0x67 of the object
#
#   So +0x67 is a card's VISIBILITY marker (0xFF = hidden) and the sign test on
#   +0x1F is what classifies a card as the opponent's. func_80018004 is already
#   decompiled here; neither the GMS idb nor the other decomp names it, and
#   Unchiga's roster has the mechanical `create_obj_set_flag_and_marker`.
#
#   Still a HYPOTHESIS in the sense this file means: nobody here has run the
#   patched binary. What is measured is the offset, the byte, the instruction
#   and the field; what is taken on trust is that the effect is as described.
# ---------------------------------------------------------------------------


Second pass, 2026-09-04 -- the enhancement sources, measured
-------------------------------------------------------------
Same source, at dca7cdd (2026-09-03). Read from a shallow scratch clone; nothing
copied. The symbol map (symbols.toml) has ONE entry and is not where the value
is: the value is 97 `#define name address /* meaning */` lines spread across
57 enhancement sources, plus one 813-line handoff document about extending
the card count, which documents record layouts because it had to.

Cross-referenced against include/variables.h and functions.h: 43 of the 97 are
D_ symbols this tree declares, 7 are func_ symbols, 47 are addresses this tree
has no symbol for at all. Of the 43, TWENTY-SIX carry no comment here and a
meaning there.

VERIFIED against our own listings before being written down (the contract):

  * D_801D4244 -- one u32 per card, indexed by id-1, and the bit layout their
    handoff reports as measured over all 722 stock cards:
        bits  0..8   ATK / 10          bits 18..21  Guardian Star 1
        bits  9..17  DEF / 10          bits 22..25  Guardian Star 2
        bits 26..30  type (0..19 monster types, 20 Spell, 21 Trap,
                          22 Ritual, 23 Equip)
    Our listings agree without being asked: func_80018608 reads it and does
    `andi 0x1FF` then `sra 9; andi 0x1FF`; func_80017F04 and func_8001B938
    do `sra 26; andi 0x1F`. That is bits 0..8, 9..17 and 26..30 exactly.
    Level is not in the word. krystalgamer's tree names this gDuel_adwCardStats.
  * D_8009B26C -- "& 0x1F is the mode index": func_80028880's region does
    `lbu` of it followed by `andi 0x1F`. Their other half, "0xC3 = in duel",
    was NOT found by the grep used and stays a lead.
  * Their gp is 0x8009AF08, which is ours (config/symbol_aliases.txt), so
    every "gp+N" offset in their comments resolves exactly here.

UNVERIFIED leads, one per symbol this tree declares without a comment. Each is
our restatement of their inline comment, not their text; several have a name
there and no stated meaning, which is recorded as such rather than guessed:
  D_80010384  (named NAME_TAIL there; no meaning given)
  D_8009AF08  (named PSX_DROP_GP there; no meaning given)
  D_8009B112  (named FR_EFFECT_FLAGS there; no meaning given)
  D_8009B140  (named PSX_FUSION_CARD_DETAIL there; no meaning given)
  D_8009B23A  campaign overlay state: 0xE00D
  D_8009B246  u16 card id for the viewer
  D_8009B24B  0x14 on every chest viewer open
  D_8009B26C  &0x1F = mode index; 0xC3 = in duel
  D_8009B26E  (named FR_SUBSTATE there; no meaning given)
  D_8009B32E  (named IDX_ADDR there; no meaning given)
  D_8009B33A  == 0xFF on the shopkeeper menu
  D_8009B345  menu row count (gp+1085)
  D_8009B34D  menu cursor    (gp+1093)
  D_8009B350  gp+1096: 1 while a menu is open
  D_8009B394  new-press mask, swapped
  D_8009B3DE  $gp + 0x4D6, u8
  D_8009B3EB  $gp + 0x4E3, u8
  D_8009B3FA  $gp + 0x4F2, u16
  D_800EA030  (named PSX_FUSION_SELECT there; no meaning given)
  D_800EB0F8  (named PSX_DROP_WIDGET0 there; no meaning given)
  D_800EB12C  (named SHOP_DLG_MODE there; no meaning given)
  D_801781D8  (named PSX_DROP_RESIDENT_BASE there; no meaning given)
  D_8017A1D8  (named PSX_FUSION_EQUIP_BASE there; no meaning given)
  D_801B0000  (named SHOP_LBL_BASE there; no meaning given)
  D_801D0000  offsets are relative to here
  D_801D3200  memcard transfer buffer

The 47 addresses this tree does not declare are not listed: they are splat
interior addresses or record fields, and a lead for a symbol that does not
exist yet is a lead for splitting, which is a different job. The extraction
lives in the scratch clone's leads.txt for the session that wants it.

What this pass says about the question "would the recomp add more than
emulator traces": for NAMING, yes, and by a lot per hour -- one static read
produced 26 leads and 3 verifications, where one trace round trip produced one
answer over four PRs. For MATCHING, still nothing, and that has not changed.
