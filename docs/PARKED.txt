# Functions attempted and parked, with the class of difference. Read by
# tools_src/candidates.py so they are not offered again. See docs/DECISIONS.md
# for what each class means and what has already been tried.
#
# When parking a function, put its best candidate C in parked/<func>.c. The
# first 37 entries below were parked without doing that, and the near-miss
# source is gone -- which means the flag sweep and the permuter can never be
# re-run on them without decompiling from scratch. That mattered the moment
# -fno-schedule-insns turned out to have been wrongly excluded from the sweep
# set: the functions most likely to be unblocked by it are exactly the ones
# whose candidates were thrown away. Only func_80040390 and func_800594C0
# survived, because they happened to still be in scratch.
#
# Best flag combination per candidate, from tools_src/sweep_try.py over the
# whole set (2026-07-26). The count is differing instructions under that
# combination; the source shape is whatever is in parked/<func>.c. Two entries
# that were on this list -- func_8001798C and func_80049394 -- matched outright
# under -mno-split-addresses and are in src/ now.
#
# THESE ARE SEARCH RESULTS, NOT MEASUREMENTS. sweep_try.py runs through
# try_func, which is validated against src/ and parked/ under the flags those
# functions actually use -- not against arbitrary combinations. Its assembler
# flags go through a per-combination flag_overrides.json write, which is
# exactly the kind of path that has produced confident wrong numbers in this
# project before. Confirm with a full build before any of these becomes a
# PER_FUNC_FLAGS entry. func_8001798C and func_80049394 were confirmed that
# way; nothing else in this table has been.
#
#   func_80016DDC     11  O1 G0
#   func_8002CCE4      3  O2 G0
#   func_8002F440     14  O1 G8
#   func_80036F80     21  O2 G8 nosched1
#   func_800382A8     33  O2 G0 nosched2
#   func_80039E9C      6  O1 G0
#   func_80040390      6  O1 G8
#   func_80040510     29  O1 G0
#   func_80045484      8  O2 G0 nosched2 macro
#   func_800472A8     10  O1 G8
#   func_8004733C     10  O2 G8
#   func_80048C0C      3  O1 G0
#   func_8004A854     13  O2 G0 nosched2 macro
#   func_8004C84C     19  O2 G8 nosched1+2
#   func_800594C0     16  O1 G0
#   func_8005F7B0      5  O2 G8
#   func_800732A0     13  O1 G8 macro
#
# func_8003201C is a clean instance: 40 instructions against 40, the whole
# sequence identical, and two symmetric pairs swapped -- (card, its copy) in
# the outer loop and (count, index) in the trailing one. Four declaration
# orders, a hoisted slot pointer and && vs nested ifs all leave it at 17; the
# sweep's best is O1 G0 at 13, which fixes the register names and loses the
# strength-reduced pointer instead, so it is not closer.
# --- register allocator: retail copies a value out of a register and we do
# not. func_80037110 escaped this by declaring a *second* pointer at the top
# and using it for only part of the function -- not `p = arg0` used
# everywhere, which does nothing. Tried on func_800382A8, func_800472A8 and
# func_80040510 afterwards and it moved none of them, so it is a shape that
# works sometimes rather than the answer. Two variables each covering part of
# the body is the thing to try first here.
# ---
# func_80070710 is ten instructions, sequence identical, every register name
# different: retail reuses $a0 as the %hi temp of the scalar load it lands in,
# and puts the table address in $v0 where we put $v1. Six source forms and both
# -G settings all sit at 8.
# ---
# func_80071424 and func_80071460 are `D_800F5B98[func_8007058C()] = <sym>[0];`
# and they are the sharpest instance of the class, because their siblings
# func_8007164C and func_80071688 -- the same store, a different source symbol
# -- match as that exact one-liner. Retail's two functions disagree about the
# registers for otherwise identical code: the siblings put the table in $a0 and
# the loaded value in $v1, these two swap them.
# Sixteen source forms brought them 6 -> 2, and the two remaining differences
# are **coupled**, which is what makes it a park rather than a puzzle:
#   - value read into a named variable (`s32 v = SYM[0]; t[i] = v;`) gets both
#     registers right, and gcc then emits `la; lb; sll`, one slot late;
#   - value read inline in the store gets the order and the addu destination
#     right, and swaps the two registers.
# No form gives both, and the sweep's best is O2 G0 at 2 for the first shape,
# 6 for the second, so no flag reaches across it either. The parked candidate
# is the first shape.
# Permuter run 2026-08-03 on both: func_80071460's best scored 40 and try_func
# calls it 10, four worse than the plain inline form; func_80071424's best
# scored 40 and try_func calls it 6, four worse than the park's 2. Nothing
# usable from either.
# ---
# func_80026D18 MATCHED 2026-08-03, on a pair of shapes neither of which works
# alone. The old entry was accurate as far as it went -- 4 differences, the
# loop counter and accumulator holding each other's register, nine shapes
# including the declaration order all at 4. What it could not see is that the
# two levers are *coupled*:
#   * `do { } while (0);` around the `if` body: 4 -> 2. Not a block -- `{ }`
#     and `if (1) { }` are both 6. gcc's loop pass sees the extra loop node
#     and numbers the blocks differently. The permuter found this one.
#   * `count = 0;` initialised before `i = 0;`: alone it is *worse* (4 -> 6),
#     which is why nine earlier shapes rejected it. With the wrapper it
#     matches.
# A shape measured alone and found worse is not eliminated: two levers can
# only pay together, and the permuter breaks that deadlock because it mutates
# from a base that already carries one of them.
# ---
# func_80073220 clears D_800EAE88[0..5] backwards and then copies the entries
# of D_800F5BE8 that are >= 0xB into it. Every block is right, including the
# dead `if (i != 0)` guard retail emits on a variable it has just zeroed, which
# `while (i <= b[0xA3])` reproduces by itself. Two things do not come out:
# retail materialises the D_800F5BE8 address into $v1 and *copies* it to $a2 --
# 32 instructions against our 31 -- and it holds count in $a1 where we get $a2.
# Nine declaration orders, two-pointer forms for the copy, and the sweep (best
# O1 G0 at 17, which fills a load delay by luck and loses the scheduling of
# i++) all leave the same rotation. `s32 v` rather than `u8 v` for the loaded
# byte is needed for retail's signed `slti`: a u8 local compares in QImode,
# which is unsigned, and that one is worth keeping.
# ---
# func_8004BBBC MATCHED 2026-08-03 and is out of the park. It had been the
# standing example of "one dup-%hi candidate a file flag cannot satisfy": the
# two D_8009B458 reads want the bare form and D_8009AF80 wants cc1psx's own
# split pair hoisted into $s1, and -mno-split-addresses is per *file*, so it
# could only ever give one of them. The resolution is that the choice is
# per *symbol* after all -- a scalar declaration makes cc1psx emit the bare
# symbol for that one name, and the assembler's -G decides whether it is
# expanded, while an unsized array keeps the split pair whatever -G says. The
# parked candidate had been forcing D_8009B458_IS_AGGREGATE, which was the
# whole problem. Default compiler flags, `as -G0`, no source change beyond
# dropping that define.
# ---
# func_8005C4F0 is sixteen instructions and every block is right: `if (v != 0)
# { if (v != -1) return 0; } return -1;`, where retail's `-1` in the first
# branch's delay slot is the *comparison* operand scheduled early, not a return
# value -- writing it as two `return -1`s costs six more differences. The -1
# also has to stay one pseudo shared by the compare and the return; `v == 0 ||
# v == -1` folds the pair into `sltiu v+1,2`.
# What is left is the usual tie: retail keeps v in $v1 and the -1 in $v0, we
# get v in $a0 and the -1 in $v1. Whichever pseudo is allocated first takes
# $v1, and nothing source-side chooses. Sweep best 4, same as O2 G8.
# ---
# func_8004A764 fills five fields of D_8009B458[0] from D_80011434[arg0]. Every
# instruction from the prologue on matches; the nine differences are one block's
# order, because gcc hoists `lui %hi(D_8009B458)` to the top of the function and
# schedules the matching `lw` away from it, where retail keeps the pair adjacent.
# -mno-split-addresses does keep it adjacent but then turns the *table* access
# into an indexed bare-symbol load (`lui $at; addu $at; lw %lo($at)`) instead of
# retail's la + addu + lw, which is worse. Four source orders for the two reads
# (pointer first, value first, address-only in a variable) give 9, 12 and 14.
# ---
# func_8004A6F8 is func_8004A764 with three fields copied from arg1 instead of
# constants, and it sits on the same barrier at 8. It is here for the one thing
# that moved: splitting the table read into an address and a value --
# `s32 *t = &D_80011434[arg0]; u8 *p = D_8009B458[0]; s32 v = *t;` -- took it
# from 26 to 8, because the address computation then lands before the pointer
# load the way retail has it. The same split makes func_8004A764 *worse* (9 to
# 14), so the two are not one problem despite looking like one; each keeps its
# own candidate.
# **Permuter, 2026-08-03: func_8004A6F8 9 -> 6** by dropping the
# `s32 *t = &D_80011434[arg0];` local and reading the table inline -- the
# reverse of what this entry recommends. The entry was right when written:
# the split helped under the *aggregate* addressing both used before the
# dup-%hi retraction. A measurement is only valid against the flags it was
# taken under. func_8004A764's own run found nothing (best 10 against its 6).
# Re-measured 2026-08-03 against the retraction of the split-versus-bare
# "barrier" (see func_8004BBBC). "Retail reaches D_8009B458 with one register
# and gcc uses a separate temp" was that barrier, and the fix is per symbol:
# both candidates were forcing D_8009B458_IS_AGGREGATE, and neither function
# has any %gp_rel, so the plain scalar arm plus `as -G0` gives retail's
# one-register form. func_8004A764 9 -> 6 and func_8004A6F8 8 -> 9, and both
# now have **equal lengths**, which they did not before -- the -1 they each
# carried was the missing second half of the bare load. The flags are in
# build.py's parked section so the counts reproduce.
# What is left is scheduling, and it is the same shape in both: retail
# completes the D_80011434 table address (`sll` then `addu`) *before* loading
# the D_8009B458 pointer, and we load the pointer first. The address/value
# split that helped func_8004A6F8 under the old form now makes func_8004A764
# worse again (6 -> 9), so the two really are separate problems.
# ---
# func_8005C5D4 spins on D_8009B062 for 0x7FFFFFFF iterations, then calls
# func_8008FBE0(1) if it timed out. D_8009B062 has to be volatile or gcc commons
# the loop's read with the entry guard and propagates the flag as zero, which
# deletes the test; that is in variables.h now and it is the useful part.
# Two things remain, and the first is a *tool* difference rather than a source
# one, so read this before trying again:
#   - the 0x7FFFFFFF is materialised by retail as `lui $v1,0x8000` +
#     `addiu $v1,$v1,-1` (splat named the pair D_7FFFFF and wrote 0x7FFFFF for
#     it in undefined_syms_auto.txt, which does not match the bytes -- the
#     encodings are 3C038000 and 2463FFFF). cc1psx emits `li $v1,0x7fffffff`
#     and maspsx's expand_load_immediate turns that into `lui 0x7FFF` +
#     `ori 0xFFFF`. Same value, different bytes. aspsx evidently splits it the
#     %hi/%lo way whenever the low half has bit 15 set. This is the only
#     in-scope function that exercises it, which is why it has never shown up:
#     when such a constant *looks* like an address splat names it and writing
#     `&D_x` reproduces the bytes by accident.
#   - retail's loop is not rotated -- `bgtz` at the head with the timeout call
#     as the fall-through -- and gcc rotates it and drops the first test because
#     the initial value is a known constant. while(1) with the exit inside,
#     a rotated while, a goto loop and an explicit block order with the body
#     behind a forward goto all produce the same 19: gcc's jump pass inverts the
#     branch and lays the call block last whatever the source says.
# ---
# func_80047A68 ORs a bit per slot whose count has reached the limit and hands
# the mask to func_80076ED0. **It needs O1 G0** -- at O2 gcc reverses the loop
# into a countdown from 3 and the whole tail moves, which is 23 differences;
# under O1 G0 the loop, the mask and the call are all exact and only the
# prologue block is left, at 6. `u32 i`, a `for`, and `i = mask` as the
# initialiser do not stop the reversal at O2, so do not spend time there again.
# What is left at O1 G0: retail loads the D_8009B45C pointer into $v0 and
# *copies* it to $v1, which also fills the load delay before `lhu 4($v1)`; we
# load straight into $v1 and take a nop, and `sw $ra` lands first instead of
# after the copy. A second pointer variable does not produce the copy -- gcc
# coalesces the two -- and declaring the bit mask before the accumulator makes
# it 7.
# ---
# func_80021810 walks 0x2D2 halfwords of a 1460-byte record and returns the
# index where the running sum passes a random limit.
# Sibling: func_800243F4 runs the identical pick -- same
# `(func_8008E590() & 0x7FF) + 1`, same running total of halfword weights --
# over D_801781D8, which is D_8017878C minus one 1460-byte tier. Decode either
# and the other follows. Everything from the call
# on is exact; the eleven differences are the multiply chain. Retail computes
# `arg0 * 1460` step by step *into* $s0 -- the callee-saved register the pointer
# lives in, saved before the first shift -- and we compute it in $v0/$v1 and
# copy. Naming the offset, integer-cast address arithmetic with the chain as the
# first operand, and accumulating in place all leave it at 11; declaring the
# locals before assigning them costs 26 more. The sweep agrees at 11.
# ---
# func_80047FAC is ONE instruction and the closest miss in the park. Retail
# zeroes its counter and its accumulator independently -- `addu $s1,$zero,$zero`
# then `addu $s0,$zero,$zero` -- and gcc commons the constant and copies the
# second from the first. Six initialisation shapes (both declaration orders,
# initialiser vs assignment, a `for` header, the pointer hoisted out of the
# loop) all leave exactly that one `addu`, and the sweep's best is the same 1.
# Everything else is exact, including the two levers it needed: the macro flags
# for retail's one-register D_8009B45C load inside the loop, and `p += i * 2;`
# so the base is the addu's destination rather than the scaled index.
# ---
# func_8003D46C MATCHED 2026-08-03, with func_8004BBBC and for the same
# reason. It was the second instance of that "barrier", roles reversed: an
# assembler-expanded bare *store* (`lui $at,%hi(D_8009B34D)` / `sb %lo($at)`)
# next to a cc1psx split *load* of D_800EF6EA into two registers. The old
# entry ended "a sized array declaration does not restore the split pair under
# the macro flags, so the size hint is not a fourth addressing form" -- and
# the error is in the clause after the comma. The size hint was only ever
# tested *with* -mno-split-addresses, which forces every symbol in the file
# bare; with default compiler flags the declaration is per symbol, and the
# assembler's -G then decides only for the ones cc1psx emitted bare. Scalar
# D_8009B34D (the default arm, not the aggregate the candidate was forcing)
# plus `as -G0`, and the split load is untouched because an unsized array is
# never bare.
# **volatile is a real per-symbol escape from the bare form, and it half works
# here.** With -mno-split-addresses plus `volatile u8 D_800EF6EA[]` the store
# comes out exactly right AND the load goes back to a cc1psx pair -- but to
# `lui $v0,%hi(s)` + `addiu $v0,$v0,%lo(s)` + `lbu $a0,0($v0)`, the address
# completed in a register, because volatile also blocks folding %lo into the
# memory operand. Retail folds it. So the knob is worth reaching for when the
# target itself shows the *unfolded* form, and not otherwise; here it is 32
# against 26 and 28.
# ---
# func_80044F58 fills a four-byte packet and then writes three fields through
# D_8009B45C. Under the macro flags everything matches except the tail: retail
# emits the second pointer load *between* the 0x49 store and the 0x510 store,
# so the 0x510 store fills its load delay and the two pointers live in $v0 and
# $v1; gcc keeps one register, stores both fields, then reloads into $v0. A
# second name for the second read and a named second pointer both leave it at
# 9 and 10. The if/else polarity below it is settled -- retail branches out on
# `!= 0`, so the `== 0` arm is written first.
# ---
# func_8003D03C is a dup-%hi candidate that the macro flags do not finish.
# D_8009B408 is touched three times -- read signed, cleared, read unsigned --
# and gcc forms the address once into a register and reaches all three through
# it, unfolded (`lui`+`addiu`+`lb 0($v1)`) where retail has three separate
# folded bare accesses. Two extra names make it *worse* (38 to 40), which is
# the opposite of what they do for func_80047EC4, so the alias rule does not
# reach this shape: there the commoning was loop-invariant motion, here it is
# plain address CSE inside one block.
# ---
# func_80049AF4 is seven differences and all seven are $s0 against $s1: retail
# puts arg0 in $s0 and the constant 1 -- hoisted because it is stored twice --
# in $s1, and gcc does the reverse. Naming the 1 as a local makes it 11.
# ---
# func_8004B854 is three, and they are which instruction fills the call's delay
# slot. Retail emits the three constant arguments, then `lui $a3,%hi(cb)`, then
# the call with `addiu $a3,$a3,%lo(cb)` in the slot -- the split_address_across
# _call shape. gcc emits the callback address first and fills the slot with the
# 0x1000 argument instead, so the post-pass cannot fire: it only rewrites
# `la / call / nop`, and there is no nop to rewrite. Hoisting the 0x1000 into a
# local does not stop gcc sinking it to the call site. Do NOT add this one to
# LA_CALL_FUNCS -- it was tried, the entry is dead, and it was removed again.
# ---
# func_80049434 is twenty stores into D_8009B458[0] and nothing else. Written
# with the symbol inline in every statement it reloads the pointer per store,
# which is right for most of them but not all: retail reuses one load across
# *groups* -- 0x510 with 0x814, then 0x512/0x514/0x516 with it too, while
# 0x815 gets its own. So some runs of stores were one pointer variable in the
# source and some were not, and the grouping has to be recovered store by
# store. 62 differences, all of them that. Worth finishing; just not quick.
# ---
# func_80044E90 is TWO instructions and the shape is fully recovered. Retail
# copies arg0 into $a1 in the first branch's delay slot and uses the copy for
# both `-0x80 - arg0` subtractions while testing the original -- the
# "second variable covering part of the function" shape, and without it there
# is no copy at all and the whole tail shifts (49 differences). With it we get
# the copy and the mirror assignment: retail subtracts $a1 and tests $a0, we
# subtract $a0 and test $a1. Three placements of the two names give the same 2,
# and the sweep agrees.
# ---
# func_80045054 sums the squares of 0x100 halfwords into a field of
# D_8009B45C[0]. It reaches the symbol four times and retail materialises it
# four times, so it needs Base2/Base3/Base4 on top of the macro flags: that is
# 49 differences down to 29, and it is the corrected alias rule working at
# scale. What is left is register allocation around the call result -- retail
# keeps it in $v0 and the pointer in $v1; splitting the reused local into two
# variables makes it 37, so the reuse is not the cause.
# --- the mult/div population, opened 2026-07-27 when that drop rule was
# retracted. Both of these are ordinary C; neither needs an idiom.
# func_8004A27C is 11: the two multiplies, the two zero stores and the call all
# match, and what is left is the %hi temp register plus where the 0xF store
# lands relative to the table read. Hoisting the table read into a local took it
# 16 -> 11; the macro flags make it 25, so this one wants the split pair.
# func_8004A3BC is 25 and needs reading again rather than nudging: the three-way
# `b < 0x40` / `b == 0x40` / `b > 0x40` split with the shared `return 0` is
# right in shape but the negation (`negu` on one path, plain mult on the other)
# and the s16 return conversion are not falling where retail has them.
# func_80047864 is TWO instructions and both are the same one: retail reads
# D_8009B45C with `lui $a0,%hi` + `lw $a0,%lo($a0)` -- one register, the bare
# form -- and we use a separate temp. The macro flags fix exactly that and cost
# five elsewhere (the 0x100000 constant moves from $v1 to $v0 and the `3` store
# reorders behind it), and declaring the constant first does not move it. So
# this one is the split-versus-bare barrier again, at its smallest yet: two
# instructions, one symbol, no second symbol to conflict with -- which makes it
# the best specimen in the park for anyone attacking that class.
# What did get it to 2, and is worth reusing: naming the two derived pointers
# (`q = p + arg0 * 2; r = p + arg0;`) so the base is the addu's first operand.
# Written inline the index comes first and it is 8.
# func_80033CF8 pushes a polar primitive onto the list at D_8009B310 and bumps
# the counter at D_8009B314. Three differences and they are the position of the
# *second* read of D_8009B314 -- retail schedules the `lw` up between two of the
# stores, we leave it at the end. Two levers got it from 40 to 3 and both are
# reusable: reading D_8009B310 inline in the first store (not into a variable
# beforehand) keeps the pointer load *after* the call instead of alive across it,
# which alone was 34 differences and one extra callee-saved register; and naming
# the u16 read of D_8009B314 gets the `lhu` into retail's place. Moving the
# second read between the stores by hand makes it 12, and the sweep agrees at 3.
# func_8002A6B8 turns two signed byte counters into a score: `(a/10)*200 +
# (a%10)*10`, plus 100 if the second is >= 10, plus its own remainder, clamped
# at 0x2D3. The instruction *sequence* is right and it is 41 differences, all
# from one thing worth writing down: retail sign-extends the **quotient** back
# to s8 (`sll 24; sra 24`) before the *200 chain, and with `s8 q = a / 10;` gcc
# omits that extension -- it can see the value fits -- and reorders the
# remainder chain ahead of it. So the quotient in retail's source went through
# something gcc could not see the range of. Needs re-reading with that in mind,
# not nudging.
# func_80018CF8 copies five signed bytes into a local s16[5], then walks the
# slots looking for the record whose field equals k for k = 0x11..0x15, marking
# each hit with -1. The block structure is fully recovered (the goto form in the
# candidate matches retail's blocks one for one) and it is 37 differences, of
# which two things are diagnosed and the rest is the register cascade behind
# them:
#   - `t = (u8 *)D_8015C424;` has to be assigned *after* the first loop, not at
#     its declaration, or the address materialises before the loop: 46 -> 38.
#   - retail keeps 0x48000 in its own register and 0x39FC as the load offset;
#     written as `+ 0x48000 + 0x39FC` gcc folds them into one constant and uses
#     offset 0. Holding 0x48000 in an `s32` variable keeps them apart (37) but
#     is a guess at the source's shape -- 0x8015C424 + 0x48000 + 0x39FC is
#     0x801A7E20, which is D_801A7AD8 + 0x348, so the real source may name a
#     different symbol entirely and that is the thread to pull.
# func_80070650 runs the dispatch table at D_800916E0 until the entry it just
# called is one of three handlers. The shape is right in outline and it is 42
# differences, with one thing measured: retail hoists *two* of the three handler
# addresses into callee-saved registers and materialises the third inside the
# loop, using four callee-saved registers where we use five. Everything after
# the prologue shifts by that one register. The three comparisons are written as
# a value read once into a local (retail reloads the table entry after the call
# and compares it three times, so that part is right).
func_80070650
func_80018CF8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80018CF8_WAS
func_8002A6B8
func_80033CF8
func_80047864
func_8004A27C
func_8004A3BC
func_80044E90
func_80045054
func_80049434
func_80049AF4
func_8004B854
func_8003D03C
    The stored 9 decomposed 2026-08-11 and installed: the D_8009B408 byte
    read named into an s8 local plus a dead if with identical arms
    conditioned on arg0 (the permuter's version read an uninitialized
    variable in the condition; arg0 works the same and is clean). The two
    are coupled -- the name alone stays at 10. A fresh 30-minute permuter
    run from the 9 base found nothing: its best-scoring outputs measure
    35-41 through try_func.
func_80044F58
func_80047FAC
func_80021810
func_8004A6F8
func_8005C4F0
func_80047A68
func_8004A764
func_8005C5D4
func_80073220
func_8003201C
func_8004CABC
# --- range-check fold: class emptied, see WORKFLOW.md. Both members
# (func_80049544, func_80049640) matched once the condition was written as
# nested ifs rather than &&. Neither had a candidate saved; both were
# decompiled again from the listing in a few minutes.
# --- cross-jumping (closed: no -fno-crossjumping until gcc 3) ---
func_800601D0
    A stored permuter output measures 12, better than this entry. Found by
    semantically checked -- two of the audit's finds so far were wrong.
    **14 -> 12**, and the exact spelling of the helper is load-bearing.
    `e - -4` for the found-return plus `inline int inline_fn() { return -1; }`
    called at the tail. Written as `static inline int inline_fn(void)` the
    function comes out 18 instructions against 20 and measures 14 -- the
    `static` and the `(void)` change whether gcc 2.8 emits the out-of-line
    copy, and that is worth the two instructions. Copy the declaration
    verbatim; do not tidy it.
func_8002F440
# --- addressing / individual causes, described in DECISIONS.md ---
# func_80070710: ten instructions, sequence identical, every register name
# different -- retail reuses $a0 as the %hi temp of the scalar load it lands in
# and puts the table address in $v0. Six source forms, both -G settings, all 8.
# func_80058A7C packs a GPU word from seven halfwords. Structure and the two
# offset adjustments are right -- the -0x280 has to be s32 arithmetic in a
# local, or gcc folds it to `+ 0xFD80` in a register because the u16 store makes
# the whole expression u16, and naming `arg0 << 8` and the +0x12 test value gets
# their reads into retail's order. 54 -> 52.
# Open: retail emits `sll 16; sra 20` for the (x & 0x100) >> 4 term -- a fused
# sign-extend-and-shift -- where we get two sll/sra pairs. An s16 local, an s16
# lvalue read and a direct (s16) cast all give the same 52, and the rest of the
# 52 is offsets shifting from it.
func_80058A7C
# func_80030D5C is TWO instructions: retail materialises the 0x2000000 mask
# after loading D_8009B0F4, cc1psx puts it in the preceding branch's delay slot
# and takes a nop after the load. Needs _O2_G8_MACRO restored when picked up.
# Naming the loaded word, naming the masked flag, and `f &= 0xBF` as its own
# statement all leave it at 2 (or make it 27). Everything else, including the
# shared store block at .L80030E1C reached with two different values, matches.
# Re-measured 2026-08-03 with the -mno-split-addresses-as-a-delay-slot-lever
# from func_80041D60, which is the same shape one level up: still 2, and the
# lengths are equal (53/53), so the count is real and not a shift. The flag is
# already on this function; what it cannot do is stop reorg hoisting a `lui`
# for a plain *constant*, because only a symbol has a bare form. Swapping the
# `&` operands, spelling the test `!= 0`, and inverting the branch all leave
# the same 2. The lever does not reach this class.
func_80030D5C
# func_80045114 checks the same slot field at n, n-1 and n-2 before appending.
# The addressing needs a -G0 assembler entry (restore it when picked up) and
# then the whole instruction stream is right -- 51 differences down to 38, all
# of which are one rotation: retail holds the D_8009B45C base in $a1 and the
# 0x11 it compares against in $a2, we get $v1 and $a1. Base-first explicit
# address arithmetic does not move it.
func_80045114
# func_800320BC finds the first free 0x10-byte slot and fills five fields from
# a D_801D4244 entry. `k--` as its own statement gets retail's explicit
# `addiu $v1,$a1,-1` instead of the -1 folding into the %lo. What does not come
# out: retail walks the record with ONE cursor at +0x2D58 (offsets 5, -4, -2, 0,
# 2) and cc1psx creates a second at +0x2D56 as well, whichever base the source
# names -- writing the offsets from the record start at +0x2D54 is 49, from
# +0x2D58 also 49, and the original single-expression form 45. Naming the table
# base does not move it either.
func_800320BC
# func_800734DC picks a table byte one of two ways and writes it to a slot.
# Down from 12 to 10 by naming the table base *inside each arm* (a variable
# outside gets hoisted and CSE'd across both, which is not what retail does)
# and by making the `+ 1` in the second arm's index its own statement so it
# stays an `addiu` instead of folding into the load's offset. What is left is
# that retail computes the destination address in the load-delay slot after the
# `lb`; duplicating that statement into both arms to force it is much worse
# (32).
func_800734DC
# func_80059700 picks one of three mode bytes from the sign of arg1, then
# dispatches on it. The three-way sign test matches once written as
# `if (arg1 <= 0) { if (arg1 < 0) ... else ... } else ...` -- gcc turns the
# inner `< 0` into the `bgez` retail has. The recomputed base (gcc 2.8 has no
# global CSE) is right too.
# 21 -> 3 on 2026-07-31. The doubled value and the argument have to be two
# registers ($a2 and $a1), and gcc coalesces them whenever the *pre-switch
# default* is `a = v` -- every arrangement of that (with and without a
# redundant `a = v` in the 0x3E arm, with `a = -v` instead of `a = -a`, with
# the assignment moved into each arm) sits at 21. Making the default `a = 0`
# and assigning `v` in the arms keeps them apart and is 3.
# The three that remain are all "the default should be v, not 0": retail has
# `addu $a1,$a2,$zero` in the first test's delay slot where we have
# `addu $a1,$zero,$zero`, `addu $a1,$zero,$zero` in the 0x23 arm's slot where
# we have a nop, and `subu $a1,$zero,$a1` where we negate $a2 directly. So the
# source wants the default to be v *and* the two registers distinct, and those
# two pull against each other through copy propagation.
func_80059700
# func_800597C8 is 25 out on the scheduling of three independent operations at
# the top -- the shift of arg2, its store to +0xE06 and the `(u16)` for the
# fourth call argument. Retail reads the loop bound before the shift and puts
# the mask in the guard's delay slot; we shift first. Duplicating the three
# into both arms of the guard to force it is much worse (49).
func_800597C8
# func_80015078 is func_80013B68's seven-argument sibling and matches except at
# the exit: retail materialises the return value (`addu $v0,$s0,$zero`) BEFORE
# reading D_8009B0F4 for the final `|= 0x20`, and reads it into $v1; cc1psx uses
# $v0 for the flag and copies the return value after. Nine differences, all the
# epilogue. A separate `ret` local before the flag update does not move it.
# func_80025B28 is 34 out from ONE register: retail keeps the +0x6C flag byte in
# $a0 (free, since arg0 was copied to $a1) and cc1psx uses $a2, which cascades
# through every use and shifts the argument setup of the two calls. The head is
# otherwise instruction-for-instruction right, including reading +0x22 before
# +0x6C and the `andi 0xFF` that a s32 local plus a `(u8)` cast at the
# comparison produces. `f |= 0x80` as its own statement makes it 43.
func_80025B28
# func_80073758 reads in 0x8000-byte chunks until short or done. The whole
# instruction stream matches, including the hoisted `sltu $s6,$s0` in the
# loop-back delay slot and `total += got` in the branch delay slot. Five
# callee-saved registers are rotated: retail has handle/buffer/total/0x8000/-1
# in $s4/$s3/$s2/$s6/$s5, we get $s6/$s2/$s3/$s5/$s4. Sweep flat at 23.
func_80073758
# func_80013B68 has two requirements that pull against each other. Retail
# assigns the four parameters to $s1-$s4 in declaration order, which cc1psx only
# does when they are *used* in that order (14 differences otherwise); and retail
# stores the three bytes at +0x38/+0x39/+0x46 before the three words at
# +0x24/+0x34/+0x2C, which needs the bytes written first. Doing both at once
# does not work: the use order fixes the registers and breaks the store order,
# 7 either way. Copying the arguments into locals in order is worse (16), and
# the sweep is flat.
func_80013B68
# func_8001D5B4 compares two signed bytes against globals and sets a mode. The
# hard part of its shape is visible and not yet expressed: retail funnels the
# early `D_8009B160 = -1` and the `= 2` case into ONE store block at
# .L8001D658, and routes three other paths through a shared `return 1` at
# .L8001D5E8. Two structurings (early returns, and the exit test first with an
# if/else) both give 35, and the sweep is flat -- so this is a layout question
# about which stores share a block, not polarity.
func_8001D5B4
# func_80014EEC dispatches through D_8009B10C if set, otherwise checks the
# D_8009B0F4/D_8009B134 flag pair and forwards seven arguments to
# func_80013998. The flag-check block matches. The rest does not: retail saves
# s0-s3 and copies a0-a3 into them across the dispatch, cc1psx keeps them in
# $t0-$t2 with a 40-byte frame against retail's 56, and places the indirect
# call inline where retail branches to it. 40 differences; flipping the
# polarity so the call is the out-of-line arm makes it 46.
# func_800137E4: 45 differences down to 16 once the three reads of D_8009B0F4
# and two of D_8009B134 got a name each -- without them cc1psx hoists both
# addresses into callee-saved registers and grows the frame. What is left is a
# three-register rotation in the first block (constant/B0F4/B134 in
# $v0/$v1/$a0 against $v1/$a0/$v0). Its sibling func_8003798C has the identical
# first block and matches, so the rotation comes from the loop that follows.
func_800137E4 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800137E4_WAS
# func_80023090 builds two record pointers from a shared table and compares
# them. Read order and grouping are retail's after splitting the index into
# `x = a * 5 + arg0[0xF]` and naming `i * 28` -- 33 differences down to 18 -- and
# what is left is a four-register rotation of the two table bases and the second
# index ($t1/$a3/$t0 against $a3/$t0/$a2). Declaration order does not move it.
func_80023090
# func_800208D4: the first half matches. In the second, cc1psx hoists the
# `D_8009B23A = 2` store and the %hi of D_800E9FF0 above the read-xor-store of
# D_8009B1D5, where retail keeps them in source order; the 24 differences are
# all that reordering. The source order already matches retail, naming the
# pointer before the flag store does not move it, and the sweep is flat at 24.
func_800208D4
# func_8002CBF4 reads D_801D4244[arg0-1] three times, in three blocks, and
# retail materialises the base and recomputes arg0-1 for each -- gcc 2.8 has no
# global CSE, so writing the expression inline three times is right and that
# part matches. What is left is a $v0/$v1 swap in the first arm: retail puts the
# base in $v1 and the loaded value back into $v1, we use $v0 for both. Splitting
# the shift into its own statement is worse (29), and the sweep's best is
# O1 G0 at 21.
func_8002CBF4
# func_80039140 copies and offsets nine halfwords from one object to another.
# The instruction stream is identical, including the three reloads of +0x30 and
# +0x32 that the intervening stores force. One register: retail keeps the source
# pointer in $a1 and we put it in $v1 ($a0 holds the destination in both, having
# overwritten arg0). Three declaration orders and the whole sweep sit at 13.
func_80039140
# func_80038800 is underdetermined between -O levels rather than wrong. At -O2
# the prologue matches (the $s1 copy scheduled between two saves) and the
# registers do not; at -O1 the registers match and the prologue is unscheduled
# -- 27 and 10. Retail has both, which neither level gives. The one real
# difference at -O1 is that retail loads the byte into $a0 and copies it to
# $s0, where cc1psx loads straight into $s0; four source forms for that copy
# (two variables, a named mask, an assignment split from the declaration, the
# store moved) all leave it. Best is -O1 -G8 at 10.
func_80038800
# func_80032370 is two loops over one 16-entry halfword table -- a sweep that
# clears entries whose referenced byte is zero, then a compaction. Both loops
# are instruction-for-instruction right, including the two reads per entry
# (`lh` for the test, `lhu` for the copy, which is a signed test and an
# unsigned store in the source). What is left is a three-register rotation:
# retail has the table in $a2 and the byte base in $a0, and the second loop's
# read/write cursors and counter rotate with them. Four declaration orders and
# inlining the byte base all give 17, 18 or 34, and the sweep is flat at 18.
func_80032370
# func_80045BE8 appends a 48-byte record with a struct assignment -- that is
# where the four-word block-move loop comes from, and Rec48 in variables.h
# exists for it. Addressing and control flow match; what is left is that retail
# puts the D_8009B45C base in $a1 and keeps arg0 in $a0 until the block move
# needs $a1, while cc1psx copies arg0 to $a1 in the prologue and gives the base
# $a0. Every access through either then differs -- 45, almost all cascade.
# Needs the -G0 assembler entry restored when picked up again.
func_80045BE8
# func_800129D8 is crt0: it writes $sp, $gp and $fp and ends in `break 0,1`.
# Filtered by candidates.py now, listed here so nobody re-derives that.
# func_800129D8 is the C runtime entry stub, not a matching failure: it writes
# $sp, $gp and $fp directly and ends in `break 0,1`.  candidates.py's
# HAND_WRITTEN filter already excludes it; the PARKED entry is belt and braces.
# func_8002C604: addressing solved (scalar D_80010000 + -G0 assembler gives the
# lui/lw register reuse retail has). What is left is 22 differences from one
# register: retail keeps the object returned by func_8002C5CC in $a0 and we put
# it in $a1, so every store through it differs. Needs the -G0 assembler entry
# restored when picked up again.
func_8002C604
# func_800338E4: the four accesses to D_8009B146 are right (scalar + -G0
# assembler, no alias) and so is the first half. What is left is a schedule:
# retail reads D_8009B146, then +0x5AA8, then the counter at +0x5AAC; cc1psx
# reloads the counter first, immediately after the store to it in the branch
# above. Naming the three reads in retail's order does not move it, inlining
# them is worse (24), and the sweep's best is O1 G8 at 18. Also note the two
# reads of D_8009B146 differ in signedness -- lh for the subtraction, lhu for
# the sum -- which the `(u16)` cast reproduces.
func_800338E4
# func_80012AE8 -- 26/24, 26 differences, and the diagnosis is now settled: it
# is NOT cc1psx output at all. Re-tried 2026-08-21 and the C is entirely
# reachable. The count `lui $s1,(0x0>>16); addiu $s1,$s1,0x0` is a %hi/%lo
# pair on a symbol the linker resolves to zero, and `config/symbol_aliases.txt`
# is exactly the mechanism for that: `CtorCount_0 = 0x0;` plus
# `extern u8 CtorCount_0[];` and `n = (s32)CtorCount_0;` emits the pair (gcc
# materialises a plain 0 as `addu $s1,$zero,$zero`, so no literal reaches it).
# With that, `as -G0` for D_800906E0 and D_80010000_IS_AGGREGATE, every
# instruction of the body matches in order.
# What does not is the FRAME. Retail's is 0x10 with $s0/$s1/$ra at 4/8/0xC and
# **no outgoing-argument area at all**, and its epilogue puts `addiu $sp,$sp,16`
# *before* `jr $ra` with a `nop` in the delay slot. A two-line control probe
# settles it: cc1psx compiling `void f(void) { func_8004A2F8(); }` -- one call,
# no arguments -- reserves 16 bytes anyway and comes out at 24, and it always
# sinks the stack adjust into the `jr`'s delay slot. No row of sweep_try
# changes either (best 23, at -O1 with the delay-slot macro). So this is
# hand-written assembly or a different toolchain's object, most likely the
# PsyQ startup's constructor walker, and it should be left as assembly.
# The candidate is kept in parked/ as the record of how far C gets.
func_80012AE8
# func_8004C560: retail copies p[0] into a third register and adds there,
# keeping p[0] live until after the sum so it can store it to p[4]. Re-tried
# 2026-07-31 with the "an extra copy is an extra name" lever from
# func_800386B8: `s32 u = t; u += n;` does produce the copy and makes the
# instruction count right, which the old candidate (11, one short) never was.
# What is left at 14 is placement: retail stores the func_8004BC2C result to
# p[2] straight from $v0 and copies it to $a1 afterwards, because $v0 is needed
# for the `return 0`; gcc copies to $a0 first and stores from the copy. Five
# orderings of the two stores and the sum, with and without a variable for the
# call result and for n, all land on 14. The parked candidate is now the
# right-sized one -- working-order step 1 says count first.
func_8004C560
# func_80038798 is ONE instruction: retail has `andi $v0,$a0,0x8000` where the
# masked value lives in $a0; cc1psx folds `(x & 0xFFFF) & 0x8000` back to
# `x & 0x8000` and reads $v0. Both registers are live (the masked value is the
# argument to the next call), so it is a combiner fold and not allocation. Six
# source forms tried -- u16 local, u16 return type on the callee, a named
# intermediate for the test, `&=` in two steps, `(s16)v < 0` -- best is still 1,
# and the whole flag sweep is 1.
func_80038798
func_80040390
func_800594C0
    A stored permuter output measures 15 against 16; not yet decomposed.
    **16 -> 15**, marginal, and still one instruction short (23 against 24) --
    the base was too, so the shortfall is older than this change. The shape is
    `if (p) { four stores } else { the same four stores; return; }` with the
    0x1000 stores after it. Inert: p is `&D_800F2C40[arg0 * 3616]`, the address
    of an array element, so it is never null, `if (p)` is always taken and the
    else arm is dead. Same family as func_8005A3D0's double `break` -- a
    control-flow node with no runtime cost. Verify p cannot be null before
    reusing this shape anywhere else; here that check is what made it safe.
# --- scheduling / rematerialisation, candidate kept in parked/ ---
func_800732A0
    A stored permuter output measures 12, better than this entry. Found by
    tools_src/score_permuter_outputs.py on 2026-08-10 and NOT yet decomposed or
    semantically checked -- two of the audit's finds so far were wrong.
    change is a `short` copy of the loop index used in `p[k + 0x38]`, and
    that alone measures 25 instructions against 24 with 23 differences --
    worse than the base, in both the inner-block and function-scope
    placements. So something else in
    **22 -> 12, and the length fixed** (24 against 24; the base was 23). A
    `short` copy of the loop index used as `p[new_var + 0x38]`, declared at
    function scope while `s32 c = p[new_var + 0x38];` stays an initialiser
    INSIDE the loop body.
    My first attempt at this failed and I wrongly recorded that the diff must
    be hiding something. It was not: I hoisted `c` out of the loop into a bare
    declaration plus an assignment, which undoes the initialiser-versus-
    statement lever that closed func_80045484 three hours earlier. Rebuilding
    the stored output VERBATIM reproduces it. Copy the winner; do not retype
    it.
func_8002CCE4
func_80048C0C
func_800472A8
    **20 -> 1.** Hold the masked argument in an `s16` local and compare
    `(u32)v` instead of `(u32)(arg0 & 0xFFFF)`. Safe because of the guard it
    sits under: the `else` arm is only reached when bit 15 is clear, so the
    value fits a positive s16 and the sign extension cannot bite. Another
    type-width answer, and it only works in this one branch. Found in an
    unscored permuter output.
    Permuter rerun from the new 1 base on 2026-08-10: no output at all, base not
    beaten.
func_800382A8
func_8004733C
func_80040510
func_8005F7B0
func_8004C84C
    A stored permuter output measures 14 and is INVALID: it deletes the
    `*(u16 *)(p + 0x1E) = 0;` store, and the listing has both halfword stores
    to +0x1E (`sh $v0` and `sh $zero`). Its other change, splitting
    `s32 o = 0x180;` into a declaration and a statement, measures the same 25
    and one instruction short on its own.
func_80039E9C
func_80036F80
    **30 -> 11.** The pointer at arg0+0x30 is dereferenced seven times in the
    source; naming it once before the `if` and using the name for the FIRST
    store only -- leaving the other six as inline re-reads -- is the whole
    change. Found in an unscored permuter output; its version also carried ten
    chained `& 0xFFFFFFFF` masks, which are identity and contribute nothing
    (the name alone measures the same 11).
func_80045484
    **8 -> 6** by splitting `s32 i = 1;` into a bare declaration and a separate
    `i = 1;` placed after the other initialiser. The initialiser-versus-
    statement distinction again: an initialiser lands with the declarations and
    a statement lands where it is written. Found in an unscored permuter output.
func_8004A854
func_80016DDC MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80016DDC_WAS
func_8005A618 MATCHED 2026-08-11, first user of the int-cast lever from
    func_80058938's park: the two computations of the same base+offset want
    opposite addu operand orders, so one is pointer arithmetic
    ((D_800F3938 + o)[0x106]) and the other a named integer sum
    (r2 = o + (s32)D_800F3938), whose name also frees it to fill the lbu's
    load-delay slot. Plus the modulo written per return arm -- gcc
    cross-jumps the tail and keeps retail's per-arm folded biases -- and the
    0x1C00 arm written first for the bgtz polarity. The do-while wrapper the
    old entry carried was not needed in the final shape.
    The entry that stood here:
    A stored permuter output measures 15, better than this entry. Found by
    semantically checked -- two of the audit's finds so far were wrong.
    **18 -> 15** with a `do { } while (0);` around the if/else and the trailing
    `return t % 4096;` together -- the whole tail of the function. Fourth use
    of that lever here (func_80026D18, func_80035AF0, func_8004CA60), and the
    second where it wraps a return rather than a statement pair. Semantically
    inert; do not tidy it away. Decomposed from a stored permuter output.
func_80024D34
func_800496C4
func_8005A3D0
    A stored permuter output measures 13, better than this entry. Found by
    semantically checked -- two of the audit's finds so far were wrong.
    **33 -> 13**, and the change is semantically inert: the inner `break` wrapped
    in `if (n || o) { break; } else { break; }`. Both arms break, so behaviour
    is identical and only the structure gcc's jump threading sees changes.
    Same family as the `do { } while (0)` lever -- a control-flow node that
    costs nothing at runtime and is not decoration. Found by
    score_permuter_outputs.py; decomposed and checked before installing.
func_8003B744
    A stored permuter output measures 8 and is INVALID: it casts the index to
    `(unsigned char)` in the second return, and the listing contains no `andi`
    anywhere -- retail does not mask that index. Its other two changes,
    `if (0) {}` and `t += 0;`, are inert and measure the same 18 without the
    cast, so the whole gain was the wrong semantics. Fifth wrong-but-better
    candidate from the permuter on 2026-08-10.

func_80014E1C MATCHED 2026-08-03. The entry that stood here diagnosed it as
  register allocation -- "retail keeps p in $v0 and copies it in with
  `addu $v0,$s0,$zero`; we coalesce p with the D_800E9E60 pseudo" -- and 13
  differences, one instruction short. What it needed was the **prototype**:
  functions.h declared it `void` and PROVISIONAL, so the return value had
  nowhere to live. Widened to `u8 *` and it matched first try, with the same
  goto-to-the-tail shape the entry describes. Nine decompiled callers plus
  func_80014EEC were re-checked through try_func afterwards; all ten MATCH.
  Worth keeping because of what nearly hid it: the stale `parked/` candidate
  from July stopped compiling the moment the prototype changed, and
  check_try_func reported it as "did not compile or diff" in the same run that
  proved the match. A park file left behind after a match is a false failure
  in that sweep -- delete both the file and the entry.

func_80041C8C -- 8 differences, register allocation, one pointer.
  Bytecode interpreter loop: read the opcode at *(u8**)(arg0+0x50) +
  *(u16*)(arg0+0x58), and while it is >= 0xF0 dispatch through
  D_80090FEC[op ^ 0xFF] (which returns -1 to stop). Structure, block layout,
  loop rotation, the duplicated read in the preheader and at the bottom, and
  the whole tail all match. The only fault is that the opcode pointer lives in
  $a0 for us and $a1 in retail; the eight lines are its three uses in each of
  the two reads plus the two tail loads.
  Getting there: the tail must be written `*(s32*)(arg0+0x4C) =
  *(s32*)(arg0+0x54) + ((p[1] << 8) | p[0]);` with the `*(u16*)(arg0+0x58) += 3`
  *after* it -- putting the increment first is 14 and splitting the sum into a
  `val` local is 14 as well.
  Tried at 8: declaration order (p before/after op, with and without a spare
  local), `op = p[0]; p++;` split from `op = *p++`, reading the program counter
  into a local before the two byte loads. Flag sweep: 8 at both -O2 -G8 and
  -O2 -G0, nothing lower.

func_800300C8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800300C8_WAS -- 7 differences, register allocation inside one basic block.
  Writes four halfword pairs into the record at D_8009B2E4 from the geometry
  at D_800EB15C. Everything matches except the second of the two
  `(s16)x / 2` expansions: retail loads into $v1 and lands the halved value in
  $v0, we load into $v0 and land it in $v1, then need the following `addu` to
  move it. $v0/$v1 inside one block with no call in it -- the class WORKFLOW
  says to stop on.
  What got it from 48 to 7, in order, each measured:
  - `u8 *b = D_800EB15C;` as the first local. Without it cc1psx folds the
    first reference's +0x3C into the base register (`%lo(D_800EB15C+60)`) and
    every subsequent offset is 60 too small.
  - `x -= 0x10; x += half;` as two statements. Written as
    `x = x - 0x10 + half` gcc reassociates and applies the -0x10 to the half.
  - a `h` local for the halved value in the *if* arm (18 -> 11);
  - separate `y` and `z` locals for the two halfword values instead of reusing
    one (11 -> 7). Reusing a local for two unrelated values swaps the pair.
  Tried at 7: a second `h2` for the other division, every declaration order of
  `h`, hoisting `n % 10` above the stores (7) and above the `if` (51),
  `y = half; y = x + y;` (8). Flag sweep: best is plain -O2 -G8.

func_800476B4 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800476B4_WAS -- 44 positional differences, really three: a %hi hoisted out of
  a loop, one missing copy, and two induction registers swapped behind them.
  Walks the u16 array at arg0+8 for *(s32 *)arg0 entries; for each entry that
  is not 0xFFFF it writes the display-list cursor into the index table at
  D_8009B45C[0]+0x43C, appends the 8-byte record at arg0+0x1A0+i*8 to the list
  at +0x444 (an 8-byte struct assignment with alignment 1, which is where the
  lwl/lwr/swl/swr block move comes from), adds arg1>>4 to the copied record's
  +6 field, and bumps the count at +0x440.
  The whole loop body matches from `lw $v1,1084($a1)` down. What does not:
  retail materialises `lui $a1,%hi(D_8009B45C)` *inside* the loop and loads
  through the same register, while gcc hoists our %hi into $t3 in the
  preheader. That extra live register is what pushes the counter from $a3 to
  $a2 and the 0xFFFF constant from $t3 to $t4. Retail also has one
  `addu $a0,$a1,$zero` we do not produce, and switches to $a0 for the last
  three accesses.
  Tried: `-mno-split-addresses` hoists the whole `la` instead (worse); a
  second name through Base2_8009B45C hoists *two* addresses; a volatile load
  (`*(u8 *volatile *)D_8009B45C`) does not stop the %hi hoist either; the
  local is already declared inside the `if`, which DECISIONS records as
  stopping the hoist to the top of the *function* but not out of the loop.
  Full flag sweep: nothing changes the hoist.

func_8001EE44 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8001EE44_WAS -- 22 positional differences, two registers and the scheduling
  that follows from them.
  Extracts a 4-bit field from D_801D4244[record->unkC - 1] twice, once per
  argument, choosing shift 18 or 22 on the 0x200 flag at record+0x16, and
  passes both to func_8002CB80. Everything matches but this: retail leaves the
  shift result in $v0 and writes the argument register at the mask
  (`sra $v0,$v0,18` ... `andi $a0,$v0,15`), while gcc coalesces the shift
  straight into $a0 and then masks in place. The knock-on is one instruction of
  scheduling: with the mask in place, gcc can sink it into the following
  `beq`'s delay slot, and retail puts %hi(D_801D4244) there instead.
  The mask must be its own statement after the if/else -- inside the call
  arguments gcc sinks it past the second block entirely (24). Beyond that,
  every attempt to split the unmasked value into its own local costs a
  register: gcc then copies arg1 into $a2 and the count goes to 52. Tried the
  conditional-expression form (which is what fixed func_800595C8's identical
  looking symptom), `a = a & 0xF` versus `a &= 0xF`, u32 locals, and both
  declaration orders -- all 22. Full flag sweep: nothing below 22.

func_800383DC MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800383DC_WAS -- 42 positional differences, one instruction short, and the
  missing instruction is an optimisation retail did not take.
  Builds a far pointer as `(base & 0xFFFF0000) + table[index]` and appends it
  to the u32 array at arg0, indexed by the byte counter at arg0+0x58. Three
  ranges of D_8009B32E pick the table: > 0xCFFF uses D_801C0000 at v - 0xD000,
  > 0x7FFF uses D_801D5800 at v - 0x8000 (these two share the tail), and the
  rest uses D_801C0000 at v (minus 0x100 above 0x500) with D_801B0000 as the
  masked base.
  Block layout, the shared tail, the branch that falls into it, and the whole
  low branch all match. The one fault: in the D_801C0000 arm gcc computes
  `(u32)D_801C0000 & 0xFFFF0000` as the `lui %hi(D_801C0000)` it already has,
  and so never materialises 0xFFFF0000 there. Retail emits `lui $a0,65535` in
  *both* arms and does the `and` at the join. The fold only fires on the symbol
  whose low half is zero -- D_801D5800 gets the real constant in both builds --
  which is what makes it look like a register difference until you count.
  Getting to 42: `s32 v`, not `u16` (a u16 makes every comparison `sltu`;
  retail's are `slt`) -- 49 to 43; and the low arm reached by a `goto` placed
  *after* the shared tail, which is where retail puts it -- 43 to 42.
  Tried at 42: tbl as u32 rather than u8*, a separate `base` local for the
  masked value, both assignment orders inside the arms, both declaration
  orders, and -mno-split-addresses (45, and it loses the `addu` as well).
  Flag sweep: -O2 -G0 reports 23 but only because it also drops the gp-relative
  load of D_8009B32E, which is two more faults, not fewer.

func_8002A3CC MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8002A3CC_WAS -- 26 differences, register allocation driving the schedule.
  Advances a 16.16 fixed-point position held as two halfword pairs in
  D_800EA1E8 (+0x8/+0xC is x, +0xA/+0xE is y, deltas at +0x18/+0x1C), counts
  down the byte at +0x16, and on reaching zero snaps back to +0x12/+0x14,
  clears the active flag at +0x17 and calls func_8003FEE0(0x35) and
  func_8002A2F4. Returns the flag byte, which the early-out path gets from the
  entry load by CSE -- that part matches.
  The fault is one register and everything the scheduler does behind it. Retail
  builds each 32-bit value into $a2 (`or $a2,$v0,$v1`), which keeps it live
  while the second pair's three loads are hoisted above the first pair's
  stores; we build it into $v0 in place, gcc sees $v0 die at the `or`, and the
  stores stay put. Same instructions, interleaved differently, and the two
  pairs' registers rotate with it.
  Tried: both stores after both computations (26), stores in the other order
  within each pair (27), named locals for the two halves (46), `x = hi|lo;
  x += delta;` split (43). Flag sweep: nothing below 26; -mno-split-addresses
  is 34.

func_8003F2B0 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8003F2B0_WAS -- 8 differences, one register.
  Nudges the s16 at arg0+0x60 toward zero by 0x40, clearing the byte at
  arg0+0x6C and snapping to zero when it crosses, then calls func_80043230 and
  (for a non-negative arg3) func_80039934 with D_800EB0F8[arg3]. Everything
  matches except that the working copy of the s16 lives in $a3 in retail and
  $v0 for us; the eight lines are its eight uses.
  $a3 is arg3's incoming register, free after the entry copies it to $s1, and
  nothing else in the function wants a caller-saved register at that point --
  so gcc had every reason to pick $v0 and did. Tried: naming the
  func_80042B98 result so it takes $v0 first, an `s16` working copy (42 -- the
  sign-extensions come back), the working copy scoped to its own block. Flag
  sweep: nothing below 8.

func_80024824 -- 46 differences; the structure is right and the addressing of
  two big constant offsets is not.
  Fills 0x50 six-byte records at D_801A7E20 from two tables that live
  0x1BBC4 and 0x1BB70 bytes past D_8015C424, searches D_80178130 for each
  halfword, and memcpy's the 0x580-byte block the search index selects into
  D_8018C2D8. The record layout, the unrotated search loop (a backward `j`, so
  `while (1)` with the exit inside), and the memcpy all match.
  What does not: retail hoists **0x18000** into $s5 and reaches both tables as
  `IV + $s5` with 0x3BC4 / 0x3B70 as the load immediates, sharing the register
  between the two. gcc gives us the whole 0x1BBC4 and 0x1BB70 with lui+ori
  inside the loop, and folding the offsets any other way moves the fault rather
  than removing it:
  - `base + i*2 + 0x18000 + 0x3BC4` (the literal reading) folds both constants
    into 0x1BBC4 and materialises it whole -- 55;
  - a `vram = D_8015C424 + 0x18000` local gets the immediates right (15300 and
    15216, as retail) but folds 0x18000 into the induction variable, where
    retail keeps the IV over `base` and adds $s5 in the loop -- 46, the best;
  - `q = base + i*2; q += 0x18000;` as separate statements -- 48;
  - writing 0x18000 before the index -- 55.
  Also needed and already applied: a `base` local for D_8015C424, or cc1psx
  folds the first reference's offset into %lo (the func_800300C8 lever).
  Flag sweep on the literal form: nothing below 53.

func_8002ABB4 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8002ABB4_WAS -- 22 positional differences, all scheduling; the instruction
  count is right.
  Clones an object: allocates through func_800400AC(func_8004002C(), 6),
  copies +0x48/+0x18/+0x30/+0x20/+0x44 across, doubles the two halfwords at
  +0x18/+0x1A into +0x3C/+0x3E, zeroes +0xC, calls func_80042918 and
  func_800428EC with `(s8)(arg0[0x16] + arg1)`, then sets +0x10, hangs
  func_8002A9C0 on +0x4C and rewrites the flags at +4. The tail's `$v0` base
  is the `return p;` -- gcc uses the return register for the last five stores.
  Two one-instruction scheduling shifts in opposite directions, so the totals
  agree and the whole diff is alignment:
  - retail hoists `lw $v1,0x48($s1)` above `addu $s0,$v0,$zero` and fills the
    load-delay slot with the copy; we do the copy first and fill the slot with
    the 0x100 constant, which then leaves a `nop` after the 0x18 load;
  - retail leaves a `nop` after `lw $v0,0x20($s1)` and puts `sw $zero,0xC($s0)`
    in the slot before the call; we put the 0xC store in the load's slot.
  Tried: the 0x60 store moved after the 0x18 pair (29) and before the 0x48
  pair (50), the 0xC store moved after the 0x44 copy (50). Flag sweep: 22 is
  the floor, and -fno-schedule-insns does not help -- retail's schedule is not
  simply the unscheduled one.
func_8005C388 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8005C388_WAS -- 11 differences, register assignment around the table base.
  Reads a halfword pair out of D_8009151C at arg0*4, sums the first halfword of
  every earlier pair, and hands the result to func_8005B8A0 through a
  func_8007E600/func_8007E710 pair. Instruction-for-instruction right; retail
  keeps the base in $a1 and the running index in $v1, we get the base in $v1
  and the loop pointer in $a0, and gcc hoists the loop pointer's initialisation
  above the counter's where retail has it after.
  44 -> 11, and each step is a lever already in WORKFLOW.md:
  - the base needs a local, or cc1psx folds the first reference's `+2` into
    `%lo(D_8009151C+2)` and every later offset is 2 too small (4 differences,
    but the wrong 4);
  - that local must be **scoped to before the calls**, or it goes callee-saved
    and costs a register everywhere (44);
  - the loop needs its own pointer variable: retail's `addu $v1,$a1,$zero` is
    a copy, and a copy is a second name (36 -> 18);
  - and that pointer must be assigned *at* the loop, not at its declaration
    (18 -> 11).
  Also: the provisional prototype said `u8 *arg0`; arg0 indexes the table, so
  it is an s32. Corrected in functions.h.

func_80017F04 -- 12 differences: two registers and the scheduling behind them.
  Creates a sprite for the record at arg0 and fills five bytes at +0x67..+0x6B,
  one of which is the record's index computed as a pointer difference divided
  by 28 (gcc factors that into `srl 2` plus the unsigned magic multiply for 7).
  Retail puts the magic constant in $a0 and the D_801D4244 base in $v0; we get
  $v1 for both, and the two `sb $zero` stores then schedule ahead of the
  `lh 0xC($s1)` instead of into the gaps after it.
  47 -> 12 with a base local for D_801D4244 assigned before the +0x68
  statement. Tried at 12 or worse: five orderings of the four byte stores
  (the division first is 20, the zero stores last 51).
  Worth recording separately: this function needs a **per-file prototype**.
  It has three parameters -- arg1/arg2 go to the sprite's +0x30/+0x32 -- but
  func_80018004 calls it with one and retail sets neither $a1 nor $a2 there,
  so that translation unit never saw the real declaration. functions.h now
  guards it on FUNC_80017F04_FULL, which the definition's file defines and
  nobody else does. First function prototype in the project to need the same
  per-file treatment the variables have.
  Re-measured 2026-08-03. The candidate had stopped compiling: it carried its
  own one-parameter prototypes for func_80016778 and func_80016D04, both of
  which have since been decompiled with a second parameter. They are only ever
  stored as function pointers here, so the parameter list is immaterial and the
  fix is to match the real definitions -- but this is the second time a park
  has rotted because the declarations around it moved, and check_try_func's
  parked direction is what caught it. Still 12, and now with equal lengths so
  the count is real.
  Four more spellings of the `- 1` measured against the fold, all 12: a byte
  base with an explicit `* 4`, the constant grouped before the base, `(tbl -
  1)[n]`, and `&tbl[n - 1]` as its own element pointer. gcc distributes the
  `- 1` into the load's displacement every time, which costs one instruction
  and gains a `nop` -- net zero, which is why the length matches. Naming the
  index as a local *costs* an instruction (47) whether declared at the top or
  scoped to its own block. The fold is a symptom of the register choice the
  entry already names, not a separate lever.

func_8005F1B8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8005F1B8_WAS -- 24 differences, and most of them hang off one thing this
  project has not seen before: **retail divides by a literal constant with a
  real `div`**, materialising 750 into $a0 first, where gcc turns `/ 750` into
  the magic multiply at every -O level (0, 1 and 2 all use it here).
  Structure otherwise recovered: `if (arg0 < 2)` with the body inside rather
  than an early return (30 -> 24, the same polarity lesson as func_8003FD14),
  the 0x32 clamp, the -0x12C, the `(s16)` test, and the halve-if-positive.
  The class is small and clustered: four in-scope functions divide by a
  register that was just loaded with a constant, and three of them
  (func_8005F1B8, func_8005F27C, func_8005F5C8) are neighbours, so it is
  probably one translation unit built differently rather than a source shape.
  Probed and rejected: -O0/-O1/-O2 (all magic), -fno-expensive-optimizations,
  -fno-peephole, -msoft-float. `-mno-mult` and `-mint64` are not accepted by
  cc1psx. Whatever turns the magic off is not in the flags tried so far, and
  worth another look when a fifth example turns up.

func_8003C568 MATCHED 2026-08-11, the operand-order seam's third payout in one
  day. The negation spelling gave both stack addus (a frame array flips with
  neither grouping nor the int cast), and the last block was the k-indexed
  load named into a local before the 0x48 store -- retail issues the load
  early, sp0 never escapes so the reorder is safe -- found by the permuter's
  first run on this function, reaching zero in under 30 minutes.
  The entry that stood here:
func_8003C568 -- 38 differences, one scheduling decision and the operand order
  behind it. Copies a 6-byte record from D_8009AF5C onto the stack, appends two
  halfword constants, and writes three fields into the records at D_8009B380
  and D_8009B388. The struct copy needs **`u16 h[3]`, not `u8 b[6]`**: with
  alignment 1 gcc copies the last two bytes one at a time where retail uses
  `lh`/`sh` (51 -> 49). Reading D_8009B37D into a local before the two pointer
  loads gets those into retail's order (49 -> 38).
  What is left: retail emits the 0xC8 store *after* the three loads and we
  hoist it above them, which offsets everything after by one; and
  `addu $v0,$sp,$v0` versus our `addu $v1,$v1,$sp` -- base first, not index
  first. Three placements of the 0xC8 store all give 38.
  2026-08-11: both addus fixed by the negation spelling
  (sp0 + 8 - -(k * 2)) -- the (tbl + i)[K] grouping does not flip a frame
  array, and the int-cast lever is the wrong direction here. Still 38 by
  count because the count is the scheduling shift: what is left is only the
  0xC8/0x48 constant block hoisted above the three global loads, plus the
  nop it leaves after the k-indexed lhu.

func_80013998 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80013998_WAS -- 26 positional differences, all of them one instruction pair
  in the wrong place. Sets up a sprite from eight arguments; every store, both
  nested branches and the shared tail match. The `arg1 |= 0x10000` in the last
  arm is `lui $v1,1` + `or`, and retail emits it first in that block where gcc
  sinks it eleven instructions to fill a slot -- arg1 has no consumer until the
  store at the join, so the scheduler is free to.
  Tried: the `|=` written at the top, the middle and the end of the block (all
  26), -fno-schedule-insns (32) and -fno-schedule-insns2 (28).
  Settles one thing on the way: **func_80013998 returns arg0**, not void. The
  three callers discard it, so the change costs them nothing -- re-checked
  func_80014EEC and func_80015078, both still MATCH.

func_8001352C MATCHED 2026-08-03, and it had been parked for weeks on a
  **misread of the target**. The entry that stood here said the permuter's
  best output "gets there by inserting `(char)0x400`, which under
  `-D__CHAR_UNSIGNED__` is zero, deleting the `addiu $a0,$a0,0x400` the target
  has". The target does not have it: `grep -c 0x400` on its listing is 0,
  where its sibling func_800135FC has 2. The parked candidate was built by
  adapting that sibling and carried the bias across; that single term was the
  whole two-instruction excess and all 27 differences after it were the shift.
  So the permuter was pointing at the right answer by a wrong mechanism -- it
  could not delete the term, so it neutralised it with a cast that evaluates
  to zero, and its "wrong" 19 was closer than our "honest" 27. Diff the two
  *listings* before trusting any constant an adapted candidate inherited, and
  read a wrong-but-better candidate as a pointer at what the target does.

func_800533D8 -- 7 differences, all of them which caller-saved register the
  three values in the first loop get. Everything else matches, including the
  three spin loops on the library calls, the D_800F2B50 stride-0x18 walk and
  the four gp-relative tail stores.
  The loop fills 255 halfwords from sp+0x12 with 0xFFFF. Retail counts *down*
  while the address walks *up*, which is gcc reversing the counter after
  strength reduction has left it live only in the exit test: an index
  expression (`sp10[i + 1] = 0xFFFF` either direction) reproduces neither, and
  an explicit `u16 *q` with `*q++` reproduces both -- 55 differences to 10.
  Writing `i = 0;` before `p = D_800F2B50;` rather than in the second loop's
  `for`-init took it to 7, the same lever func_80061008 needed.
  Retail assigns $v1 to the pointer, $a0 to the 0xFFFF and $v0 to the counter;
  we get $v0, $v1, $a0. Tried: the constant as an explicit local before and
  after the pointer, `sp10[0] = 0` moved below the pointer setup, `i != -1` as
  the exit test (56, much worse), and both declaration orders. Full flag
  sweep: 7 is the floor, at plain -O2 -G8.

func_8003B808 -- 8 differences (was 82). Same levers, including
  func_80020BE4's lever 9. The residue is the shared 4 in case 0 plus 4 in
  case 3, where retail materialises the 0xFFDCFFFF mask before the
  two-instruction 0x18000 and we do it the other way round. A `do { … } while
  (0);` round that pair is **+6 and much worse** -- it breaks the cross-jump
  that merges cases 1, 3 and 4 -- and a named 0x18000 changes nothing. Cases 1
  and 4 have the identical shape and match, so it is specifically the
  two-instruction constant.
  Was: 15 differences (was 82). The fifth member of the family and
  the only one with a real jump table rather than a comparison tree, which is
  why the arm-order levers do not apply but every naming lever does.
  What moved it, in order: `d = D_8009B118;` before the 0x10 store in case 0
  (82 -> 77), the one-name-for-two-constants trick of func_80020BE4 lever 8
  (77 -> 75), case 2's three levers -- named D_8009B118 read, named
  D_8009B0F4 read, the 0x1C store between the two D_801AF000 stores --
  (75 -> 45, and the length error from +1 to 0), and then the plainest one:
  **cases 1, 3 and 4 want the direct `D_8009B0F4 = D_8009B0F4 & mask;`, not
  the `v = D_8009B0F4; v &= mask; D_8009B0F4 = v;` that case 0 needs**
  (45 -> 15). One arm of a switch wanting the local form while three others
  want the direct form is the two-names rule at arm granularity, and it is
  worth 30 differences here.
  The residue: the same 11 as func_80020BE4's case 0, plus 4 in case 3, where
  retail materialises the 0xFFDCFFFF mask before the two-instruction 0x18000
  and we do it the other way round. Cases 1 and 4 have the identical shape and
  match, so it is the *two-instruction* constant that differs -- 0x800 and
  0x2800 are one `addiu` each. Reordering case 3's statements to put the RMW
  first is +6 and much worse, because it breaks the cross-jump that merges
  cases 1, 3 and 4 into the shared tail.
  Was: 82 differences, on `d = D_8009B118;` named
  before the `*(s16 *)(p + 6) = 0x10;` store in case 0 -- the same lever
  that took func_800434F4 from 62 to 42, and here it also takes the
  length error from +2 to +1. Two negatives worth keeping: a *shared* `d`
  across cases 0 and 1 is +6 and 85 (the two-names rule), and the
  statement reorder that is worth 4 on func_80020BE4 is worth nothing
  here -- this one dispatches through a real jump table rather than a
  comparison tree, so its arms are allocated differently.
  Was: 82 differences, **and the diagnosis this entry used to carry
  is now fixed**. It said retail "hoists the D_8009B0F4 load above the two
  halfword stores that follow it" and we did not. Reading the global into a
  local before those stores and writing it back after -- `v = D_8009B0F4;
  ...stores...; v &= 0xFFDDFFFF; D_8009B0F4 = v;` -- puts the load exactly
  where retail has it, in all five case bodies. 95 to 82, and the compound
  assignment is what makes the value its own destination the way retail's
  `and $v0,$v0,$a0` shows.
  It remains the first jump-table function matched against a -G0 assembler:
  the dispatch, `%hi(.rodata)`/`%lo(.rodata)`, `jr $v0` and all four `lui $at`
  stores are instruction-for-instruction right, which is why the "$at stores
  plus a jump table is unmatchable" claim is false for the ten candidates with
  no `%gp_rel`.
  What is left is a **three-cycle register permutation**, the func_800135FC
  class: retail puts the 32-bit mask in $a0, the loaded value in $v0 and the
  0x40 constant in $v1; we get $v1, $a0, $v0. Every instruction is otherwise
  identical and in the same order, so the count is positional.
  func_80020BE4 is the same body without the jump table and lands in exactly
  the same place -- see its entry.

func_8003D03C -- 10 differences, and the -G threshold gets the *addressing*
  right and the *schedule* wrong. Recorded because it is the first case where
  the new lever is measurably worse than the old one.
  Retail materialises %hi(D_8009B408) three separate times -- $v1 for a `lb`,
  $at for a `sb`, $v0 for a `lbu` -- which is the bare form, one expansion per
  reference. Declaring it `[8]` and assembling at -G4 produces exactly that
  (D_8009B3B8 stays gp-relative at four bytes), and the count goes *up*, from
  10 to 39: every bare load then carries a `nop` in its load-delay slot,
  because the bare reference is one pseudo-instruction when cc1psx schedules
  and maspsx only expands it afterwards, so nothing is available to fill.
  Retail fills that slot with the `sw $zero,0x600(arg0)` that follows.
  That toolchain-gap hypothesis is now **checked and mis-framed**: maspsx has a
  `nop_at_expansion` knob, but it *inserts* a nop before an `$at` expansion --
  the opposite of filling a slot -- and neither maspsx nor the real aspsx
  reschedules at all. Scheduling is cc1psx's, so the empty slot is a compiler
  decision about a reference it treats as one instruction, not an assembler
  one. Whatever is left here, patching maspsx is not it.
  Original note, kept because the alternative in it is still open: not
  proven -- it is equally possible retail's unit reached the symbol some way
  neither route reproduces. Left on the old route (aggregate, default flags,
  10) rather than the new one.

func_80048920 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80048920_WAS -- 20 differences, a $s1/$s2 swap and the two operand orders that
  fall out of it. Worth reading for the lever that got it there.
  Retail builds `(arg0 & 0x100) != 0` into a register and shifts it:
  `andi $v1,$a0,256 / sltu $v1,$zero,$v1 / sll $v1,$v1,6`. Every one-expression
  spelling of that -- `(x & 0x100) != 0` shifted, multiplied by 64, `!!`, a
  ternary, an unsigned `0 < ...` -- lets gcc combine the whole thing into
  `srl $v1,$s1,2 / andi $v1,$v1,64`, two instructions where retail has three,
  and the shortfall cascades through the rest of the function (57, and the
  ternary 72). **Splitting it across two statements against the same variable**
  --- `f = arg0 & 0x100; f = f != 0;` --- blocks the combine and reproduces
  retail exactly: 57 differences to 20. Same shape as the two-names rule, in
  the opposite direction: one name reused is what stops a fold here.
  What is left: retail keeps the pre-mask value in $a2 (caller-saved, dead
  before the loop) and the masked id in $s2, with arg1 in $s1. We get one live
  range in $s1 and arg1 in $s2. Splitting it into two source variables is the
  obvious fix and makes it *worse* (65) in all four spellings tried: a separate
  `v` initialised at its declaration, assigned as the first statement, assigned
  after the guard, and reassigning the parameter itself.
  2026-08-11: first permuter run (30 min) on this function, 20 -> 18 with two
  inert names decomposed and verified: the scaled index (arg0 & 0x1F) * 2
  named before f's two-statement mask, and a copy of id for the 0xFFFF
  comparison. The $s1/$s2 swap itself did not move.

func_80031CD4 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80031CD4_WAS -- 8 differences, one scheduling decision in the first block, and
  it is the first case where the `lui $at` expansion *hides* a load-use hazard
  from the scheduler instead of exposing one.
  Retail loads the halfword into $v1, computes the next address in $v0, and
  only then stores it to D_8009B338 -- the `lh $v0,0x2D3C($s0)` fills the
  `lhu`'s load-delay slot. We put the value and the address in the same $v0, so
  the store follows the load immediately and maspsx's `lui $at` expansion lands
  in the delay slot by accident. Same instruction count, one slot apart, and
  everything after it shifts.
  The two operand-order fixes that got it from 10 to 8 are worth reading: retail
  wants `addu $v0,$s0,$v0` (base first) for `p + n * 16`, which `p[n * 16 + K]`
  does not give and `*(p + (n << 4) + K)` does -- the same spelling that closed
  func_8002A2F4.
  Tried: naming the halfword as s32/u16/s16 (all 8), naming the byte as well
  (61), and the full flag sweep (7 at -O2 -G0, which is the same block one
  instruction better and not obviously the right unit flag for a function with
  no gp-relative access left).

func_800245EC -- 68 differences, right instruction count (71 both sides), and
  the whole thing is which caller-saved register holds what in a leaf.
  A three-way dispatch on arg1 that ends each arm with `jr $ra` and a store in
  the delay slot; the addressing is settled (scalar D_8009B0F4 plus a -G0
  assembler gives all four `lui $at` stores and the two same-register loads).
  Retail copies the pointer argument out of $a0 into $a2 as its first
  instruction, keeps the list cursor in $a3 and a shared constant 1 in $t0,
  which leaves $a0 free for the 32-bit masks. We keep the pointer in $a0 and
  spread the masks over $v1. The copy is the usual tell for an extra name, and
  it is not: `u8 *p = arg0;` and an `(u8 *)`-cast s32 parameter both coalesce
  and change nothing (68).
  Reading `*(s32 *)(p + 0x3C)` before the D_8009B0F4 read-modify-write, which
  is the order retail shows, makes it worse (70). Full flag sweep: 57 at
  -O2 -G0 -fno-schedule-insns, still all register naming.

func_8005B4D8 -- 42 differences, right instruction count, and the same class as
  func_800245EC: a leaf with nine live temporaries where every difference is
  which of $a0-$a3/$t0-$t4 holds what, cascading positionally from the first
  instruction.
  A GPU display-list builder: copies a tag word and n payload words into the
  buffer at D_800FE240, appends two terminator words, and links the old buffer
  pointer into the low 24 bits of an entry in the table at arg1+4 before
  advancing D_800FE240 past what it wrote. Everything about the *shape*
  reproduces -- the reversed `for (i = n - 1; i != -1; i--)` with a `*q++`
  cursor walking the other way, the six separate reloads of D_800FE240, the
  one local that stops the seventh, and the scalar-plus--G0-assembler
  addressing that gives both the same-register loads and the `lui $at` store.
  Retail hoists both 32-bit constants (0xE1000200, 0xE6000001) above the first
  read of arg0 and keeps arg0 itself as the cursor; we schedule the constants
  after and copy arg0 into $t1 first. Tried: a separate cursor variable (42,
  one better), hoisting `k` to the top (71, much worse), and the full flag
  sweep (43 at plain -O2 -G8).
  func_8005B260 is its sibling -- same body one word shorter -- and will have
  the same problem. Do not spend on it separately.

func_8002EA0C -- 10 differences, load scheduling in one block plus one store's
  placement. Same category as func_80031CD4.
  A two-axis smooth-scroll stepper: two `div`s to get per-frame deltas, then a
  16.16 accumulator per axis whose high half is written back to D_8009B146 and
  D_8009B148 each call, with a frame counter that snaps both to the target when
  it runs out. The -G4 threshold splits it: %gp_rel for the four-byte
  D_8009B284/88/94/98 and the two-byte D_8009B2A8/2AA/29C/27C, `lui $at` for
  D_8009B146 and D_8009B148 at a declared eight bytes. Both are read `lh` and
  the sized arm is `u16[4]`, so the reads need the lvalue cast
  `*(s16 *)&D_8009B146[0]` -- 12 differences to 10.
  What is left: retail issues all four accumulator loads before the first
  `addu` and we interleave them, and retail's D_8009B29C decrement lands after
  the D_8009B148 store rather than before. Tried: naming the two addends (11),
  naming both sums (10), splitting the counter's read from its decrement so the
  read sits between the two updates the way retail's does (18, much worse).
  Full flag sweep: 10 at plain -O2 -G8.

func_80060E70 -- 7 differences: one hoist we cannot place and one register.
  A seven-iteration loop that builds sprite entries from a pair of parallel
  arrays. Three levers took it from 58 to 7, and the second is new:
  - a named local for `*(u16 *)s`, read where retail reads it (between the
    D_801D5608[0] store and the D_8009B338 store) rather than at its use: 57
    to 9. The read-early lever again.
  - **`-fno-strength-reduce`, and the tell is an induction variable whose
    offsets are all zero.** Retail keeps the cursor at the loop's base and
    writes `10($s0)` every time; strength reduction biases it to base+10 and
    writes `0($s0)`. 9 to 7, and this is the flag's first user for that reason.
  - the earlier `-G0` assembler for the `lui $at` store to D_8009B338.
  What is left: retail materialises D_801D5608's base first in the preheader
  and we do it last, plus one $v0/$v1. Naming that base as a source local --
  the obvious fix -- gives 59 and 22 depending on whether the first store also
  goes through it, which is WORKFLOW's "naming a preheader hoist makes it
  worse" confirmed rather than contradicted. Also tried: the four
  initialisations in a `for` clause, and reordering them (both 7); indexing
  `e + i * 2 + 0xA` instead of a cursor (55).
    **7 -> 5 from the permuter, run on this function for the first time.** Two
    changes, both needed: the store and the read that feeds it chained into one
    expression, `n = (*(u16 *)(q + 0xA) = *(u16 *)p);` rather than two
    statements, and the address of that same halfword named in a local hoisted
    ABOVE the `if` that uses it rather than written inline twice inside it.
    Either alone is 7. What is left is unchanged: retail materialises
    D_801D5608's base first in the preheader and we do it last. A rerun from
    the 5 base has not beaten it.

func_80014B30 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80014B30_WAS -- 50 differences, and an **unfinished** park rather than a near
  miss: the control flow is only half read, so the next move is to re-derive it
  from the listing, not to nudge the candidate.
  What is established. Case 0's `*(e+0x14) == 0` path does not break -- it
  stores 2 to `p+0x40` and **falls through into case 1**. Case 1's
  `*(e+0x18) == 0` path jumps into **case 2's body**, which is therefore both
  a case and the shared exit for two tests. Both case 0's non-zero path and
  case 1's tail reach a shared clamp block at .L80014C04
  (`if (v < 0) *(s32 *)(p+0x1C) = -(v << 11);`). The candidate expresses all
  three with `goto`s into case labels; that construct is load-bearing -- the
  shared blocks are what the target's layout shows -- so do not tidy it away.
  The one measurement that discriminated: assigning `e = D_801D4200` **per
  case** gives 50, a single assignment before the `switch` gives 67, even
  though retail materialises the address before the dispatch. That is
  backwards from expectation and is where the next attempt should start.
  Also tried, no change: an extra name for the `p` argument (retail copies $a0
  to $a2 in a branch delay slot), with and without the per-case `e`.

func_80048658 -- 65 differences, an **unfinished** park: the body is the same
  D_8009B45C table walk as func_80048920 (parked at 20) and the same
  `f = x & 0x100; f = f != 0;` split is needed, but the *call setup* has a
  shape I have not reproduced and the count has barely moved from the first
  attempt.
  What retail does that we do not: it copies all three arguments out of
  $a0/$a1/$a2 into $t0/$a2/$a3 and makes a *second* copy of arg0 into $t1
  before the dispatch, and it computes the last call's second and third
  arguments (`0` and `arg1 & 0xFF`) *inside each arm* rather than at the join,
  along with `*(D_8009B45C+0x444)` and `v * 8`.
  Tried: assigning all four of those to locals in both arms, which is the
  documented cross-jump lever and gets 65 to 63; a second name for arg0; a
  `(u8)` cast instead of `& 0xFF` on the last argument. None of the argument
  copies reproduce -- gcc coalesces them.
  Also eliminated: a narrow signature. Retail sign-extends `$a3` in place
  (`sll 16 / sra 16`) right before the call and masks `$a2` with `0xFF`, which
  is what narrow *parameters* look like rather than casts on s32 locals. But
  `void func_80048658(s32, u8, s16)` with the casts dropped still gives 65,
  and both decompiled callers match either way, so the signature is not it.
  Next attempt should start from the argument-copy question rather than the
  table walk, which is already right.

func_80071EB8 -- 47 differences with the candidate kept, 32 with a worse-shaped
  one, and an **unfinished** park because the two numbers disagree about which
  is closer to the truth.
  Counts entries in D_801AB000 between two bounds that `func_800707C4` writes
  through pointers, skipping ones whose flag word has 0x4000 when the key is
  1, 3, 6 or 8.
  The real question is the two out-parameters. Retail loads them once after the
  call and keeps the lower bound in `$a0` for the whole loop. gcc 2.8 keeps an
  address-taken local in memory unconditionally, so passing `&lo` directly
  makes the loop reload and re-store it every iteration (that shape is 32
  differences). Passing two separate stack variables and copying them into
  `lo`/`hi` afterwards fixes the reload -- the candidate in parked/ -- and
  costs 47 instead, because the
  preheader then orders differently: retail materialises the four comparison
  constants *before* computing the cursor, and reuses the `1` among them as the
  `sllv` shift amount for `lo * 12`. That reuse is why the order matters, and I
  could not make gcc hoist the constants first.
  **Which of the two is closer is unresolved.** The only measurement is 47
  against 32, and it favours the one with the reload; the candidate in parked/
  is the other one, kept because the reload is a concrete thing to have fixed,
  not because it is known to be right. Do not read the choice as evidence.
  Tried: copying only the lower bound (48), only the upper (50), a separate
  loop counter copied from the bound (48). Flag sweep: 43 on the kept
  candidate, 32 on the other, neither at a plausible unit setting.


func_80047DB0 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80047DB0_WAS -- unfinished, 67 differences. The func_80048920 family: a
    0x8000 early exit, a 0x4000-keyed table lookup, then four iterations ORing
    bits into a mask. Needs the -G0 assembler (D_8009B45C is bare); the flag is
    in build.py's parked section so the count is reproducible.
    The unread thing is retail's second copy at the top: `addu $a1,$a0,$zero`
    then `addu $a0,$a1,$zero`, a round trip through $a1 before the first call.
    Three spellings were measured and none of them emits the second copy --
    `func_800464F0()`, `func_800464F0(arg0)`, `func_800464F0(arg0, arg0)` --
    and all three stay at exactly 67, with both sides the same length. So the
    copy is being traded against something else rather than simply missing,
    which is the signal that the shape is structural and not a call-argument
    question. Do not guess a fourth spelling; read what makes $a0 dead across
    the branch.

  2026-08-21, two measured findings that do not yet compose. (a) `p =
  D_8009B45C;` **re-assigned as the last statement of the loop body**, with
  the `while` reading `*(s16 *)(p + 0x4C)`, is what puts the three case
  constants in retail's $t2/$t1/$t0 and the three induction variables in
  $a2/$a1/$a3 -- without it `p` lives in $t0 for the whole function and every
  register downstream is shifted by one. It costs 2 instructions, though: gcc
  then peels the loop's own `if (count == 0) break;` into the preheader,
  where the guard has already proved `count > 0`, and folds it away. Six
  spellings tried to get it back (while(1) with an explicit bottom test, the
  guard or the body test through the symbol instead of `p`, `volatile` on
  either or both reads) and all are -2 or worse. (b) retail's back edge is
  `slt`/`beqz exit` followed by a redundant **`bgez $a2`** (the counter is
  never negative). The only spelling found that emits it is an explicit
  `if (i < 0) break;` as the loop's last statement, which is +1 -- so the
  shape is identified and the source is not. A full sweep_try finds no flag
  row better than the installed 61.

func_80045334 -- unfinished, 52 differences and one instruction too many.
    Structure is fully read and three levers landed on it, in this order:
    the aggregate form of D_8009B45C (retail's `lui/lw %lo` pair, where the
    scalar at -G8 is gp-relative and gcc then parks the pointer in a
    callee-saved register); a *local* `b` for the pointer, because the arms
    reuse it while the default arm and the post-call block re-read the symbol,
    and writing `D_8009B45C[0]` inline in the arms costs an extra `lw %lo`
    each; and the arms as a **switch**, not an if/else-if chain -- retail
    branches out to both cases and falls through into the default, which is
    switch layout, and the chain puts the first arm inline. 66 -> 59 -> 52.
    What is left is allocation and one instruction. `x = a` coalesces: retail
    keeps the parameter in $a0 for the 0x8000/0xF000 tests and the 0x534 store
    while `x` lives in $s3, and every spelling tried collapses both onto one
    register (using `x` for all the tests, using `a` for all of them, and the
    inverse assignment all give the identical 52). `b` lands in $s0 where
    retail has $a1, `t` in $s1 where retail has $v0, and `k`/`x` are swapped
    between $s2 and $s3. Six declaration orders change nothing -- literally
    nothing, all six report 52. The flag sweep's best is 53.
    The remaining +1 is in the default arm: retail fills the second `beq`'s
    delay slot with `x += 0x6000` and the `j`'s with `k = 0x70`; we take a nop
    in the first and put the add in the second.

func_8001944C -- unfinished, 30 differences with equal lengths (70/70).
    Needs the **default compiler with a -G0 assembler**, which is a
    combination the flag sweep could not produce until today: D_8009B0AC is a
    one-byte scalar, small data at -G8, so cc1psx emits one instruction and
    gcc's delay-slot filler hoists it into the preceding branch's slot where
    retail keeps a nop. At as -G0 the same one compiler instruction is a macro
    to the assembler and the nop survives. That row alone was 62 differences
    with a length mismatch to 32 with equal lengths; sweep_flags now carries
    four as-only rows for it.
    Then 32 -> 30 by fixing two offsets (0x8000 + 0x565C is 0xD65C, and I had
    written 0xDC5C) and moving the `b = D_8015C424` assignment after the first
    rectangle store so the two symbols materialise in retail's order.
    What is left is one allocation question that pulls two ways, and it is
    worth reading before spending anything here:
      * Retail copies the table base into a cursor before the loop
        (`addu $a1,$s0,$zero`) *and* re-materialises D_8015C424 after it. With
        one local reused for both, we get the copy and no rematerialisation
        (30). With a separate local for the block after the loop, we get the
        rematerialisation and gcc then walks the first local itself, dropping
        the copy -- 67 instructions, three short. Three loop forms (a forward
        `for` with a cursor, an index expression, `*q++`) all land at 68.
      * The four rectangle stores are 0, 4, 6, 2 in retail. Through the symbol
        gcc reorders them to 4, 6, 0, 2 because it can prove the offsets
        disjoint; through a local pointer the order is retail's, but the local
        then takes $a3 and costs an `addu $a0,$a3,$zero` (+2). The offset-0
        store is `%lo(sym)($hi)` in retail and the others are `n($a0)`, which
        is exactly the mixed shape, so the answer is probably a narrower split
        than "all symbol" or "all local" -- I did not find it.
    Also measured: passing the 0/0x140 value as a third argument to
    func_80081ED4 puts it in $a2 where retail has it (53 from 58), which is
    the "a value left in an argument register is the next call's argument"
    lever pointing the right way, but it then displaces the rectangle pointer
    into $a3. Not installed, because a third argument to a two-argument
    library function is a guess and the length got worse.

func_8003B5C8 -- unfinished, 18 differences and one instruction too many, and
    the whole of it is one induction variable. Twenty bytes of Shift-JIS
    full-width digits are copied from D_80010330 onto the stack, then read as
    ten big-endian halfword keys, each looked up in the four-byte-entry table
    at D_801D9004 and its 1-based position stored to D_800EAFF8.
    Everything matches except the loop bound. Retail eliminates the counter:
    it keeps `a3 = &b[k]` as an address giv, `t0 = k + 1` as a biased biv for
    the second byte, `t1` as the output giv, and tests `slt $a3,$t4` against a
    limit `t4 = sp + 20` computed in the preheader. Note the `slt` -- a
    *signed* compare between two addresses, which is the fingerprint of gcc
    rewriting the original `k < 20` rather than of a pointer comparison in the
    source. We get the same three ivs plus the counter itself, and `slti`.
    Our gcc will not build the address giv for the array the loop **reads**,
    though it builds one for `D_800EAFF8[j]`, which the loop **writes**. The
    first reading of that was "frame-relative access", and it is wrong -- four
    synthetic probes say so, and they cost one try_func run each:
      * the same counted loop over a *global* `u8` array: no giv for the load
        either, so the frame has nothing to do with it;
      * a single read instead of two: same;
      * the exit test moved onto the other counter, so the load's index is not
        the test biv: same;
      * both indices at unit stride: same. In every one, the store's
        destination gets `addiu $reg,$reg,2` and the load's source is
        recomputed with an `addu` each iteration.
    So the measured statement is: **this cc1psx builds an address giv for a
    store's destination and not for a load's source**, and retail's listing
    shows one for a load, which is the thing not yet explained. Not a
    toolchain-version difference either -- 4.6 behaves identically on the probe
    and is 8 instructions worse on the candidate.
    Measured source shapes, all landing worse:
      * `b[k]` and `b[k + 1]` (both spellings of the buffer, struct member and
        plain array): no giv at all, two `addu`s per iteration, 55/57.
      * naming the second index (`m = k + 1` as a loop clause): three
        increments and *equal length*, but a3 holds `k` rather than `&b[k]`,
        so every register in the body rotates -- 42.
      * an explicit cursor for the first byte plus the named index for the
        second: 18 and +1, the entry above. Four increments where retail has
        three.
      * bounding that cursor instead of the counter (`c < b + 20`, and with an
        `end` local): +4 or +5. gcc recomputes `sp + 20` inside the loop and
        compares `sltu`, and the counter's disappearance does not pay for it.
      * indexing through a pointer local (`bp = b; bp[k]`): 33.
    The flag sweep's best is 45 (O2 G8 nosched1+2), which is scheduling noise
    and not this. What would settle it is a second function that indexes a
    stack array by a counted loop variable and whose target *does* show the
    giv; if that one also refuses, the class is the frame-relative access and
    not this function.

func_8003CCD8 -- unfinished, 6 differences with equal lengths (72/72), and all
    six are the order of the last six stores. The controller decoder: 32 bits
    of held state walked from bit 31 down, an auto-repeat counter per button in
    D_800EF6B0, and the result written out as three 32-bit values in halfword
    pairs.
    68 -> 28 -> 6 on addressing alone, and the second step is a **new use of
    the declared-size knob**. D_8009B39C and D_8009B3A2 are gp-relative here,
    so they want the scalar guards. D_8009B0D8 is read one byte at a time
    *inside the loop* and retail re-materialises `lui %hi` every iteration --
    with the unsized-array form cc1psx emits its own %hi/%lo pair and gcc
    hoists the %hi out as a loop invariant, which is one instruction too many
    in the preheader and one too few in the body. The fix is the same
    mechanism as D_80010038's `[4]`: give the symbol a *declared size it does
    not have* (`u8 D_8009B0D8[8]`, guarded on D_8009B0D8_IS_SIZED) and
    assemble at **-G4**. Eight bytes clears the -G4 threshold so the symbol is
    not small data and the assembler expands the bare reference through the
    destination register; the four-byte scalars around it (D_8009B3A8,
    D_8009B390, D_8009B3B0, D_8009B3B4) are `<= 4` and stay gp-relative. And
    because the bare form is *one* instruction to gcc, there is nothing for
    the loop-invariant pass to hoist. -G0/-G1/-G2 all wreck the gp-relative
    group (+20, +18, +12); -G4 is the only threshold that separates them.
    What is left: retail stores the three pairs in source order --
    `sh lo; sh hi` for each of D_8009B3A4/6, D_8009B398/9A, D_8009B394/6 --
    and we sink all three `lo` stores past the `hi` stores, which all reuse
    $v0 from their own `srl`. Measured against it, none of these moves it:
      * writing the pairs hi-first (8, worse);
      * naming each shifted half in a local, one name reused or three separate
        names (both 6, byte-identical to the inline form);
      * declaring each pair as one two-element `u16` array so the stores are
        to a single object (7 -- gcc reorders stores within one object too,
        so aliasing is not the lever here);
      * declaring the two low halves **volatile**, using the guards that
        already exist for them: **byte-identical output**. Worth knowing on
        its own -- WORKFLOW says gcc sinks a non-volatile store past a
        volatile one, and here the volatile store is the one that moves. Do
        not reach for volatile to pin a store order on the strength of that
        rule alone.
      * -fno-schedule-insns (+3, 43), -fno-schedule-insns2 (15), both (+7).

func_800497E0 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800497E0_WAS -- unfinished, 18 differences and one instruction too many.
    A dup-%hi function from the reopened class: three bare reads of
    D_8009B458, no %gp_rel, so the plain scalar arm plus `as -G0` (in
    build.py's parked section). Everything up to the last block matches --
    the two `return -1` exits share retail's block at .L80049864, the
    `(s16)` cast on the third parameter, the `sltu` clamp, all of it.
    The last block is the return value and it is the register-rotation
    shape: retail's return pseudo *is* `$v0`, taking `addu $v0,$s2,$zero`
    in the branch's delay slot and `addiu $v0,$zero,-2` on the
    fall-through. Ours puts the same pseudo in `$a0` (or in `$s2` if the
    accumulator reuses `h`) and copies to `$v0` at the end, and the extra
    name rotates every register in the block by one -- `lui $v1`/`lw $v0`
    where retail has `lui $v0`/`lw $v1`.
    Measured, all +1 except where noted: the accumulator written as
    `ret = h; if (c) ret = -2;` (18, the entry); reusing `h` as the
    accumulator (17); a ternary (18); declaring `ret` first (18); `s32`
    rather than `u32` for the position (18); reusing `avail` for it (23).
    Two plain `return`s remove the copy and get **equal lengths** but
    duplicate the `-1` block that retail shares (20), and inverting that
    test is 21. A single-exit rewrite with one `ret` for all three values
    is much worse (52).
    The permuter is not the answer here: base score 180, no improvement in
    43 iterations, then an internal randomizer failure (traceback in
    src/randomizer.py:2564). That is from running `permuter.py` directly.
    The first attempt, `tools_src/permute.py func_800497E0`, produced a log
    containing nothing but the command it would have run -- because
    **permute.py only prepares unless you pass `--run`**, and a log holding
    just the command reads exactly like a run that finished and found
    nothing. permute.py now says NOT RUN in that case. Tenth instance of a
    tool answering about something it never measured, and the first where
    the tool was telling the truth and the reader supplied the fiction.

func_80047788 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80047788_WAS -- unfinished, 12 differences with equal lengths (55/55).
    Another from the reopened dup-%hi class: three bare reads of
    D_8009B45C, no %gp_rel, plain scalar arm plus `as -G0` (in build.py's
    parked section). 48 -> 18 -> 12 with two source levers, both worth
    reusing.
    First, **gcc folds `(x * 8 + 0x7FF) / 2048` into `(x + 255) / 256`**
    and retail does not, so the whole prologue was wrong by two
    instructions. An intermediate assignment blocks the fold:
    `n = *(u16 *)(p + 2) * 8;` then `n = (n + 0x7FF) / 2048 + 1;`. Writing
    it as one expression, with `<< 3` instead of `* 8`, or splitting the
    `+ 0x7FF` into its own statement instead, all keep the fold or cost
    more (48, 48, 22).
    Second, the tail's address wants its own name. Written as one
    expression -- `*(s32 *)(off + *(u8 **)(p + 0x448) + 4)` -- it is one
    instruction short; reusing `e` for it (`e = off + *(u8 **)(p + 0x448);`
    then `*(s32 *)(e + 4)`) is the missing `addu`. 18 to 12 and equal
    lengths.
    What is left is two `addu` operand orders and one delay slot:
      * **the permuter took this from 12 to 11** (2026-08-03) by splitting
        `e = off + *(u8 **)(p + 0x448);` into the load and the add as two
        statements. The parked candidate carries that now.
      * retail has `addu $v1,$s0,$v1` and `addu $s0,$s0,$v0` -- the offset
        first at both sites, with the *other* operand's register as the
        destination. With `off` a named local we get the base first at
        both. Inlining the index at either site or both changes nothing
        (12 each), and `base + off` in source order is the same 12. This
        is the func_8004DB14 lever pointing the wrong way: there a named
        offset *gave* base-first and that was what retail wanted; here
        retail wants index-first and naming is forced, because the value
        is used again after two calls.
      * retail leaves the `bgez` of the division sequence with a `nop`;
        gcc duplicates the target `sra` into the slot and branches to
        target+4. Naming the quotient separately, naming the biased
        dividend, and four-statement long division all leave it (12, 12,
        23). -fno-schedule-insns / -insns2 / -fno-delayed-branch are all
        worse and change the length.

func_80047CC4 MATCHED 2026-08-03, and the last instruction came from the
    **permuter** -- its second win in this project. Kept as an entry because
    the shape is reusable. Sixth from the reopened dup-%hi class: four bare
    reads of D_8009B45C, no %gp_rel, plain scalar arm plus `as -G0`.
    32 differences to 1 on one source shape: retail's counter wrap is **two
    statements**, `i++;` then `i &= 3;`, which gives `addiu $s0,$s0,1` and
    `andi $s0,$s0,3` against the same register with the mask free to sink
    into the loop branch's delay slot. `i = (i + 1) & 3;` builds a temp,
    emits both before the counter bump, leaves the slot a `nop`, and rotates
    $s0/$s1 through the whole function.
    The last one was an `addu` operand order: retail has the loaded base
    first and every `+` spelling gives index-first, because the base is a
    pointer *value* and not a symbol -- seven were measured (offset grouped
    with the base, constant first, `((u16 *)p + i)[0x202]`,
    `((u16 *)p)[i + 0x202]`, an `(s32)` cast sum, a base local, and naming
    the scaled index, which is 42 and one instruction short). The permuter
    found it at iteration 79 from a base score of 10:

        p + 0x404 - -(i * 2)      instead of      p + i * 2 + 0x404

    A subtraction of a negation is arithmetically identical and puts the
    base first. Unlike the `(char)0x400` the permuter produced for
    func_8001352C, this one changes no value and deletes no instruction --
    read the diff, then keep it. src/func_80047CC4.c carries a comment
    saying so, because it looks exactly like something a later pass would
    tidy back.
    Also recorded: retail passes `1 << ((u8)i + 0x14)` to func_80047C70 in
    `$a0` with a bare `nop` in the delay slot -- computed at the top,
    shifted once per iteration, read nowhere else, so it can only be the
    argument. src/func_80047C70.c declares its parameter `u8 *`, which
    cannot be what this caller passes; the call site matches through an
    implicit declaration and no prototype was added.

func_80049010 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80049010_WAS -- unfinished, 5 differences with equal lengths (56/56).
    Seventh from the dup-%hi class: seven bare reads of D_8009B45C, no
    %gp_rel, scalar arm plus `as -G0`, and the obvious C. Every block is
    right; the five are one register rotation in the first block, where
    retail loads the compared halfword into `$a0` and the base into `$v0`
    and we get `$v0` and `$v1`. Measured, all 5: one pseudo reloaded across
    the call versus two names, `s16` for the shared `-1`, assigning it after
    the first load instead of before, and a base local for the block. The
    permuter ran 828 iterations from a base score of 35 and found nothing.

func_8005B260 -- unfinished, 28 differences with equal lengths (67/67).
    A GPU packet copier and ordering-table insert: six bare loads of
    D_800FE240 plus one bare *store* through `$at`, no %gp_rel, so the
    scalar arm plus `as -G0`. 49 -> 39 -> 28 on two shapes worth reusing:
    the second ordering-table address is a **compound assignment** on the
    offset (`off += *(s32 *)(ctx + 4);`, giving retail's `addu $a0,$a0,$v0`
    where a fresh name gives a fresh register), and the word read back from
    that address wants its own local before the D_800FE240 update, or gcc
    hoists the update's `sll`/`addiu` into the load's delay slot where
    retail takes a `nop`.
    What is left is one register decision that rotates the whole prologue.
    The 0xE1000200 packet header needs a `lui`/`ori` pair, and retail puts
    it in `$a1` -- copying the `ctx` parameter to `$t3` in the function's
    *first* instruction to free it -- while `src` stays in `$a0` and is
    walked in place. We materialise it into `$a0` instead, so `src` gets
    copied to `$t0` and every register after shifts. Two shapes measured,
    both 28 -- naming the whole header expression at the top, and reading
    `*(s32 *)(ctx + 4)` early to make ctx die sooner. The discriminator is
    not established. Do not read that as closed: func_80047CC4 looked shut
    after seven spellings and the permuter found an eighth.
    The permuter was run here (`permute.py func_8005B260 --run`) and found
    nothing usable: base score 1000, eight saved candidates, best 365, and
    that best one is **semantically wrong** -- it hoists
    `new_var = &*(s32 *)src;` out of the copy loop and reads `*new_var`
    inside, so every iteration copies the same word while `src` advances.
    Second instance of the permuter scoring a wrong program better than a
    right one, after func_8001352C's `(char)0x400`; the rule that catches
    both is to read diff.txt before the score.
    Its neighbour func_8005B36C is the same routine with an extra argument
    and a negated count; whatever fixes this one should fix that.

func_8005B36C -- unfinished, **two instructions short**, and it is
    func_8005B260's diagnosis confirmed on a second specimen. Same routine
    with three extra stack arguments and a 0xE2000000 texture-window word
    built from four `(-x & 0xFF) / 8` fields; same recipe (six bare loads of
    D_800FE240 plus a bare store through `$at`, no %gp_rel, scalar arm plus
    `as -G0`).
    Read this entry with func_8005B260's. In **both**, everything is right
    except which register a D_800FE240 read lands in, and in both the
    consequence is a parameter copy in retail that we do not emit:
      * B260 -- retail materialises the 0xE1000200 header into `$a1`, so
        `ctx` is copied to `$t3` in the first instruction and `src` stays in
        `$a0`; we materialise into `$a0`, so `src` gets copied instead.
      * B36C -- retail's *first* D_800FE240 read lands in `$a2`, so `idx` is
        copied to `$t2` in the fifth instruction; ours lands in `$t2`, `$a2`
        survives, and no copy is needed.
    Two specimens, one shape: gcc picks a caller-saved register for a
    global's value and the parameter copy follows from that choice. Nothing
    in the source names the register. Four shapes measured here (a named
    local for the header read, computing the ordering-table offset early,
    the copy loop through its own cursor, and the tail rewritten to retail's
    exact order); the copy-loop cursor and the tail order are real gains --
    86 -> 87 -> 76 differences with the shift, but the *length* gap only
    ever moves between -3 and -2 -- and the discriminator for the register
    is not established.
    Computing the ordering-table offset at the top scores better (76) and is
    structurally *worse*: it hoists the `andi`/`sll` into the prologue where
    retail keeps `idx` raw in a register and masks it late. The parked
    candidate is the honest shape, not the lower number.

func_80048F14 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80048F14_WAS -- unfinished, 15 differences with equal lengths (63/63), all
    in one block. Tenth from the dup-%hi class: three bare reads of
    D_8009B45C, no %gp_rel, scalar arm plus `as -G0`. An init routine --
    three groups of stores through three separate reads of the base, then a
    handful through the pointer it just stored at +0x1564.
    42 -> 15 on two shapes, both worth reusing:
      * the halfword `0xFFFF` store must be `*(u16 *)` and not `*(s16 *)`.
        Through `s16` gcc notices the stored halfword is the same as the -1
        it already has in a register for the four stores above and reuses it,
        which is one instruction short; through `u16` it materialises
        `ori $v0,$zero,0xFFFF` the way retail does.
      * that store's address must be **read back** from the struct --
        `*(u16 *)(*(s32 *)(q + 0x1564)) = 0xFFFF;` -- and not the literal or
        a local holding it. Retail has `addu $a0,$a1,$zero`, a copy of the
        0x801EA800 it just stored; writing the literal or one local gives no
        copy, and two locals both holding it gives no copy either. The
        read-back is what produces it. 45 -> 15.
    What is left is a register rotation in the second and third store
    groups: retail puts the base reads in `$v0,$v0,$v1` and the constant 255
    in `$v1`, we get `$v1,$v1,$v1` and `$v0`, and the `0x1582` store
    schedules before the third read instead of after it. All six orderings
    of the second group's three stores measured -- three give 15 and three
    are a whole instruction worse -- as are naming the 255 and moving the
    third base read earlier.

func_8003700C -- unfinished, 26 differences and one instruction short, and
    the point of the entry is that **the second recipe branch worked on a
    fresh function**. Three bare reads of D_8009B394 (`u16`, 2 bytes) next to
    nine gp-relative accesses of D_8009B34D and two of D_8009B345 (both `s8`,
    1 byte). Window `max(gp size) <= G < 8` is `1 <= G < 2`, so `as -G1` is
    the only threshold, real declarations throughout, no size inflation --
    and it gives retail's addressing exactly. -G2 takes the gp group out
    with it (one instruction worse).
    Two source shapes after that, 61 -> 28 -> 26:
      * D_8009B394 is **volatile** here. Three reads on one path, and gcc
        CSEs a plain read into one; retail re-loads for each test. Guarded on
        D_8009B394_IS_VOLATILE so the three other users keep the plain type.
      * `return 0` belongs *last*, with the whole body under
        `if (D_8009B394 & 0x5008) { … return 1; }`. Written as an early
        `if (… == 0) return 0;` gcc puts the zero block inline after the test
        and branches over it, where retail branches out to a block at the end.
      * the byte counter is read `lbu` and the declaration is `s8`, so the
        reads want `*(u8 *)&D_8009B34D` -- the lvalue cast, not a value cast.
    What is left is one instruction in the second arm. Retail materialises
    the `1` in the test branch's *delay slot* and leaves the following `j` to
    the epilogue with a `nop`; we sink the `1` into the `j`'s slot and save
    an instruction. Five spellings measured -- `goto act` with the restore
    inline, `!(less)`, `>=`, a named `r = 1` before the arm, and the same
    `r = 1` immediately before the test -- all 26 and all one short. The
    discriminator is not established.

func_80013360 -- unfinished, 7 differences with equal lengths (85/85), and
    like func_8003700C it is here to record that a recipe branch worked.
    This is the **third** branch, used for the first time on a fresh
    function: D_8009B3A4 wants the bare form for its six reads and
    D_8009B098 must stay gp-relative, and both are `u16`, so no real
    threshold separates them. Declaring D_8009B3A4 `volatile u16 [4]` --
    eight bytes it does not have -- and assembling at `-G4` gives both:
    8 > 4 takes it out of small data while 2 <= 4 keeps D_8009B098 in.
    Guarded on D_8009B3A4_IS_VOLATILE_SIZED alongside the existing volatile
    and aggregate arms. Volatile for the same reason func_80023FBC needs it:
    six reads in a row that retail reloads each time.
    Source shapes after that, 8 -> 7:
      * the loop is the unrotated `while` -- a forward `j` into the bottom
        test -- so `goto test;` at the top, and the back-edge tests
        `(D_8009B398[0] & 0x800) == 0`, not `!= 0`. Reading the polarity
        off the listing rather than off the sense of the code was the whole
        of one difference.
    What is left is the prologue's address pair. Retail materialises
    D_800E9D28 into **one** register and schedules the offset-0 store
    between the halves -- `lui $s0,%hi` / `sh $zero,%lo($s0)` /
    `addiu $s0,$s0,%lo` -- where we get a separate temp for the `%hi` and
    the address in another register. Five shapes measured, all 7: the store
    through the symbol with the local assigned after, the local assigned
    first, both stores through the local (gcc folds `p[0]` back to the
    symbol form anyway), and the gp-relative `|=` moved to first and to
    second. Discriminator not established.

func_800464F0 -- unfinished, 61 differences with equal lengths (95/95). The
    record compactor: walk D_8009B45C's 0x30-byte slots from +0x80, and when a
    slot's tag byte is 0x24, 0x29 or 0x2B, copy the next slot over it and
    decrement the count at +0x4C without advancing. Seven bare reads, no
    %gp_rel, plain scalar arm plus `as -G0`.
    90 -> 93 -> 61 on two shapes:
      * the three-way tag test is a **switch**, not an `||` chain. Retail
        splits it `beq 0x29` / `slti 0x2A` / `beq 0x24` / `bne 0x2B`, which is
        gcc's binary search over three sparse cases; the `||` chain gives
        three linear compares and no `slti`. Both the loop's test and the
        identical one on +0x7C at the end.
      * one base local for the entry test, the loop-top test and the tag read,
        with the symbol written inline in the move branch and at the bottom
        test -- retail keeps `$v1` live across the loop head and re-reads in
        the other two places.
    What is left is mostly one missing instruction's worth of structure: gcc
    **eliminates the loop-top `count == 0` test** because it CSEs the read
    with the entry test's, where retail keeps it. Three shapes measured for
    that and all are worse and change the length: reading the count through
    the symbol at the loop top (93, -2), storing the decrement through the
    base local instead of the symbol (92, -3), and a `volatile` cast on the
    loop-top read (98, +3). The base-register and the initialisation order
    (`i` in the `blez` delay slot, then the three switch constants, then
    `nxt`, then `off` copied from `i`) are also unreproduced. Discriminator
    not established.
    Its caller func_80047DB0 is parked on the `$a0` argument at the call to
    this function; matching it would settle that too.

func_800601D0 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800601D0_WAS -- unfinished, 14 differences, two instructions short. A linear
    search of D_800F5918's 0x50 eight-byte entries for a function pointer,
    returning the word at +4. Twenty instructions, no %gp_rel, default flags.
    Retail has **three** separate `jr $ra` exits -- an early `return -1` when
    the argument equals func_80089E20, the found `return e[4]`, and the
    exhausted `return -1` -- and gcc merges the two `-1`s into one however the
    source is written. Three layouts measured: `if (f == …) return -1;` before
    the loop with a trailing `return -1` (15), the loop wrapped in
    `if (f != …) { … }` with one trailing return (14), and a `goto` into the
    loop (15). All two short. The found-block's placement follows: retail puts
    it *before* the loop body, reached by a backward branch, and every shape
    puts it after.
    **Permuter, 14 -> 12**, via two changes that are another coupled pair:
    `*(s32 *)(e - -4)` for the found-return and an `inline int inline_fn() {
    return -1; }` called at the tail. The negation alone is 14, unchanged --
    the same lever that closed func_80047CC4 does nothing here by itself. The
    12-difference output is in build/permuter/func_800601D0/output-700-1/; the
    parked candidate is still the honest 14, since a helper function invented
    to hold a constant is not a shape worth propagating without knowing why it
    pays. Discriminator not established.

func_8004CABC MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8004CABC_WAS -- unfinished, 15 differences with equal lengths (20/20).
    Walks 0x2C-byte records from D_8009B458 checking a byte at +0x53C, returns
    1 on the first mismatch and 3 otherwise. `as -G0` (in build.py's parked
    section) for the bare read.
    20 -> 15 on one shape: the count is **two names**. Retail loads it, tests
    it against zero, and then *copies* it (`addu $a1,$v0,$zero`) for the loop
    bound; one name gives one register and one instruction fewer.
    What is left is the inner branch's polarity -- retail branches out on
    `== 1` to the latch and falls through to `return 1`, and gcc inverts every
    spelling. Writing it as `if (== 1) { advance } else { return 1; }` is 20
    and two instructions long.
    **Permuter, 15 -> 9**, and the load-bearing mutation is a semantic no-op:
    `i++; i--; i++;` in place of `i++`. Decomposed -- naming the loop bound a
    second time and naming the `3` are both 15, alone or together -- so it is
    the redundant increment pair and nothing else. gcc's induction-variable
    handling sees a different RTL sequence. The parked candidate carries it
    with this note, because it looks exactly like a typo. Discriminator for
    the branch polarity still not established.

func_8003A198 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8003A198_WAS -- 3 differences, 21/21, instruction-for-instruction identical.
    Register allocation in the second block only: retail keeps the intermediate
    base in $v0 and the final address in $a1, this coalesces both into $a1.
    Two levers are already in the candidate and both are load-bearing. Nested
    `if`s rather than three `if (v == 0) return 0;` statements: 18 -> 11,
    because retail reaches one `addu $v0,$zero,$zero` from all three paths
    while the early-return form emits the first exit inline and folds the last
    pair into `sltu`. And `p = (e = arg0 + v)`, a dead store to a local that is
    overwritten two lines later, acting as an allocation hint -- the permuter
    found it, 11 -> 3. Measured and no better: the same hint in the third block
    (3), `p` as s32 with casts (3), a second permuter run from the 11 base,
    which saturated at 3 in ~4 minutes. Discriminator not established.
    sweep_try.py run 2026-08-10: best is 3, reached at both the defaults and
    -O1 -G0. No flag combination helps.

func_8003CEB8 -- 1 difference, 23/23. CRC-16 CCITT (poly 0x1021) over arg1 bytes,
    outer loop over the buffer, inner loop of 8 shifts. Everything matches except
    the two initialisations: retail materialises zero twice
    (`addu $v1,$zero,$zero` for crc, `addu $a3,$zero,$zero` for i) and gcc
    materialises it once and copies (`addu $a3,$v1,$zero`). The permuter got
    from 2 to 1 with `i = crc & 0xFFFF; return i;` -- reusing the dead loop
    counter as the return value, the assign-to-an-already-dead-local hint -- and
    then saturated. Measured and still 1: `j` instead of `i` for that reuse (2,
    worse), declaring i before crc, crc as u32, a `do { } while (0);` between the
    two initialisations, and `i = 0` as its own statement before `crc = 0` with
    an empty for-init. `u32 i` is 3. So the remaining instruction is a constant
    CSE between two variables that both start at zero, and no shape here stops
    it. Rerun from the 1 base after learning that the permuter is worth
    restarting from an improved base (that took func_8004CA60 from 9 to 1); here
    it produced no output at all in 200s, so 1 is where it saturates from both
    sides. Discriminator not established.
    sweep_try.py run 2026-08-10: best is 1 at -O2 -G0, the same as the current
    base. No flag combination helps.

    **2026-08-28: the candidate file was a permuter dump** -- 700 lines of
    regenerated typedefs and externs around a twenty-line function -- and is
    now the plain C it should have been.  Byte-identical output, and it is the
    reason nobody re-read it.  Check `head -1 parked/<f>.c` for
    `asm(".include ...` before believing a park has been looked at;
    func_80058938 is the other one.
    A second permuter run from the 1 base, 799 iterations, found nothing.
    Four more shapes measured and still 1: the comma-operator init
    (`for (crc = 0, i = 0; ...)`), `do { i = 0; } while (0);` with an empty
    for-init, `crc` as `u32`, and the explicit-guard `if (arg1 > 0) { i = 0;
    do { … } while (i < arg1); }` (3, worse).  `s16 i` is +6.

func_8004A8E4 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8004A8E4_WAS -- 17 differences at equal length (23/23), down from 21 at -2.
    Reads a byte at +3 of a 40-byte record, and if it is not 0x63 uses it to
    index 24-byte records and decrements a nibble at +6.
    What paid: D_8009B458_IS_AGGREGATE for retail's %hi/%lo pair, a named
    scaled offset, and -- the fix for the two missing instructions -- writing
    the first address as TWO statements against one name,
    `e = D_8009B458[0]; e = e + off;`. As one expression gcc folds the 0x180
    into the load displacement (`lbu ...,387`) and flips the `addu`'s operands
    to index-first; split, retail's `addiu $v0,$v0,384` and
    `addu $a1,$a2,$v0` both come back. **A base local re-enables the fold**:
    four spellings with `b = D_8009B458[0];` -- the offset as one expression,
    as two statements, with the base assigned before or after -- are all -2
    again, so the split only works when `e`'s first definition is the load
    itself rather than a copy.
    What is left: retail loads D_8009B458 once into `$a2` and reuses it in
    both blocks; our two symbol reads become two `lw`, and retail has an
    `addu $v0,$v1,$zero` copy before the second multiply that we lack. Those
    two cancel in the instruction count, which is why 23/23 hides them. A
    second name for the byte (`w = v; off = w * 24;`) does not produce the
    copy -- it coalesces, in both the one-expression and two-statement forms.
    The two wants are in tension: the single load needs a base local and the
    base local re-folds the 0x180.

func_800466C8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800466C8_WAS -- 6 differences, 21/21. Three reads of the gp-relative pointer
    D_8009B45C: one at entry, one re-read at the end of the `if` body, one for
    the final read-modify-write of the halfword at +0x40. A local `p` reassigned
    inside the `if` reproduces the first two exactly, and the halfword store of
    -0x40 must go through `s16` -- `u16` gives `ori $v1,$zero,65472` where retail
    has `addiu $v0,$zero,-64`, which is the signed-store rule from the func_80048F14
    entry read in the other direction.
    What is left is scheduling: retail hoists the third `lw` ABOVE the store to
    +0x512, filling its own load-delay slot, and gcc keeps the store first
    because both addresses derive from the same global and it cannot
    disambiguate them. Naming the third read is worse (10), and moving the
    read-modify-write above the +0x512 store is much worse (15). The permuter
    ran from the 6 base and returned 6. Discriminator not established -- note
    this is the mirror of the func_80049CF8 case, where the problem was stopping
    a hoist rather than causing one, and the byte-address cast that solved that
    one has no obvious analogue in this direction.
    sweep_try.py run 2026-08-10: best is 6 at the defaults. No flag helps.
    **6 -> 2, and the "gcc will not hoist that load" wall in this entry is
    WRONG.** Naming the address of the final read-modify-write gets the hoist:
    `q = (u16 *)(D_8009B45C + 0x40);` placed BEFORE the `p[0x49] = 0` store,
    with `q` DECLARED first, and the +0x512 store written against a fresh
    `D_8009B45C` rather than through `p`. All three details matter -- the same
    statements with `q` declared second and assigned after the byte store are
    7, and my own clean rewrite of the permuter's output was 7 until the
    declaration order was copied exactly.
    Semantically safe: the function has no calls and never writes the pointer,
    so every read of D_8009B45C yields the same value.
    What is left is two instructions: retail has a `nop` and one `lw` where we
    emit two `lw`. Sharing one read between the +0x40 and +0x512 uses is 19
    instructions against 21, so retail is not sharing them either.
    Found by harvesting an OLD permuter directory that had never been scored --
    output-205-1 had been sitting there at 2 while the entry said 6.
    Permuter rerun from the new 2 base on 2026-08-10: best output measures 3,
    worse than the base.

func_8004CA60 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8004CA60_WAS -- 1 difference, 23/23. Clears a byte at +0x53C and a word at
    +0x518 of each 0x2C-byte record, for *(u16 *)(base + 0x7FA) records, with
    the base pointer D_8009B458 re-read every iteration. Four levers, all
    load-bearing, taking it 23 -> 12 -> 9 -> 1:
      - the plain SCALAR declaration plus an assembler `-G0`. The default
        gp-relative form is the wrong addressing outright; the unsized-array arm
        gives cc1psx's own %hi/%lo pair, which gcc hoists out of the loop as an
        invariant while retail re-materialises the `lui` every iteration. A bare
        reference is ONE instruction to gcc, so the invariant pass has nothing
        to take. Same mechanism as func_8003CCD8, reached through recipe
        branch 1 (no %gp_rel in the function) instead of an inflated size.
      - a base local REASSIGNED inside the loop: retail uses the pre-loop read
        for the first store of each iteration and the re-read for the second
        store and the bound.
      - naming the stored constant 1 and assigning it before `off = 0`, because
        retail materialises the 1 before zeroing the offset.
      - a `do { } while (0)` around the WHOLE body, which the permuter found
        from the 9 base and which is worth 8 on its own. Third use of that lever
        in the project (func_80026D18, func_80035AF0) and the first where it
        wraps an entire function rather than a statement pair.
    What is left is one instruction: retail's `addu $v0,$v1,$a0` for the second
    store's address against our `addu $v0,$a0,$v1`. Measured: `(p + off) + 0x518`
    (1, installed), `p + (off + 0x518)` (2), `(p + 0x518) + off` (2), an explicit
    `u8 *e = p + off` local (1, no change), and the subtraction-of-a-negation
    `p + 0x518 - -off` (19, much worse -- that trick is not general).
    Discriminator not established.
func_80015780 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80015780_WAS -- 3 differences, 23/23, and they are three adjacent instructions
    in one window. Retail issues both loads -- `lbu %gp_rel(D_8009B141)` and
    `lbu 0x4($s0)`, the second being the argument to func_800156B8 -- BEFORE the
    `sh $zero,8($s0)`, and gcc emits the halfword store first. Nothing moves it.
    Measured: naming both loads in locals and writing them above the store in
    source order (3, unchanged -- gcc reorders anyway), `-fno-schedule-insns`
    (30) and `-fno-schedule-insns2` (30), both far worse. The permuter ran 200s
    from a base score of 60 and never beat it, so it produced no output at all.
    Same aliasing family as func_800466C8 in this batch: two accesses through
    different globals that gcc will not reorder the way retail did, and the
    byte-address cast that solved func_80049CF8 is for causing the opposite
    effect. Discriminator not established.
    sweep_try.py run 2026-08-10: best is 3 at -O2 -G8, the current defaults. No
    flag combination helps.

func_80026C0C MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80026C0C_WAS -- 2 differences, 24/24, and the two candidate shapes that reach 2
    fail in opposite ways, which is what makes this a coupled pair that has not
    closed. Scans five 28-byte records from
    D_801A7AD8[(D_8009B1D5 * 15 + arg0) * 28] for one whose halfword at +0x16
    has bit 0x8000 clear, returning that record's absolute index or -1.
    Retail computes the return value `n + i` in the branch's DELAY SLOT, into
    $v0 directly, and its target block is a bare `jr $ra` / `nop`.
      - With the permuter's extra names (`r = n; new_var = r;` before the loop,
        which is what fixes the register allocation -- without them this is 12)
        and the sum written inline in the `return`, the allocation is right but
        the slot is a `nop` and the `addu` lands in the target block.
      - Naming the sum in the loop before the test puts the `addu` in the slot,
        but into $a2, and the target block copies it to $v0 -- the extra name
        costs the copy.
    So one shape has the right position and the wrong register, the other the
    right register and the wrong position, and both are 2. Reusing `r` for the
    sum instead of a fresh name is 22. Discriminator not established; the
    permuter found the naming from a base of 12 and saturated at 2.
    sweep_try.py run 2026-08-10: best is 2 at -O2 -G8, the current defaults. No
    flag combination helps.

func_80059134 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80059134_WAS -- 8 differences, 35/35. A straight-line block of eighteen stores
    into D_800F2C40[arg0 * 3616] at +0xD70..+0xD9E, no branches. The index math
    and the addressing match on the first attempt; everything since has been the
    order in which gcc materialises the constants.
    20 -> 8 came from ONE line moved: the `*(s32 *)(p + 0xD94) = -0x1388;` store
    belongs four positions later, after the 0xD7C/D/E byte group rather than
    before the 0xD74 word. The permuter found it wrapped in three invented
    locals and a chained `p[0xD7E] = (p[0xD7D] = (p[0xD7C] = 0x80))`; decomposed,
    the reorder alone is 8 and the chaining adds nothing, so only the reorder is
    installed.
    What is left: retail interleaves `addiu $a1,$zero,5000` and
    `addiu $a0,$zero,128` -- the two constants used more than once -- early among
    the stores, while gcc materialises them together at first use. Naming both in
    locals assigned at the top does not move it, measured against both the old
    and the new store order (20 and 8, unchanged either way). Discriminator not
    established.
    sweep_try.py run 2026-08-10: best is 8 at -O2 -G0, no better than the base.


func_80070710 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80070710_WAS / func_80071424 / func_80071460 -- 8, 3 and 3 differences, all
    with the right length and instruction sequence.
    The two `D_800F5B98[func_8007058C()] = <global>` functions went 6 -> 3 via
    siblings.py, which pairs them at ratio 1.000 and 0.933 with func_80071688
    and func_8007164C -- both already matched, both the same one-liner. The
    matched ones put the table base in $a0 and the value in $v1; these two want
    the reverse. $v1 is allocated before $a0, so the table's pseudo has to be
    created FIRST, and writing the address into its own pointer does it:
    `s32 *p = &D_800F5B98[func_8007058C()]; *p = <global>;`. Plain pointer
    arithmetic and a computed byte address are also 3; a base local for the
    table is 11 and a local for the value symbol is 10, so it is specifically
    the whole address that must be named.
    What is left in both is one scheduling step: retail materialises the table
    before the `sll $v0,$v0,2` that scales the index, and gcc emits the shift
    first. The permuter was run on both from the 3 base and returned 6 and 10.
    Cross-checked against gonzaloberteri/ygofm-decomp, which has all three in
    src/manual/ with per-function flag comments. Their C agrees with ours on
    semantics and their annotated flags (-O2 -G0, -fno-schedule-insns2 on two)
    give the same counts as our defaults, so the overlap confirmed the reading
    and moved nothing. Their D_8009B361 is a scalar and ours an array; both
    measure the same.
    One trap from that exercise: `D_800F5B98[...] = D_8009B361;` against our
    array declaration stores the symbol's ADDRESS and measures 4, better than
    the correct `[0]` form at 6. Read the semantics before the count.
    sweep_try.py run 2026-08-10: best is 8 at -O1 -G0, same as the base.

func_8004B374 -- 63 differences and TWO INSTRUCTIONS LONG, 76 against 74, which
    is the whole problem: gcc uses eight callee-saved registers ($s0-$s7) where
    retail uses seven ($s0-$s6), and the extra sw/lw pair is the length.
    Structure is right on the first attempt. It scans records of 0x28 bytes
    while `*(s16 *)(base + 0x510)` allows, comparing two bytes at +0x183 and
    +0x185 against `arg0 & 0xFF` and `arg1 & 0xFF`, ORing `*q++` from
    D_80011434 into a mask on a hit; then a retry loop around func_80076ED0 /
    func_80077090 that exits when the result is 2 or 0; then a byte store at
    `D_8009B458[0][(arg0 & 0xFF) * 24 + 4]`.
    D_8009B458 needs the aggregate arm, and the base is RE-READ every
    iteration -- same shape as func_8004CA60, and the pre-loop read is what the
    first iteration uses.
    74 -> 63 differences by initialising the loop counter FIRST and deriving
    the other two zeros from it (`i = 0; mask = i; ... off = i;`) rather than
    writing three separate zeros: retail materialises zero once and copies it
    twice, `addu $s1,$s0,$zero` and `addu $s2,$s0,$zero`. Length did not move.
    Retail's allocation is s6=arg0, s5=arg1, s4=arg0&0xFF, s3=q, s2=off,
    s1=mask, s0=i. Ours adds an eighth; which value is spilling into it is not
    yet identified. The permuter has not been run.
    The extra register is identified: gcc hoists `arg1 & 0xFF` out of the loop
    into a callee-saved, while retail recomputes it every iteration in the
    first `bne`'s delay slot (`andi $v0,$s5,255`) and keeps only raw arg1.
    Retail does hoist `arg0 & 0xFF`, which is reused by the final store, so
    exactly one of the two masks is loop-invariant to it and both are to us.
    Measured and all still 76/63: naming the masked value in a local inside the
    loop body, `u8 arg1`, and `u8 arg0, u8 arg1` with the masks dropped.
    The permuter reached 75/48 and its candidate is SEMANTICALLY WRONG -- it
    pins a second base name before the loop, never updates it, and reads the
    `while` bound from that stale pointer, where retail's `lh 0x510` uses the
    base it just re-read. Reconstructing the same shape correctly is 76/63
    again, so the permuter's gain came from the bug. Third broken candidate
    from it today.
    **63 -> 25 and the length fixed, from sweep_try.py, which I had not run.**
    `-mno-split-addresses` (the sweep's "O2 G8 macro" row) takes it to 74/74
    with 25 differences: the bare form frees the register the eighth
    callee-saved was eating, so the two-instruction excess is gone. Everything
    else in the sweep is 63 or worse, and the four as-only -G rows are all 63.
    What is left is prologue ORDER: retail saves $s5 and copies arg1 into it
    before saving $s0 and zeroing the counter, and reads D_8009B458 before the
    frame is even set up. Reading the base before `i = 0` and copying arg1 into
    an early local are both still 25.
    **The per-symbol route reaches the same 25 without any compiler flag**, and
    is what the candidate now carries: the plain SCALAR declaration of
    D_8009B458 plus `as -G0`. There is no %gp_rel in the function, so recipe
    branch 1 applies and any -G is free (-G0 and -G2 both give 25; -G4 is 71
    instructions and 72). This matters because -mno-split-addresses is
    file-wide while D_80011434 wants cc1psx's own pair with a separate temp,
    which the flag takes away -- the declaration is per symbol and the flag is
    not.
    A permuter rerun from the 25 base reached 9 and it is INVALID: it drops the
    `& 0xFF` from the second comparison, so the code compares $s5 raw where
    retail has `andi $v0,$s5,255`. Verified by grepping the diff for the andi.
    Fourth wrong-but-better candidate from the permuter today.
    Two more shapes aimed at the hoist, both still 25: writing BOTH masks
    inline (so gcc would common the arg0 one, which the final store reuses, and
    leave arg1's alone the way retail does), and giving arg1's mask a side
    effect so it cannot be a pure loop invariant. Seven shapes total against
    this one decision now; it is not reachable from the source.

func_80058938 -- 8 differences at 81/81, register/schedule class, first user
    of _O2_G8_NOSTRENGTH after func_80017708. The free-slot allocator over
    the 10-record table at D_800F2B50 (stride 0x18) that func_80058838 and
    func_80059AF8 feed. Candidate in parked/, flag in PER_FUNC_FLAGS.
    The path, every step measured through try_func:
    - 79 raw. The two Word4 by-value copies into the found record expand as
      block moves whose destination addresses are forced into registers, and
      strength reduction turns r+0x14 into a giv that every neighbouring
      store gets rebased on (sh -8/-6/-10 off an r+20 anchor). Retail
      addresses everything as displacements off the marching cursor. No
      source spelling removes the anchor -- index-only loop, derived cursor,
      struct members, goto-out-of-loop arm all keep it or break worse -- and
      -fno-strength-reduce is the only lever that does: 55. The goto shape
      is disproved by the prologue: retail computes both argument masks and
      the inner loop's 0xFF before the loop, which is invariant hoisting, so
      the found-arm is inside the loop.
    - 18 after three names: o = off copied mid-arm; the inner loop's dst
      named once and computed in the beq delay slot for both arms; and the
      permuter's find of reusing the dead off as the name for the unkA value
      (off = *(u16 *)(tbl + 0xE06); r->unkA = off;), which also recovered
      the one-instruction length gap -- retail's nop after the lhu that our
      early arg4*2 sll had been filling.
    - 14 with do { } while (0) around the six-statement h chain. Fourth
      function that wrapper moves.
    - 11 with base = D_800F2B50 hoisted above tbl and the unkA store fused
      into the off reuse (off = (r->unkA = ...)). Permuter finds, both.
    - 9 with ONE mask named -- new_var = (arg0 & 1) << 1; placed between tbl
      and off = 0 -- another permuter find. Naming the arg1 mask the same
      way is 77/-4 alone and 78/-3 on top of this base, so the pair is NOT
      the coupled shape it resembles; the asymmetry is measured, not
      understood.
    - 8 with the dst sum cast to integer arithmetic:
      (u8 *)((i + o) + (s32)base). Pointer + canonicalizes the base first
      whatever the spelling (&base[i + o] included); the int cast is what
      lets the sum keep retail's addu $v1,$v0,$t4 operand order.
    What is left is ONE phenomenon: the preheader emits [off=0, base,
    r=copy, end] then [m1 mask, 0xFF] where retail emits the two groups the
    other way around -- the hoisted invariants land after the explicit
    inits for us and before them in retail. sweep_try on the 11 base:
    every non-nostrength row is 55-85. Permuter: 10 min from 11, 30 min
    from 11, 30 min from 8 -- the last found nothing at all, so the
    remaining block-order decision is not reachable by its mutations from
    this base either.

func_8002C6C8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8002C6C8_WAS -- 58 positional at 71/72 under _O2_G8_NOSTRENGTH (third user
    of the flag, and the first where the giv anchor appears WITHOUT a block
    move: the record walk's field references get rebased on a base+0x14
    anchor retail does not have, and the anchor also eats the callee-saved
    register that the D_800F2848 address hoist wants -- so at default flags
    the function is two phenomena and under the flag it is one). Walks 8
    records of 0x20 at D_800EAD88 through a flag-byte cursor at +0x1C;
    D_8009B261/D_8009B264 declared for the first time here. Base local t for
    D_800E9D90 stops %lo(sym+4) folding (the func_800300C8 lever); a third
    base local for D_800F2848 is worse in both flag states, as are field
    refs through p versus q -- the rebase is canonical either way. What is
    left after the flag: the missing D_800F2848 address hoist (one
    instruction short) and register roles. The permuter's first 30-minute
    run produced only a wrong-but-better 46: it moved the unconditional
    D_8009B264 store inside the 0x40 guard and turned the lh at
    D_800F2848+0xE into a byte load -- both contradict the listing -- and
    its inert pieces alone (a p alias assigned mid-arm, a dead identical-arms
    if around the loop increments) measure 50 with the length off. Sixth
    wrong-but-better this week; rerun from a better base when one exists.

func_8003CA5C -- 4 differences at 72/72, a $v0/$v1 swap in one basic block:
    retail keeps the first return's delay-slot 1 live in $v0 and runs the
    D_8009B398 load chain through $v1; we reuse $v0. The path 13 -> 4 is two
    permuter finds, both inert and decomposed: the +0x5A halfword read named
    into a local, and a dead identical-arms if on D_8009B378 around the
    second return 0 -- which is what finally stops gcc folding that test
    into a setcc; the plain returns, a named mask value, a nested
    accumulator, a do-while wrapper and a dead-if INSIDE the arm all fold
    (13, 13, 12, 13, 14). Two permuter runs from 13 and from 4 saturated,
    and a named return-1 to keep the constant live does not move the chain
    register. First user of D_8009B398_IS_AGGREGATE outside its own note.

func_80045208 -- 25 differences at 75/75, register-allocation class. The
    command builder that fills the 0x24 packet from the a & 0xF000 switch
    (the addu with the mask register in case 0x8000 is w = a + m against
    the named switch value; the default arm re-reads D_8009B45C inline
    because the sh through q blocks CSE, while the cases read fields off
    the local). Two decisions differ from retail and both are allocation:
    the switch-join pointer e lives in a callee-saved where retail keeps
    it in $v0 dead at the call (declaration order does not move it), and
    retail's q lives in $a1 -- the second parameter's register, free
    because arg1 is declared and never read; the existing two-parameter
    prototype in functions.h is load-bearing for that. Two 30-minute
    permuter runs produced only outputs that score better on its weighted
    metric and measure 42-77 through try_func against the base's 25 --
    the divergence documented in WORKFLOW, in its strongest form yet.

func_8005F27C MATCHED 2026-08-18, together with func_8005F5C8 and on one
    shape -- see that entry. What stood here follows.
    ONE instruction: the multiply's operand order. Everything
    else in the 79 matches, including the real `div` that aspsx wraps with
    break 7 / break 6, which is the interesting half of the function: gcc
    only emits a runtime div when the divisor is not a compile-time
    constant, so retail's `/ 750` went through a local variable. Written as
    a literal it becomes a reciprocal multiply (mult/mfhi/sra) and the
    function is seven instructions short. `k = 750;` assigned inside the
    block that divides gives retail's `addiu $a0,$zero,0x2EE` and the div;
    assigned at the top of the function it is 78 and the length is wrong.
    What is left is `mult $v1,$v0` against our `mult $v0,$v1` -- d first
    versus t first, same registers, same result. Measured: with the product
    assigned to `t` (t = d * t, t *= d, t = t * d, t = (s32)d * t, all
    four) gcc puts the destination's own register first and emits (t, d);
    with a fresh name `u` it emits retail's (d, t) and then allocates the
    product to $v1 instead of $v0, which is three differences instead of
    one. So the two halves are reachable separately and not together: the
    product needs a pseudo distinct from t (for the operand order) that
    lands in t's register (for the allocation). Also measured and not it:
    24 declaration-order permutations of (v, d, t, k), five of (v, d, t, k,
    u), a two-step `t = d * t; t = t / k;` (14), `v = d * t / k + v` (3),
    `d = t * d` (7), the full flag sweep (every row 1 or worse, and both
    as-G rows also 1), and a 4700-iteration permuter run from this base
    that never beat the base's own score of 10.

func_8004C420 -- 16 differences, two register swaps and one delay slot that
    follows them. The structure is settled: retail branches out on
    `(c & 0x80) != 0` so the `== 0` arm is written first; the second name
    `d = c` is real and load-bearing (without it the function is 65 and a
    length mismatch, because b = c makes c live across the whole tail); and
    the three-way tail is `if (x != 0xF0) { if (x != 0xFF) goto other;
    BE88; } else { C0AC; } return 0; other: BE80;` -- the goto is what puts
    C0AC before BE80 in the layout, where every if/else and switch spelling
    puts BE80 first (switch 39 and a length mismatch, an if/else-if chain
    35, early returns 20, the goto 16).
    What is left: c and d are swapped ($a1/$a0 in retail, $a0/$a2 here) and
    so are v and t ($s1/$s3 against $s3/$s1), and because our `c & 0xFF` is
    then CSEd across the join it fills the delay slot retail gives to
    `addiu $v0,$zero,0xF0`. Measured and not it: all 24 declaration-order
    permutations of (c, v, t, d) -- every one 16 -- routing the two 0xFF
    tests through d instead of c (65 and a length mismatch, both of them),
    `s32 v` with an explicit (u32) cast on the shift, and a named index for
    the table lookup (20).
    A 25-minute permuter run from that base found one thing and it is
    installed: naming the 0xF0 the tail compares against (`m = 0xF0;` then
    `!= m`) is 16 to 15. Semantically a no-op, verified by reading diff.txt
    rather than trusting the score, and the same shape as the named-1 that
    matched func_8005611C -- a constant the compiler would otherwise
    materialise wherever it likes becomes a pseudo with a birth position.

func_80049FB4 -- 74 differences, and listed here as a *decode* rather than a
    near miss, so that the reading is not redone. The arithmetic is settled
    and reads back cleanly:
        n = (s16)(arg1 + arg3);  q = n / 128;  r = n % 128;
        d = (s16)(arg0 + q - arg2);
        d >= 0:  return D_80010834[(d % 12) * 128 + r] << (d / 12);
        d <  0:  m = -d;
                 return D_80010834[((12 - m % 12) % 12) * 128 + r]
                        >> ((m + 11) / 12);
    Both `/ 12` are reciprocal multiplies by 0x2AAAAAAB (mfhi, sra 1,
    subtract the sign), so the divisor really was the literal 12 -- the
    opposite of func_8005F27C's `div`, and the two together are the pair
    that makes that rule readable.
    What is open, in the order it shows up: retail sign-extends the sum
    *into the parameter's own register* and the copy that costs fills the
    `bgez` delay slot, where every spelling tried leaves a nop there and
    computes the quotient candidate in one register; and retail falls
    through into the d < 0 arm (`bgez -> the positive arm`) where gcc
    inlines the positive one whichever way the source is written.
    Measured and not it: `s16` versus `s32` for n and r in four
    combinations, `n - q * 128` against `n % 128` (77 and a length mismatch
    against 74 and the right length, which is what is installed), an
    `s16 arg1` reassigned in place, and if/else against two returns.

func_8002525C MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8002525C_WAS -- 32 differences, and the aligned diff says they are ONE
    problem rather than several. The structure is settled and every piece of
    it was a documented shape:
      - the two tails are written out in full, not reached by a goto. The
        discriminator is in the listing: D_800E9FF0 is materialised twice,
        one per source write, and gcc cross-jumps the common suffix starting
        at the `sll` -- a goto-into-a-label version materialises the base
        once, in the tail.
      - the index is a named local in each arm (`i = D_8009B1D5 ^ 1;` then
        `&D_800E9FF0[i]`). Written inline the D_8009AF38 read is scheduled
        into the arm prefix instead of the shared suffix and the function is
        three instructions long.
      - the second tail is reached by `goto`, which is what puts the
        func_8002C68C block in the natural flow and the tail after it; the
        if/else spelling puts them the other way round.
      - the call's result is assigned to `p`, the same name the tails use.
        A fresh name gives no `addu $v1,$v0,$zero` -- retail's copy is the
        extra name, and reusing p is what produces it (38 to 32).
      - D_8009AF38 has to be declared with a size <= 8. The listing reaches
        it as `addiu $v0,$gp,%gp_rel(...)`, which cc1psx only spells while
        the symbol is small data; unsized gives its own %hi/%lo pair.
      - D_8009B22A is read `lh` here and is a u16 elsewhere, so the test is
        `*(s16 *)&D_8009B22A == 0`. The value cast does not do it.
    What is left is one register rotation in the shared tail: retail puts
    the record base in $a0 and reuses $v1 for both the D_8009AF38 byte and
    the record halfword, and that anti-dependence is why retail has a
    load-delay `nop` we fill. Rotate the allocation and the nop comes back
    for free -- so the whole 32 is one allocation, not two problems.
    Measured and not it: all 120 declaration-order permutations of the five
    locals, every one exactly 32.

func_80022D94 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80022D94_WAS -- 8 differences, and they are a coupled pair in the strict
    sense: each half is reachable alone and the two do not compose yet.
    The store order in the source already matches retail exactly (B204,
    B1EE, B192, B190, B166, B1C4, B15C, B158, B224, then the four deltas),
    which is why no permutation of it moves the number -- three orderings
    tried, 8, 11 and 18. The only float is the two `sll`/`ori` pairs sitting
    four slots early.
    `-fno-schedule-insns2` puts the pairs exactly where retail has them and
    comes out at **9**: it then emits `sh D_8009B166`, `sw D_8009B1C4` and
    `sw D_8009B158` each about four slots early instead. So one flag fixes
    the pairs and breaks three stores, and the default fixes the stores and
    misplaces the pairs. Naming the shifted values into their own locals is
    42 under either flag, and putting all four pairs before the stores is
    42 as well.
    Worth knowing for the divisor rule: all four divisions here are real
    `div`s wrapped in break 7 / break 6 because the divisor is the *first
    parameter*. That is the same rule func_8005F27C needed a local for and
    func_80049FB4 shows the other side of.
    UPDATE: 9 -> 7 under the flag, from a 25-minute permuter run, and both
    of its mutations are no-ops read out of diff.txt: a named pointer for
    the first read (`pa = (s16 *)D_800F2848; a = *pa;`) and swapping the
    D_8009B1C4 and D_8009B166 stores. Measured on the installed candidate:
    7 under -fno-schedule-insns2, 10 at default flags -- so the flag row in
    build.py is the right base and stays. The three stores still floating
    about four slots early are D_8009B166, D_8009B1C4 and D_8009B158.

func_80050F24 -- TWO differences, and both are one instruction: retail
    computes a halfword's address into $v1 and loads through it, where we
    reuse the index's own $v0 for the sum. The rest matched on three
    documented levers.
    The second cursor is built from the *symbol*, not from the first one:
    `q = D_800F2C40 + 0xCF8 + arg0 * 3616;` gives retail's `addiu` on the
    base plus a shared index (and the addiu fills a load delay slot), where
    `q = p + 0xCF8;` is one instruction off it and 65 differences.
    All three `base + index` adds want the base first, which needs the
    scaled index in a local -- `o = n * 118;` then `p + o + 0x7C4`. Written
    inline every one comes out index-first, exactly as func_8004DB14
    recorded.
    And the call whose $a1 is never set is not a missing prototype: $a1
    already holds `p[0xDFE] + 3` from the index computation, so the source
    passes it and gcc reuses the register. Same family as func_8003B6AC.
    Measured and not it for the last two: naming the halfword's address,
    scaling inline instead of in the local, an explicit paren around the
    base-plus-index, and four separate index names instead of one reused.

func_8005F5C8 MATCHED 2026-08-18. The answer was `u = d * t;` into a FRESH
    name followed by `t = u;`: the fresh destination gives retail's (d, t)
    operand order, because expand_binop swaps a commutative operator's
    operands when the destination is one of them, and the copy back links
    the new pseudo to t's register, which is where retail puts the product.
    The copy coalesces away. Either half alone is three differences, which
    is why nine hand shapes and two permuter runs all floored at one.
    What stood here follows.
    ONE difference, and it is the SAME one as func_8005F27C:
    `mult $v1,$v0` against our `mult $v0,$v1`, same registers, operands
    swapped. That matters more than either function alone. The candidate is
    func_8005F27C's, adapted in four lines (a fourth parameter, two extra
    arguments on the tail call, func_8005D994 for func_8005F3B8), and it
    came out 83/83 at one difference on the first compile -- so the residue
    is systematic and not this function's register pressure.
    The mechanism, as far as it is established: gcc's expand_binop swaps a
    commutative operator's operands when the destination is one of them, so
    `t = d * t` and `t = t * d` both emit (t, d) with the result in t's
    register. A fresh destination emits retail's (d, t) and is then
    allocated $v1 -- d's own register, which dies at the multiply -- where
    retail has $v0. Both halves are reachable and they do not compose.
    Measured here as well as on the sibling: fresh name declared first and
    last, the one-expression form, `t = t * d`, and `v = v + d * t / k`.
    Solving it is worth two functions, so it is the best permuter target in
    the park list.

func_8002C7E8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8002C7E8_WAS -- (re-read 2026-08-18 against the loop-guard lever: its
    `if (arg0 != 0) { for (i = 0; i < 3; i++) ... }` is a genuine guard, not
    the loop's own entry test, because the bound is the constant 3 and the
    condition is a parameter. The lever does not reach it.)
    17 differences and every one of them is a single register
    rotation: retail's loop counter is $a3 where ours is $a2 (both loops),
    and in the fill loop retail has the record cursor in $a1 with the slot
    cursor in $v1 while we have them the other way round. No instruction is
    missing or extra.
    NOTE: this function carries `PER_FUNC_AS_FLAGS["func_8002C7E8"] = "-G0"`
    in build.py while it is parked, so try_func compiles it that way. That
    is deliberate and load-bearing: there is no %gp_rel anywhere in the
    function, so recipe branch 1 applies, and the scalar declaration of
    D_8009B1D5 plus a -G0 assembler makes the reference ONE instruction to
    gcc. Under the _IS_AGGREGATE arm cc1psx emits its own %hi/%lo pair, gcc
    hoists the `lui` out of the block and into the search loop's load delay
    slot, and retail's `nop` there has nowhere to come from -- the same
    delay-slot use of the knob as func_800151D8 and func_8003CCD8.
    Three other shapes were needed and all of them are recorded elsewhere:
    the two stack arrays are declared out[3] before sl[5] (the frame layout
    is declaration order, and the wrong way round is 69 differences); the
    search loop is `while (1)` with the exit inside, because its back edge
    is an unconditional `j`; and the halfword the match loop walks is
    written as `*(u16 *)(t + n * 2)` so gcc builds the giv itself -- an
    explicit `c` cursor puts its initialisation and its `+= 2` two slots
    early each and costs seven.
    Measured and not it: all 24 declaration-order permutations of the four
    counters, an explicit slot cursor with the two increments in both
    orders, and `c = t + 2` before the advance instead of after.


func_800260D0 -- 25 differences, all of them the scheduling of one block:
    the one between func_8002C604 returning and func_8003FEE0 being called.
    Retail evaluates every input of that block first -- %hi/%lo(D_80090800),
    `lh D_8009B20C+2`, `lbu D_8009B1D5` -- and only then stores into the
    record; we build the table address just before its use.
    Four things got it from 93 to 25 and three are worth reading:
      - D_8009B20C is reached as `%gp_rel(D_8009B20C + 0x2)`, which is a
        member access on a SMALL-DATA object. An unsized array is never
        small data whatever -G says, so the declaration carries a size --
        `s16 D_8009B20C[4]`, eight bytes, still clearing -G8. Unsized costs
        a %hi/%lo pair at every touch and seven instructions of length.
      - the `>= 5` arm is the `else` of a `< 5` body, not an early return:
        retail branches out on it and puts it last.
      - the counter's store has to be written inline in the compare's own
        statement -- `D_8009B20C[1] = *(u16 *)&D_8009B20C[1] + 1;` then a
        fresh read for the test. Through a named `m` the store is scheduled
        after the sign extension instead of before it (31 against 25).
    Measured and not it: the index written four ways (the constant grouped
    with the base, with the index, flat, and in a named offset -- 61 for the
    named one), naming the table base (26, and func_8002778C already says
    base locals are punished), naming the two loaded values above the first
    store (27) and below it (25, no change), moving the whole `e`
    computation above the +0x1A store (33) and the `m` read alone (32).
    Progression for anyone picking this up: 93, 40, 31, 25.
func_80049920 -- 21 differences, 81/81, and they are all the *order* of the
    prologue: retail sets the counter to zero between two register saves and
    fills the guard's delay slot with the accumulator's zero, where we group
    the initialisations. No instruction is missing or extra.
    NOTE: carries `PER_FUNC_AS_FLAGS["func_80049920"] = "-G0"` while parked.
    There is no %gp_rel in the function and D_8009B458 is a four-byte scalar
    pointer, so at -G8 it would be gp-relative; retail reaches it lui/lw
    through one register, which is recipe branch 1 -- the scalar arm plus a
    -G0 assembler. Fifth use of that knob in this session.
    Three shapes were needed to get here and two are already rules:
      - no outer `if` around the loop. The `blez` in front of it is the
        for's own entry guard; writing `if (n > 0) { for (...) }` emits two
        tests and two extra instructions, exactly as func_80072DC0 did.
      - the inner do-while gets its OWN name for the handle pointer
        (`s = t;`). Retail has a copy into a sixth callee-saved register
        right where the inner loop starts, and without it we use five
        registers, a smaller frame and 81 differences. The extra copy is the
        extra name, again.
      - `(D_8009B458 + o)[0x18D]`, not `D_8009B458[o + 0x18D]`: the adds
        want the base first.
    Measured and not it for the last 21: the for-clause in both increment
    orders, the counter initialised before the accumulator, the cursor
    initialisations sunk into the for-init, and all three written as copies
    of the counter (retail's `addu $s3,$s4,$zero` shape).

func_8004A0FC MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8004A0FC_WAS -- 38 differences, and the candidate installed is deliberately
    NOT the lowest-scoring one. Two shapes of the three-way volume test were
    measured at equal length:
      - `if (vol < 0x40) {...} else if (vol == 0x40) {...} else {...}` is 28,
        but it puts the `< 0x40` block INLINE where retail has it last, so
        five instructions are in the wrong place and the count is flattered.
      - `if (vol >= 0x40) { l = t; if (vol == 0x40) ... } else { ... }` is 38
        and has retail's block order exactly. Its extra count is one register
        rotation (retail keeps the volume in $a2, we get $a0) plus the same
        tail interleave both share.
    The second is installed because a rotation is what the permuter is for
    and a misplaced block is not. This is the ranking hazard WORKFLOW warns
    about, in a form where both candidates are the same length.
    Measured and not it: all 24 declaration-order permutations of the four
    locals (every one 38), and hoisting `r = t` out of the branch to explain
    retail's `addu $a1,$v1,$zero` in the delay slot -- that reads like the
    unconditional-then-conditional shape but costs three instructions here
    (93 built against 96) in both polarities.
    What is left besides the rotation: retail interleaves the two final
    products -- sra, andi, mult, re-read p[0xE], mflo, sra, andi, mult, sra,
    sh, mflo, sra, sh -- where we finish one chain before starting the other.

func_80031574 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80031574_WAS -- 24 differences and ONE missing instruction, all of them in
    the tail block after the last call. Everything up to there matches: the
    ten-argument call, the shifted-and-masked switch selector, and the four
    cases with 0x14 and 0x17 sharing one body (two separate bodies with the
    same statement do NOT cross-jump here and cost two instructions; the
    shared `case 0x14: case 0x17:` spelling is what gives retail's three
    blocks plus a shared store).
    What is missing is retail's `addu $v0,$s3,$zero` -- a plain copy of the
    object pointer into a caller-saved register, with all ten tail stores
    then going through the copy. Writing `e = o;` before the block does not
    produce it: gcc coalesces the copy away and stores through $s3, which is
    correct code and one instruction short. This is the extra-copy-is-an-
    extra-name tell in the case where the obvious extra name is not enough.
    Measured and not it: the tail name in both orders, the four cases as
    separate bodies (+2 length), `case 0x17` written first, and the switch
    in plain ascending order.
    Three more spellings of the copy measured afterwards and all identical
    at 24: `e = (u8 *)(s32)o;`, `e = o + 0;` and `e = &o[0];`. gcc coalesces
    every source-level copy of a pointer into itself.
    UPDATE, 24 -> 14. The permuter found it and the mechanism is worth more
    than the number: a copy of a pointer into ITSELF coalesces away, but an
    assignment of a *derived* address does not -- `o = e + 8;` is a dead
    store (o is never read again) that gcc still allocates, and allocating
    it is what produces retail's `addu $v0,$s3,$zero`. Any non-zero offset
    works (4, 8, 0x18, 0x1A, 0x24, 0x44 all give 14); `o = e + 0` folds back
    to the plain copy and is 24 again. Where it goes matters as much: at the
    TOP of the tail block it is 14 with retail's copy in the right place, at
    the bottom -- which is where the permuter first put it, inside the last
    expression as `*(u16 *)(o = e + 8)` -- it is 16 with the copy emitted
    after all the stores.
    This is the dead-assignment register hint (func_800135FC) in its
    sharpest form yet: the assignment has to be of something gcc cannot
    prove equal to what it is copying.
    The permuter's own best-by-score output was a different candidate that
    retyped the fifth parameter to `unsigned long long` -- it measures 16
    but the caller (func_8003353C) stores exactly one word at 0x10($sp), so
    the parameter is 32 bits and that candidate is wrong. Reading the caller
    is what settled it.
    What is left at 14: retail does the +4 word's read-modify-write before
    the +8 halfword's and we do them the other way round.

func_8003C628 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8003C628_WAS -- 45 positional differences at +1 length, and the count is
    inflated: the real edits are about six instructions misplaced inside two
    otherwise-identical blocks. The function is otherwise settled -- three
    object allocations, a nine-argument call, an eight and a ten, and the
    handles stored to three gp-relative globals.
    The addressing is worth the entry on its own. D_8009B408 is reached
    through ONE register (`lui $v1` then `lbu $v1,%lo(...)($v1)`), which is
    the assembler's own expansion of a bare symbol, not cc1psx's pair --
    under the _IS_AGGREGATE arm cc1psx emits a two-register pair and the
    function is three instructions long. So it takes the _SIZED arm (eight
    bytes) and `PER_FUNC_AS_FLAGS["func_8003C628"] = "-G4"`, the same
    4 <= G < 8 window as func_80025BEC: the gp-relative neighbours here are
    one and four bytes wide. That row is live while the function is parked.
    What is left: retail reads `o[8]` BEFORE reading D_8009B408 in the first
    block and fills the second block's load-delay slot with the
    `sw D_8009B388`, where we finish one chain before starting the other.
    Measured and not it: all twelve orderings of the four statements in the
    first block (45 is the best, 62/66/67/70 the rest), splitting the read
    out of the read-or-write in one block and in both (62 and 66), the
    compound `|=` spelling, and `v` as s8, u8 and s32 with the cast moved to
    the test.

func_8007214C -- 59 differences at +2 length, and the diagnosis is one
    sentence: gcc hoists three loop invariants that retail keeps inside the
    loop. The structure is settled -- a linear search over 12-byte records
    for the first whose halfword at +0 matches, skipping two exclusion
    cases, then the index or zero stored to a handle slot.
    The three it hoists are the constant 8 (retail materialises it inline
    each time), `k < 5` (retail's `slti $v0,$s1,5` is in the loop body), and
    the D_800F5B98 base for the two final stores (retail shares only the
    `lui` and emits each `%lo` in its own arm, which is the split-pair form
    an unsized array gives). Retail also RELOADS the loop bound from its
    stack slot at both the continue and the exit, where we cache it in a
    register -- and it is an address-taken local, which by the rule in
    WORKFLOW should stay in memory.
    One real bug found on the way and worth repeating: D_801AB000 is
    declared `Rec12[]`, so `D_801AB000 + i * 12` advances **144** bytes, not
    12. The listing said so immediately -- `sll 4` where retail has `sll 2`.
    Use `(u8 *)&D_801AB000[i]`.
    Measured and not it: volatile on one and both out-parameters (62 and
    66), the byte-base and &-index spellings of the record address (both
    59), and the four flag rows -- nostrength 59, nosched 57, nosched2 69,
    O1 71.

func_80046DE8 -- 17 differences at +2 length, and both extra instructions
    are load-delay `nop`s we cannot fill. The structure is settled.
    Two readings mattered to get here and both are reusable:
      - there is NO base local. Writing `p = D_8009B45C;` once per group and
        indexing `p` keeps the pointer in a caller-saved register across the
        whole function and rotates every register in it: 83 differences. The
        global is re-read at every group in retail, because a store through
        the pointer may alias the pointer itself, so each source reference
        is its own `lw`. Written inline it is 18.
      - the loop's halfword store wants the scaled index in a local
        (`o = i * 2;`) so the add comes out base-first, one more instance of
        the func_8004DB14 rule.
    Also here: func_80049C40 takes no argument and this caller passes one,
    so its prototype is now guarded on FUNC_80049C40_VOID, defined only by
    the file that defines the function -- the same per-file guard shape as
    FUNC_80017F04_FULL. The callee was re-verified MATCH afterwards.
    What is left: retail reuses the base register the LOOP loaded for the
    two stores just after it, where we reload, and fills both delay slots.
    Measured and not it: naming that base inside the loop and using it after
    (18), the parenthesised base-plus-index spelling (18), and keeping the
    per-group base local (83).

func_800478EC MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_800478EC_WAS -- 16 differences, 95/95, and the real edits are about eight
    register renames in one block plus the loop counter's increment landing
    one slot early. Everything structural is settled and eight readings got
    it there from 80:
      - `u8 v` and `u8 w` for the two byte reads, which is what gives the
        `andi 0xFF` before the test AND the *unsigned* `sltu` comparison; as
        s32 locals the compare is `slt` and the mask is gone.
      - `if (v >= w) { = 0; } else { = w - v; }`, not the other way round:
        retail branches out on `v < w`.
      - the func_80047864 arm is the THEN and the clearing block is the
        else; there is no `continue`, the arm just ends and falls into the
        timer block below.
      - no base local for D_8009B45C, same as func_80046DE8: every group
        re-reads the global.
      - `(D_8009B45C + i)[K]` for the byte accesses and a named `o = i * 2`
        for the halfword, so all the adds come out base-first.
      - `PER_FUNC_AS_FLAGS["func_800478EC"] = "-G0"` with the scalar
        declaration: no %gp_rel in the function, so recipe branch 1, and the
        reference is one register.
    What is left: retail sets up the call's `$a1 = mask` before reading the
    +0x434 byte and does `acc |= mask` after it; we do the two the other way
    round. Measured and not it: `acc |= mask` in all three positions in that
    block and a named local for the call's argument.

func_80053248 -- 45 differences at 100/100, and the open half is register
    allocation around a parameter-homing shape I do not have a clean
    explanation for. Six parameters; retail homes the first four into the
    incoming argument area and re-reads them, which costs about seventeen
    instructions that no plain-parameter spelling produces (85 built against
    100). Declaring `a` and `b` `volatile` reproduces the homing and the
    reloads and is what the candidate carries -- but that is a KNOB, not a
    claim about the original: nothing here takes an address. Measured
    alternatives: `a` alone (96 built), `b` alone (90), all six (115),
    a+b+c+d (106), and -O1 and -O0 on the plain signature (91 and 166).
    Two things that are settled and not knobs:
      - the base local is assigned INSIDE each of the five blocks. Held
        outside, gcc hoists it and the whole function shifts; written with
        no local at all, cc1psx folds each store's constant offset into the
        `%lo` (`%lo(D_800F2C40+3576)`) where retail keeps it as the store's
        displacement. That is the func_800300C8 rule, and this is its
        clearest instance: five blocks, five materialisations.
      - the tail is `D_8009AF94 = 0xF;` after the if/else, which I missed on
        the first read because it is past the last label.
    Field map, so nobody re-derives it: +0xDF8 <- b (halfword, also mirrored
    to D_8009B488[a]), +0xDFA <- c, +0xDFC <- d, +0xDFE <- (e != 0) with a
    mirror to D_8009B48E[a], +0xDFF <- (f != 0) mirrored to D_8009B490[a],
    each guarded by its own `>= 0` test, and the c..f group only when
    a < 2; otherwise D_8009AF88 = D_80091008 + D_800F5678[0] * 178.

func_8005BE3C -- 75 differences at -6 length; the decode is settled and the
    open half is how many times D_8009B498 is reloaded.
    Retail loads that pointer global THREE times -- once for the x group,
    once for the y group and the func_8008FD38 base, once for the
    func_8008FDB4 base -- and we load it once and keep it. Neither writing
    the global inline at every reference nor reassigning a base local once
    per group produces the reloads: gcc CSEs all of them, so it is treating
    the `sh` through the derived pointer as not aliasing the pointer itself.
    Declaring the global `volatile` DOES produce them (95 built against 93,
    74 differences) but that is a knob with no justification -- nothing here
    needs it and no other file uses the symbol -- so it is NOT installed and
    the candidate carries the plain declaration. Whoever picks this up
    should look for the real reason gcc reloads before reaching for it.
    Settled, from the listing:
      - `func_8008FD38(D_8009B498 + D_8009B066 * 0xE000 + 0x1B000,
         D_8009B060)`; the scaling is `sll 3, subu` = 7k then `sll 13`, and
        the second argument is the `lbu` twenty instructions earlier, which
        is the early-evaluated-argument shape.
      - `func_8008FDB4(D_8009B498 + D_8009B067 * 0x2D00 + 0x37000,
         *(s16 *)(...0x4242C) * *(s16 *)(...0x4242E) / 2)`; scaling is
        `3m * 16 - 3m` = 45m then `sll 8`.
      - the tail returns func_8005BFC8's own value, not a literal: the edge
        that skips func_8005C5D4 carries $v0 through untouched, so there is
        no second materialisation of 1 there.
      - `x` is `*(s16 *)buf` and `y` is `*(u16 *)(buf + 2)` -- lh against
        lhu, deliberate.
      - one call to func_8007FEC8; the doubled `addiu $a0,$sp,0x10` is
        per-path argument materialisation, not two calls.
    Also open, and probably one instruction of the six: retail's first store
    pair goes through TWO registers holding the same base+0x40000
    (`addu $a3,$a2,$zero`), the second pair through one. A second name does
    not survive coalescing.

func_80071FC8 -- 54 differences at equal length; gcc hoists two more loop
    invariants than retail and the discriminator is upstream of the loop pass.
    Sibling of func_8007214C (same D_800F5B98 handles, same 12-byte
    D_801AB000 records, same func_800707C4 range call, same 1/3/6/8 mask
    set), and the decode is settled and confirmed against the listing:
    want/k/out are three func_8007058C results, two of them indexed through
    D_800F5B98; the body is the k-in-{1,3,6,8} 0x4000 test, then
    `*(s16 *)r != 0` splitting the 0x1000 test (want==2) from its else
    (want==1), with the zero side breaking on want==0; the tail is
    `if (i > hi) D_800F5B98[out] = 0; else D_800F5B98[out] = i;` written in
    that order, which is what shares the store-0 block with the entry guard.
    All the polarity and store-order work is done. What is left:
      - retail RELOADS `hi` from its stack slot three times (the entry guard,
        the loop's bottom test, the post-loop test); gcc commons ours into
        one preheader load plus `addu $a2,$v1,$zero`, which takes the
        register retail spends on the constant 8 and rotates every name in
        the loop.
      - retail materialises the constant 2 at its use, in a branch delay
        slot; gcc hoists it into a register. Independent of the above: with
        the reload forced, the 2 is still hoisted.
    `volatile s32 hi` DOES force the reloads (97/97, 24 differences, of which
    most are label renumbering) and is NOT installed -- and the reason is
    measured, not stylistic: applied to the sibling, whose target has the
    identical three-reload signature, volatile makes it WORSE (59 -> 66). A
    knob that fixes one of two functions with the same signature is not the
    mechanism.
    Measured and dead: `s32 rng[2]` with `&rng[0], &rng[1]` (64, unchanged --
    with no store and no call in the loop every MEM is invariant whatever
    the spelling), declaration order swapped (64), reading hi through a
    pointer local (+4 length), `lo` as the counter (64). Loop form DID move
    it: the source-written guard plus `do { } while (i <= hi)` is 97/97
    against the `for`'s +1, so the `for`'s manufactured guard was costing an
    instruction -- that is installed. Flag sweep best is 47 at
    `-O2 -G0 -fno-schedule-insns`, which is a different cascade and not the
    hoist.
    Same loop, same flags, different movable set: the discriminator is how
    the loop RTL was built, not what is in it.


func_8004C8C8 -- 5 differences, and only ONE of them is real: where the
    cursor's initialising copy sits. Everything else in the function is
    byte-identical, including the inner retry loop around func_8004C420 /
    func_8004BB34 and the shared decrement store that two predecessors reach
    with the `addiu -1` in their own delay slots.
    Retail emits `addu $s0,$s1,$zero` (p = b) AFTER the loop's entry-guard
    branch, in the preheader; we emit it before the guard's own load. Six
    placements measured and all identical: `for (i = 0, p = b; ...)`,
    `for (p = b, i = 0; ...)`, `p = b;` as a statement above the `for`,
    `i = 0;` above with `p = b` as the sole init, `p = D_8009B458 + 0x518`
    as the init, and an explicit `if (count != 0)` around the whole loop --
    that last one is 88, exactly the double-branch WORKFLOW warns about.
    Two levers did land and are installed: `-fno-strength-reduce`, because
    gcc otherwise builds a second cursor biased at +0x1C (five uses of that
    field) and the two givs cost a spill and a wider frame; and distinct
    names for the three values through the 0x1C field -- `t` for the first
    read, `r` for func_8004BB34's return, `d` for the decrement -- where one
    shared name cost a register copy on the return and rotated the whole
    chain to $v1. That pair took it from 101 to 18, and the increment order
    in the `for`'s third clause (`p += 0x2C, i++`, not the reverse) took 18
    to 5 by putting the counter's `addiu` in the bound load's delay slot.
    Needs PER_FUNC_AS_FLAGS -G0: gp == 0 here, so recipe branch 1 applies
    and the scalar D_8009B458 plus a -G0 assembler gives the %hi/%lo form.
    Handed to the permuter.


func_80016E70 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80016E70_WAS -- 8 differences, one scheduling slot and the register that
    follows from it. The whole function is otherwise byte-identical: two
    func_80016DDC calls on D_800E9FF0 and D_800E9FF0+0x20, the packet at
    0x1F800320 built in retail's own store order (0x10, 0x00, 0x0C, 0x0E,
    0x08, 0x14 -- ascending order is 13, so that odd sequence IS the source
    order and gcc did not reorder them), and four func_80016D2C calls whose
    x/y are written into the packet just before each one.
    What is left: retail issues `lbu D_8009B1D5` immediately after the first
    store, reusing the register the 0xF10100 constant just freed, so that
    constant lives in $v1 and ours lives in $v0. Naming the read moves it
    three instructions closer but no further, and its position in the source
    does not matter at all -- seven placements measured (before the base
    local, before the pointer load, before and after each of the first three
    stores) and every one emits the load in the same slot. Also measured and
    dead: declaration order, a named local for the 0xF10100, passing the
    packet local instead of the literal at the four calls (identical -- gcc
    rematerialises the constant either way), the symbol written inline for
    D_800E9FF0, and `do { } while (0)` around the store run (13) or around
    the store-plus-read pair (13).
    Handed to the permuter.

func_8005B0B4 -- 19 differences at equal length, and the first 29
    instructions are byte-identical. RETRACTION: an earlier version of this
    entry said the residue started at retail's `andi $s0,$s1,255` and that
    nothing produced it. That was measured on the wrong candidate. `s32 k =
    mode & 7;` with `(u8)k` written at both comparison sites gives the
    `andi` and the `sltiu` after it exactly; it was declaring `k` itself
    `u8` that turned the mask into a copy, because then gcc knows three bits
    fit in a byte and the truncation is free. The candidate now carries the
    cast spelling. Read a park entry's claim against the file it points at.
    Decode, all confirmed: seven parameters (the last three on the stack at
    0x58/0x5C/0x60), `k = mode & 7` and `m = mode & 8`, func_8005A98C fills
    an 8-byte packet at sp+0x18, `(k + 3) % 6` through the 0x2AAAAAAB
    reciprocal when m is set, the 0x1E field either zeroed or scaled by arg5
    / 4096, func_8005ABA0 into a 4-byte buffer at sp+0x20, an optional
    `lvl - c` inversion of the three bytes, a zero-to-one fixup on each, and
    three `lb`/`sb` copies into the out pointer with `return out;`.
    What is left is one allocation tie and its cascade. The modulo's cursor
    goes to $s0 in ours -- reusing the register the masked selector just
    freed -- and to $a0 in retail. That leaves $v0 for retail's first
    zero-fixup where ours takes $a0, so all three fixup values are still
    live at the tail and gcc reuses them instead of emitting retail's three
    `lb` reloads. One register, and the rest follows.
    Measured and dead: three distinct fixup names beat two (two is +1); the
    tail needs the lvalue cast `*(s8 *)(rgb + i)` for retail's `lb` against
    the `u8` array the fixups want as `lbu`, and naming those three reads is
    -3 because gcc folds them into the fixup values; `h += 3; h %= 6;` gives
    retail's in-place `addiu`/`subu` destinations and is installed;
    declaration order of the cursor changes nothing.

func_80039794 -- 43 differences, six of them real, and the decode plus three
    addressing decisions are done. Structure: four 0x64-byte records from
    D_800EB0F8 walked by two cursors (the record base for the three calls,
    base+0x30 for the flag word at +4 and the pointer at +0), an inner retry
    loop around func_800393B0 bounded by D_8009B0C1, and a tail that maps
    D_8009B35A through D_801D9000 into func_8003B50C's argument.
    Installed and load-bearing:
      - `-fno-strength-reduce`. Without it gcc rebases the +0x30 cursor at
        +0x34 (the flag field is read five times, the pointer twice) and
        every displacement in the loop is wrong. Sixth user.
      - D_8009B0C1 declared `u8 [8]` behind a per-file _IS_AGGREGATE guard,
        assembled at `-G4`. Recipe branch 3: it needs the non-small form
        while D_8009B35A (2 bytes) and D_8009B356 (1) stay gp-relative, and
        it is *narrower* than the widest of those, so no real threshold
        exists. With the plain unsized array gcc hoists its `%hi` out of the
        inner loop as an invariant, which retail re-materialises.
      - a base local for D_801D9000 assigned before the loop. Written
        against the symbol, `&D_801D9000[v] + 2` folds the +2 into the `%lo`
        and the hoisted invariant becomes base+2; against a register base
        the +2 stays a displacement, which is what retail has.
      - `k = -1;` unconditionally with `if (v >= 0)` overwriting it, the
        delay-slot form; `goto` rather than `else` for the 0x1C00 arm; the
        `for` third clause in the order `q += 0x64, j--, p += 0x64`.
    What is left, all in the preheader and the block that follows it:
      - gcc hoists the constant -1 (both `D_8009B35A = -1` stores) and puts
        it LAST among the preheader instructions; retail has it second, right
        after the loop counter. Every ordering of the three named setup
        assignments leaves it last, so the position is the loop pass's, not
        the source's. Naming it (`m1 = -1;`) does put it second -- and then
        gcc hoists the *store* out of the inner loop as an invariant and the
        whole `reset:` block disappears (-3). Naming it for one of the two
        sites only is +3. That coupling is the thing to break.
      - retail hoists the second `lhu 4($s1)` above the `sh $zero` to
        D_8009B35A and we do not, so we spend a `nop`. Writing the store
        after the test, or naming the read, are -4 and -1.
      - the first `D_8009B35A = -1` uses the loop counter's own -1 register
        in ours ($s0) and the hoisted one in retail ($s4); swapping the two
        statements changes nothing.
    Flag sweep on top of the installed pair: nothing better.
    Handed to the permuter.

func_800243F4 -- 10 differences, all of them register names inside the
    shuffle loop's four-instruction swap, and the other 93 instructions are
    byte-identical. Decode: a 40-entry deck builder. With a null source it
    zeroes a 0x2D2-byte stack histogram and then draws 40 distinct cards by
    walking D_801781D8's 0x2D0 halfword weights against a running total
    until it passes `(rand() & 0x7FF) + 1` -- the same pick func_80021810
    makes one 1460-byte tier further into the table, see that entry --
    rejecting any index already used three times; with a non-null source it copies 40 halfwords straight
    through. Either way it then does 160 random pair swaps of both the
    halfword and the byte arrays.
    Four levers landed and are worth reading:
      - **one counter for all three loops.** Retail's $s1 is the histogram
        clear index, the weight-walk index and the shuffle counter; two
        names for those put the walk index in a caller-saved register and
        cost a whole callee-saved register (28 differences to 10).
      - **the parameters walk and two locals hold the bases**, not the other
        way round: `b16 = out16; b8 = out8;` at the top with `out16`/`out8`
        themselves advancing gives retail's `addu $s0,$a1,$zero` /
        `addu $s6,$s0,$zero` pair; cursors derived from saved copies gives
        the same instructions with every register exchanged.
      - the clear loop is written **downward** (`for (i = 0x2D1; i >= 0;
        i--)`); the forward form gets a descending pointer walk instead of
        retail's base-plus-index.
      - the weight table's base is a local assigned before the outer loop,
        or gcc re-materialises `%hi` inside it.
    What is left: retail loads the two halfwords in the order [x] then [y],
    and every spelling we can find loads [y] first. Six measured -- both
    source orders of the two reads, both orders of the two stores, and
    address locals in the `base + i * 2` and `i * 2 + base` forms (the first
    also flips the `addu` to base-first, which is a second difference). The
    byte half of the same swap comes out in retail's order with no help at
    all, so it is specifically the halfword pair.
    A permuter run produced a 6 and it is REJECTED: pycparser's round trip
    had rewritten `s32 func_800358FC(s32)` to `long long func_800358FC(s32)`
    and the function body was character-for-character the base, so the whole
    gain is the wider return type shuffling register pressure. The target
    reads no $v1 after either call. Third instance of this class -- when a
    permuter output's body matches the base, diff the *declarations*.


func_8002A4A8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8002A4A8_WAS -- 14 differences, two register rotations, and the other 96
    instructions are byte-identical including both `break 7` / `break 6`
    wrappers. Decode: caches a (row, column) pair in D_8009B258/9 and returns
    early if unchanged, then writes a cursor record at D_800EA1E8 --
    `u = (a % 10) * 14 + (a >= 10 ? 0xAE : 0xE)` with the whole modulo
    expression written out in BOTH arms (retail duplicates it, so do not
    factor it), `v = (b / 10) * 178 + (b % 10) * 16 + 0xE`, and two
    velocities `((s16)x - field) << 16` divided by the third parameter.
    Those two divisions are a real `div`, which is the tell that the divisor
    is a variable -- here it is simply the parameter, no local needed.
    UPDATED: 14 down to 3 on two applications of one lever -- borrowing a
    later-live local as a temp. `y = a;` in the `a < 10` arm with the modulo
    taken of `y` is 7 (a fresh local in the same place is 14), and
    `y = (…) << 16; x = y / n;` for the first velocity is 3. `y` is the
    second velocity's own variable, assigned for real much further down.
    What is left is one register: retail's `sll ... 16` writes $v0 in place
    over the subtract that feeds it, and ours writes $a0. One name for the
    whole chain puts it in place but in $a0 (4); borrowing `v` instead of `y`
    is 30; a fresh name declared first, last or in the middle is 7.


func_8001B938 -- 3 differences, one register. 107 of 110 instructions are
    byte-identical; retail holds the unpacked 5-bit field in $a0 (reusing the
    register the record index just freed) and we hold it in $v1 (reusing the
    one the D_801D4244 base just freed). Both are free at that point and
    nothing else in the function differs. Seven shapes measured -- naming the
    record index and reusing the field's own name for it, splitting the
    `>> 26` from the `& 0x1F` against one name (4), naming the halfword read
    between them, and the declaration order of the four locals -- and every
    one of them is 3.
    Also measured after the fact: the borrowed-later-live-local hint that
    took func_8002A4A8 from 14 to 3 does not apply here -- borrowing the
    loop counter for the record index is 6, borrowing the byte-table index
    is 18, and using one name for both the index and the field is 10.
    Two readings got it here and both are now WORKFLOW lines: a base local
    for D_8015C424 (against the symbol the 0x4B6B8 folds into `%hi`), and
    then a SECOND name for the intermediate pointer, because retail splits
    that offset as 0x48000 in a register plus 0x36B8 as the load's
    displacement and one local only gets you the whole constant in the
    register.



func_80025D30 -- 38 differences at -1 instruction; the addressing is solved
    and what is left is one constant's materialisation. Decode: a five-step
    timer that decrements D_8009B20C[1], reloads it to 0x10, picks a record
    out of D_800907D8 by `D_8009B1D0 + (D_8009B1D5 * 20 + 5)`, and if the
    record's 0x8000 flag is set spawns a func_8002C604(0xD) packet carrying
    the record's 0x30/0x32/0x34 halfwords, `D_8009B1D0 << 14` added into the
    packet's +0x14 word, and a 2/-0x1F4 or 1/-0x3E8 pair chosen on
    `D_8009B1D2 == 0x15D`; then bumps D_8009B1D0 and sets 0x40 in D_8009B220
    at five.
    Two things landed and both are now WORKFLOW lines or close to it:
      - the `-G4` window. D_8009B260 needs the bare form while D_8009B20C
        stays gp-relative, and D_8009B20C is eight bytes, so no threshold
        exists as declared. The unit touches only element [1], so a per-file
        `D_8009B20C_HALF` guard declares it `s16 [2]` and the window opens.
        Without this the `%hi` is hoisted into a branch delay slot retail
        leaves as a `nop`.
      - naming the 0x34 read and storing it after the +0x14 update. That is
        what puts the packet pointer in retail's $a1: the named halfword
        occupies $a0 across the branch, and every one of the ten sites that
        touch the packet was wrong before it.
    What is left: retail materialises the -0x1F4 / -0x3E8 that goes into the
    packet's +0x12 at the END of each arm, reusing the register the record
    update just freed, and we hoist it to the top of the arm into a register
    of its own. `s16 k` is the best of the four types (an `s32` k also makes
    the record's own subtraction an `addu` through that register instead of
    retail's immediate, which is +1); `u16` is the same as `s16`, and
    `0 - 0x1F4` changes nothing. Naming the shifted counter, in either
    position, is -2.


func_8004A940 -- 77 differences at -3, after m2c corrected the decode in
    three places the hand-read had wrong. THE SIGNATURE WAS WRONG: it takes
    FOUR parameters, not two, it RETURNS a value, and func_8004A8E4 takes
    four arguments. m2c saw it because $a2/$a3 are read before being written
    on the path where the first loop does not run, and because the `addu
    $v0,$a0,$zero` this entry used to call "a dead copy of the loop counter
    into a register nothing reads" is the return value being set to the loop
    index. Two of the three things this entry listed as open were that bug. Five loops, all over the same 0x28-byte
    records starting at D_8009B458 and bounded by `*(s16 *)(base + 0x510)`:
      1. bail out entirely if any record's 0x183 byte is 0x63;
      2. take the first record whose 0x18D byte is zero;
      3. take the first record matching (arg0 & 0xFF) at 0x183 AND
         (arg1 & 0xFF) at 0x185 -- this one reloads BOTH the base and the
         bound every iteration, where 1, 2 and 4 hoist them;
      4. loop 2 again;
      5. otherwise pick the record with the smallest unsigned halfword at
         0x19E, and give up if that is still 0xFFFF (`0xFFFE < best`
         unsigned).
    The tail calls func_8004A8E4(index, byte) where the byte is
    `arg0 & 0xFF` -- and on loop 3's exit gcc does not recompute it, because
    $a1 already holds the 0x183 byte the branch just proved equal to it.
    That is the shape to write: the loop-3 arm passes the loaded byte, the
    other arms pass the masked parameter, and the `jal` cross-jumps.
    Solved: the bound has to be written INLINE as
    `*(s16 *)(D_8009B458 + 0x510)` in loops 1, 2 and 4, not named. Retail
    tests the raw load in the guard and the loop compares against a hoisted
    copy of it, which is exactly what an invariant load in the condition
    gives; a named `n` gets one register for both and is -3 instructions.
    Also corrected: loop 1 compares the record byte against the VARIABLE
    holding 0x63 (retail keeps it in $a3 and does `beq $v0,$a3`), not against
    the literal; the literal costs a spare copy. 92 -> 80 on that alone, and
    80 -> 77 with the first loop's bound written inline.
    Still open, and it is the known reload class: retail re-reads the
    D_8009B458 pointer global at the bottom of loop 3 every iteration, and
    gcc hoists ours into a base local however the source spells it -- the
    same thing func_80071FC8's `hi` does, unsolved there too.


func_8003C328 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8003C328_WAS -- 21 differences at equal length, and the layout is solved. A three-way
    dispatcher on the second parameter: mode 1 sets the +0x1C word to 0x800,
    clears two bits of D_8009B0F4 and points +8/+0xC at D_8009B118; mode 0
    builds a 0x18000 window, clears a different bit pair, sets one, writes
    +0x46 = 2 and points +8/+0xC at D_8009B118 and D_8009B118 + 0x800; mode 2
    calls func_80081DE8 and points both at D_801AF000. Modes 1 and 2 share a
    `+0x46 = 1` tail, mode 0 does not.
    The lever was already in WORKFLOW and was re-derived the hard way here:
    **only a `goto` moves an arm out of line.** Written as nested ifs with the
    blocks inline -- either polarity, gcc canonicalises both to the same
    thing -- it is 83 differences at -6, and the six are precisely the jumps
    a fall-through layout can never emit: the dispatcher's two
    `j epilogue`/`nop` pairs, mode 0's terminal jump, and mode 1's jump to
    the shared tail. Rewritten as a dispatcher of `goto m0/m1/m2` with the
    blocks laid out in retail's own address order and an explicit
    `goto tail;` at the end of m1, it is 60 at -2.
    Signature corrected on the way: it takes `(u8 *, s32)`, not `(void)`.
    functions.h had it PROVISIONAL as void; it is a callback passed by
    pointer to func_80014E1C from func_8003C498, which still MATCHes after
    the change.
    Addressing: `as -G4` with D_8009B0F4 declared `[2]`, which is eight bytes
    and so not small at that threshold -- that is what gives the `lui $at`
    stores and destination-register loads retail shows. gp == 0 here so the
    threshold is free.
    RETRACTION on the addressing: this entry used to say the two symbols
    wanted different `-G` thresholds and no single one worked. Wrong -- the
    threshold is `-G0`, not `-G4`. gp == 0 here, so recipe branch 1 makes any
    threshold free, and at -G0 D_8009B118 is non-small too, which is what all
    three of its references need. That alone took the length error from -2 to
    exactly 0. The -G4/-G0 comparison had been read across different length
    errors, which try_func's own header warns against.
    Two source levers then took 60 to 28, and the first is now a WORKFLOW
    entry: naming an unrelated read in the arm (`t = D_8009B0F4[0];`, placed
    AFTER the arm's first store) is what lets the delay-slot filler take the
    mask constant's `lui`, 60 -> 57 -> 49; and moving the D_8009B118 load
    ABOVE the `+0x6` store is 49 -> 28, because a store through the record
    pointer may alias the global and gcc will not move the load across it in
    either direction -- the emitted order is the source order, pinned.
    The same load-above-the-store lever then applied a second time, in mode
    2: naming the D_8009B118 read and putting it above the `+0x0` store is
    28 -> 21, and it is what puts the `+0x6` store in the `jal`'s delay slot.
    Two positions matter and both were measured -- above the `+0x4` store
    instead is 26 -- and the name must be the SAME `v` mode 0 uses; giving
    mode 2 its own name is 23, which is the one-name/two-names rule pointing
    the other way for once.
    What is left: the `sh $zero,0x30` / `sw $a1,0x1C` pair in mode 0 sits
    three instructions early, and mode 2's 0xFFDCFFFF mask is completed late
    with its `+0x1C` store hoisted above the D_8009B0F4 read. Measured and
    dead on the store pair: `do { } while (0)` around it (32), swapping the
    two (21, no change), moving it after the second read-modify-write (-4).
    Measured and dead on mode 2: naming its D_8009B0F4 read the way mode 0's
    worked, at either position (28 and 31) -- gcc folds the reload because it
    knows the value it just stored.


func_8004BCE8 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8004BCE8_WAS -- 82 differences at +1, and the count is badly inflated by the
    shift; the real residue is four instructions. Decode, settled: it resets
    a block at D_8009B458 (+0x801 byte, +0x7F0/+0x7F4 words, +0x518 = 8),
    takes func_8004BCA8's result into +0x7FC and func_8004BC2C's >> 8 into
    +0x808, computes `60000000 / rate * 100 / 115`, clamps it to 0xFF, then
    halves or quarters it by mode -- 0x1E gives >> 2, 0x18 and 0x3C give
    >> 1, anything else no shift -- masks to a byte into +0x16 and +0x14,
    calls func_8004BAE4, and finally writes +0x7FC into +0x804 (twice: once
    under an `>= 0x60` test with a zero in the else arm, then again
    unconditionally, which is what the listing shows).
    The divisor 115 was recovered by solving gcc's unsigned reciprocal
    (magic 0x1CF06ADB with the add-correction and a final `>> 6`) rather
    than guessed; the first divide is a real `divu` with `break 7`, so its
    divisor is the variable, per the WORKFLOW rule.
    The layout lever from func_8003C328 applies again and got it from -3 to
    +1: the four shift arms are out of line, reached by `goto`, with the
    no-shift arm carrying its own `& 0xFF` and jumping past the shared one.
    Retail has TWO `andi $v0,$a1,0xFF`, not one -- count them before
    factoring the mask out.
    What is left, all four of them: we emit a THIRD `andi`, because gcc fills
    the `m != 0x3C` branch's delay slot by copying the shared mask, where
    retail fills it with `addu $a0,$s0,$zero` -- the func_8004BAE4 argument,
    which retail sets up in three delay slots and we set up in two. Naming
    the argument at the join to try to produce it is +4 and much worse. Plus
    the first D_8009B458 load takes $v0 in ours and $v1 in retail, and the
    +0x808 store lands after the `divu` in ours and before it in retail --
    three source orderings of the store and the divide are identical, so
    that is not the axis.


func_8003C120 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8003C120_WAS -- 101/101, 75 differences (was 102/101, +1 and 80)
    residue is where two mask constants are materialised. Direct sibling of
    func_8003C328: the same three-way-plus-one dispatcher on the second
    parameter, and the same `goto` layout applies with the arms in retail's
    address order (m0 < m1 < m2 < m3 < join < tail). Mode 3 is the extra one:
    it points +8/+0xC at the literal 0x80140000 and shares the join block
    with mode 2, which is where the +0x1C word (0x800 or 0x8000) and the
    second D_8009B0F4 mask are written once for both.
    Signature was PROVISIONAL `(void)` and is `(u8 *, s32)`; it is a callback
    passed by pointer from func_8003C2B4, which still MATCHes.
    THE ADDRESSING FINDING, and it is the useful half: this function needs
    `as -G4` for D_8009B0F4's `lui $at` stores AND D_8009B118 declared `[2]`
    behind its own guard so it is non-small at that same threshold -- every
    one of its references in retail is a %hi/%lo pair, and left small it is
    one instruction short at each, which was -7. Its sibling func_8003C328 is
    the exact counterexample: there only SOME of the D_8009B118 references
    expand, so the same guard costs +3. Two functions, one symbol, opposite
    answers -- the threshold has to be decided per unit by counting the pairs
    in the listing, not by precedent.
    What is left: retail materialises the `lui` halves of both masks
    (0xFFDDFFFF and 0xFFDCFFFF) in the dispatcher's own branch delay slots
    and completes them with `ori` inside the arms; we build them whole inside
    the arms and spend the slots on other constants. Flag sweep finds nothing
    better.
    MEASURED AND FALSE, so it is written down here rather than assumed again:
    the two levers that took func_8003C328 from 60 to 28 do NOT transfer,
    even though the two are siblings with the same dispatcher. Naming the
    first D_8009B0F4 read is worth exactly nothing (80 either way) and
    hoisting the D_8009B118 load above the `+0x6` store is -3. The arms store
    in a different order -- func_8003C328 writes 0x32, 0x4, 0x30 and this one
    writes 0x32, 0x30, 0x4 -- and that is enough to change which reload gcc
    folds. The `-G` differs too: this one wants -G4 where its sibling wants
    -G0, and at -G0 here a whole D_8009B0F4 read-modify-write folds away
    (-5). Same shape is not the same residue.
    ALSO CORRECTED: the sentence above about D_8009B118 said func_8003C328
    wants only SOME of its references expanded. That was measured at -G4,
    where the question is confounded; at -G0 all three of that function's
    references are pairs and it needs them all. The per-unit rule stands, the
    reason given for it did not.

  **2026-08-28: the +1 was a `nop` in a load-delay slot, and the fix is a new
  declaration arm.**  Retail hoists the D_8009B0F4 reload ABOVE the
  `p + 0x1C` store and fills the reload's own delay slot with the `lui` for
  0x10000; gcc will not hoist a load of a non-volatile global past a store
  through `p`, so it emits the store first and the slot gets a `nop`.  A
  named read folds under the `_SIZED` (plain array) arm.  `D_8009B0F4_SIZED_
  VOLATILE` -- the sized arm plus `volatile`, new in variables.h -- keeps the
  named read AND lets gcc move it, because gcc 2.8 moves a volatile load
  across a non-volatile store.  101/101.
  **And it does NOT break the bare form at -G4**, which the note above
  `D_8009B0F4` in variables.h predicted it would: the addressing comes out
  `lui %hi` / `lw %lo` / `lui $at` / `sw %lo`, the same as retail.  That note
  was narrowed once before (2026-08-20, for -G0) and this narrows it again --
  it is about the *aggregate* arm, not about volatile as such.
  The residue is 75, and the first difference is the dispatcher: retail puts
  m0's mask in `$a0` and the `lui` for m3's 0xFFDCFFFF in the `beq ... L6`
  delay slot, where we put `w = 0x8000` there.  Measured and worse: naming
  the shared 0xFFDCFFFF in m1 and the join (+1), naming it in m2 and m3 as
  well (+4), a `do { w = 0x8000; } while (0);` in m3 (+2), and a named local
  for m3's two 0x80140000 stores (75, no change).
func_8005C1F4 -- 70 differences at -2. Decode settled, and it was the lowest
    unclaimed function in the tree. It flushes a pending job if D_8009B060 and
    D_800F5D44 are both set, copies an EIGHT-BYTE UNALIGNED record from
    D_8009B498+0x42428 to +0x42400 indexed by D_8009B067 * 8 (that is the
    `lwl`/`lwr` + `swl`/`swr` quartet -- an all-u8 struct, alignment 1, per
    the struct-copy rule), calls func_8007F978 with that record and a
    0x2D00-strided buffer, advances `D_8009B067 = (D_8009B067 + 1) % 4`, adds
    a halfword delta into +0x42428 and either calls func_8008FDB4 with
    `field0x4242C * field0x4242E / 2` or sets D_8009B062.
    The `andi 0x1FC` is the modulo's mask narrowed to the range gcc can prove
    (the counter is a u8, so the value is at most 256) -- it is `% 4`, not
    some 9-bit field.
    Two addressing decisions, both recipe branch 3 and both installed:
      - D_800F5D44 needs the non-small form while the 4-byte D_8009B498 stays
        gp-relative, and `4 <= G < 4` is empty. Declared `[2]` behind a guard
        and assembled at -G4, which is the same trick func_80025D30 needed
        from the other side (there the gp symbol was shrunk instead).
      - the 0x40000 must stay a CONSTANT in a register, added at each use.
        A `g = base + 0x40000` local computes the sum once and is -1 and
        wrong; writing the full `base + 0x42428` offsets inline lets gcc hoist
        just the constant, which is what retail holds in $s0.
    Also measured: the base pointer must NOT be a local that spans the
    func_8007F978 call. Retail reads D_8009B498 into a caller-saved register
    before the call and re-reads it after; one local across the call takes a
    callee-saved register, widens the frame and costs 18 differences (88 vs
    70).
    What is left: retail copies BOTH the index and the base into fresh
    registers before computing the two record addresses (`addu $v0,$v1,$zero`
    / `addu $v1,$a2,$zero`), which is two more names than the source has, and
    the constants land in exchanged registers.


func_8003BD14 -- 42 differences at equal length, and every one is the same
    register rotation the rest of the family has. Fifth dispatcher
    (func_8003C328, func_8003C120, func_8004BCE8, func_80032184), and the
    first one where the whole recipe went in at once: `goto` arms in retail's
    address order, the volatile scalar arm of D_8009B0F4 for mode 0's
    back-to-back read-modify-writes, `as -G0` because gp == 0, and the
    D_8009B118 load written ABOVE the `+0x1C` store. First draft with all of
    that was 58 at +1; the load hoist alone took it to 42 at exact length.
    Signature was PROVISIONAL `(void)` and is `(u8 *, s32)`.
    Modes 1 and 3 share a real tail -- both set `+0x1C`, pick a pointer
    (D_8009B118 or D_800101D8, the latter a const pointer to 0x80168000 that
    lives in rodata) and mask D_8009B0F4, then the join stores all three and
    writes `+0x46 = 1`. Written with the two arms falling into one `join:`
    label, which is what retail has.
    THE FAMILY RESIDUE, now seen in three of the five and worth stating once:
    the mask constant whose `lui` half sits in the dispatcher branch's delay
    slot lands in **$a0** in retail and in $v0 or $v1 in ours, and the read
    that consumes it is exchanged to match. $a0 is the incoming parameter
    register in every case, free from the moment the parameter is copied to
    its callee-saved home at the top of the function. Nothing tried so far
    reaches it: naming the constant is propagated back to the use, and the
    delay-slot placement is already correct.


func_80048768 MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_80048768_WAS -- 100 differences at +3. Decode settled: a volume/pan update.
    If the selector has 0x8000 it forwards to func_80044E90 and returns;
    if the selector's top nibble is 0x4000 it indirects twice through
    D_8009B45C (a +0x44C table indexed by `(sel & 0x1F) * 2` plus 64 when
    bit 8 is set, then a +0x43C pointer table) and returns on either 0xFFFF;
    then a fixed four-iteration loop calls func_80077C50, matches the
    resolved id against each slot's +0x404 halfword, and when it matches and
    the out-parameter is non-zero scales two fields by `0x80 - amt` and
    `amt + 0x80` before calling func_80047864.
    The lever that mattered: `((sel & 0x100) != 0) * 64` written inline lets
    gcc fold the whole thing into TWO constant displacements and branch
    between them (+7). Naming the boolean first is +3 and 114 -> 100.
    Measured and dead: four one-expression spellings of the boolean (`0 <
    (u32)`, `!!`, `<< 6`, plain `!= 0`) are all identical to each other, which
    is the tell that the axis was the fold and not the spelling; and naming
    the whole offset instead of just the boolean is +5.
    What is left: retail computes the boolean with `andi` + `sltu $zero`
    where we still get `srl`. The documented two-statement form from
    func_80048920 (`k = sel & 0x100; k = k != 0;`) DOES produce the `sltu` --
    confirmed present -- but costs more elsewhere: +4 and 107 against +3 and
    100. So the instruction is reachable and the surrounding allocation is
    not, which is a different problem from the one func_80048920 had.



# --- guard sweep, 2026-08-20 -------------------------------------------------
tools_src/sweep_guards.py's first run, over every parked candidate at or below
50 differences. One match promoted (func_8003353C) and three parks improved:

  func_80013998   26 -> 20 at equal length, D_8009B118_SIZED
  func_8002C604   28 at -1 -> 24 at equal length, D_80010000_IS_AGGREGATE
  func_800496C4   38 at -6 -> 33 at -5, D_8009B458_IS_AGGREGATE

All three installed. A fourth hit is recorded and NOT installed:
func_8004A8E4 goes from 21 at -2 to 21 at -1 on D_8009B458_IS_VOLATILE. The
length error improves and the difference count does not, which is the weakest
shape a hit can have, and `volatile` on the game's main record array is a large
claim to make on it. Left for whoever next reads that function's residue.


func_8004D75C -- 40 differences at equal length (110/110), and every one of
    them is a register. The instruction sequence is identical: two loop nests
    over one 0xE20-byte record, the first walking a linked structure and
    accumulating byte weights, the second taking a running maximum per row.
    What paid, in order:
      - all three of the first nest's cursors are **givs**, not source
        variables: `t + i * 4` (retail's `addu $t2,$a3,$zero` + `lw 480($t2)`),
        `i * 2` and `i * 2 + 0x74`. Writing the first as an index and the other
        two as explicit scalars is what retail has -- an all-index version
        collapses the `+0x2C8` store into a t-based giv and loses 5
        instructions, an all-explicit one builds the wrong giv for the first.
      - the `v >= 0` arm is out of line, after the negative path's blocks, so
        it needs a `goto`, not an inline `if`.
      - `s = (u32)v >> 16; s = s & 0x7F;` as two statements against one name.
        Retail leaves the `bgez`'s delay slot empty because `s` is live on the
        taken path; one expression puts the shift in a dead temp and the
        filler hoists it. Now a WORKFLOW rule.
      - the second nest's index, cursor and counter need **their own names**.
        Reusing the first nest's is 63 differences and separate names are 40 --
        no instruction moves, the shared name just outlives its phase and
        outranks `t` in the allocator. Also now a WORKFLOW rule.
    What is left: `t` should be `$a3` and is `$t1`, `o` the reverse, and the
    hoisted 0xFFFF is materialised fifth in the preheader where retail has it
    second. Everything else follows from those.
    Measured and not installed, both with their exact shapes:
      - hoisting `b = r; u = n;` above the second nest's entry guard puts `t`
        in `$a3` and is 31 differences -- but at +1, because gcc then biases
        `r`'s giv by 0x7C4 and the two per-iteration copies retail has
        disappear. Ten shapes tried to keep the allocation and the structure
        together (index-based outer loop, explicit `b = t + n`, the zero store
        through four different lvalues, `for` versus `do`/`while`, and the
        dead-assignment hint, which gcc deletes because a plain copy
        coalesces); none does. `-fno-strength-reduce` is 85 -- it removes the
        givs the first nest needs.
      - the permuter reaches 21 differences by wrapping the second nest's
        store in `if (r || t) { store } else { store }`, identical arms that
        gcc cross-jumps back. That is semantically a no-op and correct, and it
        does put `t` in `$a3` -- but nobody writes it, and the
        `do { } while (0)` that stands in for this shape elsewhere does
        nothing here (40). Recorded rather than installed; a plausible source
        shape with the same effect is the open question.
      - declaration order does nothing: four orders, all 40.


func_8002FD10 -- 8 differences at equal length (112/112), and all eight are the
    *position* of one three-instruction group in the prologue. A screen
    set-up: it zeroes three globals, registers a callback with func_80014E1C,
    resets three 0x14-byte records, spins on func_80082324, then runs a 25-step
    loop that blits two sprites per step and walks a 5-wide grid.
    What paid:
      - the addressing. Two `$at` stores (D_8009B146, D_8009B148) beside
        gp-relative D_8009B2A0 (4 bytes) and D_8009B2A4 (2), so the window is
        `4 <= G < 8`: `D_8009B146_SIZED` declares both [4] and the unit
        assembles at `as -G4`. Same pair of knobs as func_8002E470.
      - `D_80010000` and `D_800EAE98` each want a size in the window
        `4 < N <= 8` -- at or below cc's own -G8 so cc1psx emits the *bare*
        symbol, above the assembler's -G4 so the assembler expands it. A bare
        symbol is one instruction to the scheduler, so the `lui`/`lo` pair
        stays in the destination register; the unsized array gives cc1psx's
        own splittable pair and it lands in a temp. `[0x40]` on D_800EAE98 --
        an honest size for a table that reaches +0x3C -- does NOT work,
        because it clears cc -G8 too. That is the sharp edge of this knob and
        it cost a round.
      - `lhu` not `lh` on D_800E9D70[5]: `*(u16 *)&D_800E9D70[5] + 1`, since
        the symbol is declared `s16 []`.
      - the three-record loop wants `p += 0x14;` before `i++`, not after.
    What is left: retail materialises `p = D_800EAE98` at prologue position 6,
    after the call's argument set-up and the `$s0` save; we put it at position
    1, before the `sh $a0` of the first global store. Nothing in the source
    moves it. Measured: four placements of the assignment (all 8), `p` written
    before the D_8009B2A4 store (8), `&D_800EAE98[0]` (8), naming the callback
    pointer (8), naming the two zero arguments (8) -- five shapes at the same
    number, which is the tell that this is not a source axis at all. Placing
    it *after* either call is catastrophic (+16). `do { } while (0)` around it
    moves it the wrong way (14 and 16). The full flag sweep says the current
    row is best; the permuter's only output is 22 and semantically broken.


func_80024E58 -- 2 differences at equal length (116/116), and both are the
    position of one `andi`. A duel-phase step machine: four mutually exclusive
    arms on D_8009B220's 0x40 and 0x20 bits, then a 30-record sweep.
    What paid:
      - the addressing. D_8009B364 is stored through `$at` and loaded twice
        through its own register, so it wants the bare form; the gp-relative
        symbols beside it are up to four bytes, so recipe branch 3 applies --
        `D_8009B364_SIZED8` (a new arm, `u8 [8]`) plus `as -G4`.
      - D_8009B0F4 and D_8009B134 are loaded back to back and retail keeps
        each `lui`/`lw` adjacent; unsized, cc1psx interleaves the two `lui`s.
        Their `_SIZED` arms ([2], eight bytes) make each reference one
        pseudo-instruction and indivisible. func_8002E470's lever, third use.
      - D_8009B1D2 is declared `s16` and read `lbu` here: `*(u8 *)&D_8009B1D2`.
      - three names that were being shared: the D_8009B1C8 record pointer with
        the loop cursor (it went callee-saved), and the 0x1A value with the
        loop counter.
      - hoisting func_80040410's two arguments into locals above the `-2`
        store, which is what sinks that store into the call's delay slot.
      - and the last one, worth remembering: `n = v & 0xFF;` before
        func_8002C604 and `n = n - 1;` AFTER it. Both before the call is 3,
        because gcc then fills the call's delay slot with the *mask* instead
        of the subtract; retail's `addiu $s0,$s0,-1` is in that slot, which
        only happens when the subtract is the last thing the filler sees.
    What is left: retail emits the `andi` after the `lui $at`/`sb` pair and we
    emit it before -- two independent instructions reading the same register,
    and gcc gives the one with the longer downstream chain priority. Measured
    and dead: `u8 v` so the mask comes from the type (4), an `(u8)` cast at
    the use (3), reading the value back out of D_8009B364 (4), the mask
    written above the store (3), `do { } while (0)` around the store or
    around both (+1 and much worse), a separate name for the masked value
    (4), `n -= 1` (3), and three placements of the decrement after the call
    (2, 7, 2). `volatile` on D_8009B364 is +2 and 68 -- it forces re-reads at
    the other two sites. The full flag sweep says the current row is best, and
    880 permuter iterations from a base score of 60 produced nothing.


func_8004D58C -- 13 differences at equal length (116/116). The sibling of
    func_8004D75C: same 0xE20 record, same 0x2C8/0x750/0x7C4/0xBEC fields,
    and it is the *reset* pass where func_8004D75C is the accumulate pass.
    What paid:
      - `*(u16 *)` on the 0xFFFF store. Through `s16` gcc folds the constant
        into a `-1` and emits `addiu $t6,$zero,-1`; retail has
        `ori $t7,$zero,65535`, which only the unsigned store produces.
      - naming that constant and assigning it among the prologue's zero
        initialisers. It is a hoisted loop invariant, and its position in the
        preheader follows the source line's position (func_8005611C's rule):
        56 differences to 46 on one line's placement.
      - the two `p += K; if (p != 0)` shapes are written with the pointer
        bumped IN PLACE inside the `if`, not into a second name -- retail puts
        the `addiu` in the branch's own delay slot, which a separate name
        cannot produce.
      - `g += 4;` before `i++`, not after: with `i++` last, gcc schedules the
        increment ahead of the loop condition's own load and leaves a `nop`.
        56 to 25.
      - naming the `1` that is shifted, and assigning it before the cursor
        copy that follows it in the preheader: 25 to 23.
      - and the prologue assignment ORDER, which is the whole remaining
        question. Five locals are initialised to zero and gcc materialises one
        zero and copies it four times. Writing the two loop cursors FIRST
        gives retail's exact register mapping -- p3=$t3, p2=$t6, i=$t0,
        n=$t5, m=$t4 -- and is 13; writing them in retail's *emission* order
        (p3, p2, i, ff, n, m) gives the right instruction order and rotates
        three registers, and is 23. The two cannot be had together by any
        ordering, because the pseudo assigned first becomes the master of the
        copy chain and is emitted first.
    Measured and dead: six assignment orders (13, 13, 13, 22, 24, 24);
    declaration order (no effect at all, three tries); writing the copies
    explicitly as `p2 = p3; n = (s32)p3;` so the chain is in the source (23,
    same as the plain order, three spellings); `d = d * 8` against one name
    for the modulo (16); `d = d << 3` (14); `i - d * 8` and `i - (d << 3)`
    (13, no change); inlining the division into the address (15). The
    permuter's best of eight outputs is 12 and carries a `new_var` artifact.

func_8005A6A8 -- 1 difference at equal length (116/116): retail negates in
    place, `subu $v1,$zero,$v1`, and we read the copy's source instead,
    `subu $v1,$zero,$v0`. Both are correct; it is which pseudo gcc's copy
    propagation picked. A geometry helper -- two square roots, a reciprocal
    /12, a real `div` by a variable, and a clamp.
    What paid:
      - naming the `+8` field read so the four table reads happen in retail's
        order ([8], [0xC], [0x14], [0]): 79 to 71.
      - and the one worth remembering: **the `- 0x2BC` on the first square
        root's result belongs AFTER the second call.** Written before it, gcc
        fuses the call-result move and the subtraction into one
        `addiu $s0,$v0,-700` and the function is an instruction short;
        written after, retail's `addu $s0,$v0,$zero` / `addiu $s0,$s0,-700`
        pair appears, because the raw result now has to survive a call in a
        callee-saved register before anything is done to it. Ten spellings of
        the subtraction measured at the earlier position -- two statements,
        `-=`, a fresh name, a fresh name plus a copy, and four placements
        among the neighbouring multiplies -- all 79 at -1. Moving it past the
        call is 9 at equal length.
      - three names for the clamp chain rather than two, and *which* name
        takes which role: `w = h / 16; v = w; ... w = v;` is 1, the same three
        statements with v and w exchanged is 9, and a genuinely third name is
        3.
    Measured and dead on the last difference: four spellings of the negation
    (test on the other name, `0 - v`, an explicit if/else, negating the other
    name) -- all 1, which is the tell that this is not a source axis.


func_8002E730 -- 2 differences at equal length (122/122), both of them one
    store two positions early. A screen-state toggle: it either recentres a
    rect or creates/destroys an overlay sprite.
    What paid:
      - the addressing, which is most of the function. Five symbols want the
        non-small form and the gp-relative ones top out at four bytes, so
        every bare-wanting symbol is sized into the window `4 < N <= 8`:
        D_8009B0F4 and D_8009B134 take their `_SIZED` arms ([2]), D_8009B146
        and D_8009B148 take theirs ([4]), and D_8009B145 and D_8009B0AC
        needed new `_SIZED8` arms written for them. `as -G4`.
      - the `D_8009B27C & 0x2000` arm written as an `else` rather than an
        early return, so its block lands after everything instead of inline;
        that alone is 32 differences to 9.
      - a base local for D_8009B0AC assigned at the *top of the function*,
        before the first call, where the symbol is only read inside the first
        branch. 9 to 3. A fresh name and a borrowed one score the same, so it
        is the hoist that matters, not the name. The permuter found it.
    What is left: retail puts `sw $s0, D_8009B280` between the `lui` of the
    0x2000000 constant and the `or` that consumes it; we emit it two
    instructions earlier. Measured and dead: naming the read-modify-write's
    load and placing it either side of the store (4 and 4), the store written
    after the RMW instead of before (6), the store and `t = D_800EAE98`
    swapped (3, no change), `do { } while (0)` around the pair (4), and a
    name for the 0x2000000 constant (6, gcc propagates it).

  **2026-08-28, 3 -> 2** on `do { D_8009B280 = e; } while (0);`.  Retail
  loads `e+4` and materialises the 0x2000000 BEFORE that store, and every
  reordering of the two source statements, a named read of `e+4`, a named
  constant, and a `do`/`while` round the read-modify-write instead are all 4
  or 6 -- only pinning the store itself moves it.
  The last 2 are retail putting the next call's argument copy
  (`addu $a0,$s1,$zero`) ahead of the D_8009B280 store where we put it just
  before the `jal`.  A second name for the pointer coalesces away at both
  placements tried, and 1079 permuter iterations from the 2 base found
  nothing better than 3.
func_8004DC38 -- 117 differences at +1, which reads far worse than it is: the
    instruction *sequence* is right almost everywhere (28 of 30 aligned blocks
    are the same length) and the residue is a whole-function register rotation
    plus two local shape questions. A leaf function, no calls, third member of
    the 0xE20-record family after func_8004D75C and func_8004D58C: it walks a
    run of 4-byte entries subtracting `entry[-2] * 16` from a modulo residue
    until it runs out.
    What is understood and written:
      - the two strides, `c * 0x74` and `c * 0x76`, and the `divu` -- an
        unsigned modulo by a *variable* (`w * 0x10`), which is why there is a
        `break 7` in the listing.
      - the first loop's two arms both re-read `cur[-2]` and re-shift it,
        because gcc 2.8 has no global CSE; written inline in each arm.
      - the `t[0xE16] == 0x3C` test hoisted into a local before the loop.
    What is NOT solved, and what was measured:
      - retail puts `e += 4` in the outer loop's branch delay slot and we emit
        it two instructions early. Three orderings of the three increments all
        score the same.
      - the second loop is a `while (1)` with a `bltz` break and a counted
        loop-back whose `b += 4` sits in the delay slot. Written as
        `do { ... } while (i < v)` it is +5, not +1 -- the `break` inside a
        do/while costs extra jumps here. The while(1) form with the test last
        is what is installed.
      - retail's first instruction is `addu $t3,$a0,$zero`, a copy of the
        parameter, because `$a0` is reused for `t + k`. An explicit
        `tt = arg0;` does NOT produce it (117 either way, the copy coalesces),
        and naming `b = t + k` separately is worse (119). That copy is the
        likely key to the rotation and no source spelling has produced it yet.


func_8005AE68 -- 22 differences at equal length (123/123), and ALL of them are
    now a three-way rotation of $s3/$s4/$s5. A colour transform: unpack a 15-bit
    BGR word, optionally rotate the hue by three sixths, scale the value, pack
    it back.
    What paid, and one of them is the whole reason it is close at all:
      - **`u8 m`**, where `m = (arg1 & 7) & 0xFF` is the switch-ish selector.
        As `s32` gcc folds the mask away entirely and the function comes out
        five instructions short; as `u8` the truncation survives, the `m < 7`
        test becomes `sltiu` in QImode, and the value needs a callee-saved
        register across the call -- which is the seventh saved register retail
        has and we were missing. -5 to 0 on that one word.
      - `u32` for the packed colour, so the two `>> 5` and `>> 10` are `srl`
        and not `sra`.
      - a name for the 0x1F used by the three `0x1F - x` subtractions; retail
        holds it in $s4 while the two call sites materialise their own.
      - the three subtractions written r, g, b -- 30 differences to 26. The
        source order and the emitted order are not the same here (gcc hoists
        the second load), so this is the permute-the-statements case again.
    What is left: retail has arg0 in $s3, the 0x1F in $s4 and the 8-bit flag
    in $s5; we have them in $s5, $s3, $s4. Measured and dead: a local copy of
    arg0 assigned after the e6 computation or before the 0x1F (+1 and much
    worse, 116 and 102), six orders of the three subtractions (26/30/30/32/
    32/30), three spellings of `m`'s assignment (all 30 before the reorder).
    One instruction also differs in kind rather than position: retail masks
    with `andi $s0,$s1,255` where we emit a plain copy, because gcc knows
    `arg1 & 7` already fits in a byte -- no spelling found that keeps the
    mask.
    The permuter's second run, from the improved base, found the last real
    thing: `s16 x` rather than `s32` for the packed-colour temp, 26 to 22 --
    the same narrow-type lever as the `u8 m` above. Narrowing `y`, `lim` or
    `f` the same way is +2, -1 and +3, so it is that one variable.
    An earlier run reached 23 from 26 with a win that is **semantically
    wrong**: it moves `lim = 0x1F;` to after a `return`, so the variable is
    read uninitialised at the three subtractions. The declaration reorder it
    also made is worth nothing on its own (26, unchanged), so the whole gain
    was the bug. Third such find this session -- read every permuter diff.

func_8004E7B0 -- 94 differences at -5. A camera-angle update with two
    reciprocal divides (`/384` and `/85`), one real `div` (a variable
    divisor, hence the `break 7`), and an eight-byte *unaligned* struct copy
    at the end -- `lwl`/`lwr` + `swl`/`swr` between two gp-relative symbols,
    which is what an all-`u8` record of alignment 1 gives.
    What paid: writing `/ 85` per arm rather than through a shared `z` local,
    which stops gcc cross-jumping the three arms together -- -17 to -5.
    What is left, and it is all the same shape: retail keeps **three** copies
    of the `* 0x1A` expansion and three materialisations of the 0x60606061
    magic, one per arm, sharing only the `mult`/`mfhi`/`sra` tail; and it has
    **two** `addiu $v1,-313` for the two out-of-range arms where we emit one.
    gcc is cross-jumping what retail keeps apart. Measured and dead: swapping
    the inner arms (no change), naming the four halfword reads (no change),
    `z` shared versus per-arm (that one is the -17 to -5).


func_8005FC1C -- 27 differences at +2 (138/140). A function-pointer relocation
    table: it looks `arg0` up in D_800F5918's 80 eight-byte entries, then
    dispatches on the found value's high and low halves to one of twelve
    hand-written GTE routines.
    What paid, and it is almost entirely one lever: **the case order decides
    the arm order in memory**, and the target's is not numeric. Reading the
    arm addresses out of the listing gives 9, 0x209, 0x11, 0x211, 0xD, 0x20D,
    0x15, 0x215 for the first group and 0xD, 0x20D, 0x15, 0x215 for the
    second -- an interleaving of the two 0x200 ranges, not a sort. Writing
    the cases in that order is 30 differences to 27, and every arm's label
    then lands where retail's does.
    What is left: retail's SECOND dispatch has two inline `jr $ra` /
    `addu $v0,$a0,$zero` returns, one per subtree leaf, where we emit one
    shared block and two `j` to it (+2). The first dispatch, which has a
    deeper tree, already comes out with its four inline returns. Measured and
    dead: explicit `default:` labels in one or both switches (27, unchanged),
    an explicit `default:` in the outer switch too (27), and hand-writing the
    second dispatch as the nested `if` tree retail's branches describe --
    that is -2 rather than +2 and inlines each arm's address load into its
    return, a different shape again. The full flag sweep says the default row
    is best.

func_8005A98C MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8005A98C_WAS -- 2 differences at equal length (133/133), all of them
    register naming. An RGB-to-HSV converter, leaf, gp == 0, no addressing
    work at all.
    What paid:
      - a name for `c[i]` inside the max/min loop: retail reads the byte once
        into `$a1` and uses it for both comparisons, and without the name the
        two reads stay separate. -1 and 118 differences to 133/133 and 73.
      - **computing `mx + mn` BEFORE `(lim & 0xFF) * 2`, not after.** 73 to
        57 on swapping two adjacent statements. The permuter found it as an
        extra name; the plain swap is worth exactly the same, so it is the
        order and not the name.
      - initialising hi, lo, i in that order: 57 to 55.
      - `df = mn; df = mx - df;` rather than `df = mx - mn;`, 55 to 54. The
        permuter's rerun found it paired with a `(sum << 8) << 4` split that
        is worth nothing on its own -- decomposed before installing.
      - naming BOTH modulo indices -- `j1 = (hi + 1) % 3; j2 = (hi + 2) % 3;`
        -- rather than writing them inside the subscripts: 54 to 43. The
        permuter's third run found the `j2` half alone (49); doing the same to
        `j1` is six more, which the permuter had not reached.
      - naming `sum << 12` and placing it before the `d2` computation: 43 to
        24. The permuter's fourth run found it; it is the same
        statement-order lever as the `mx + mn` swap above.
      - **writing `df << 12` inline in BOTH arms** of the saturation branch
        rather than through a shared `n` above them: 20 to 3. gcc 2.8 has no
        global CSE, so the shared name is what makes the two arms differ from
        retail -- the same lever as func_8004E7B0's per-arm `/ 85`. The
        permuter found it bundled with two changes worth nothing (an `int`
        third parameter and a two-name split of `c[lo]`, both 20 alone).
      - `n / df + (hi << 13)` rather than `(hi << 13) + n / df`: 3 to 2.
      - **`x > c[hi]` rather than `c[hi] < x`** in the max/min loop: 24 to
        20. Semantically identical, and it decides which of the two bytes is
        loaded first -- the loaded value on the left of the comparison is
        loaded first. Three spellings measured: `c[hi] < x` is 24,
        `x > c[hi]` and `!(x <= c[hi])` are both 20.
    What is left: two registers exchanged on the final add -- retail has the
    quotient in $v0 and `hi << 13` in $v1. Measured and dead: a name for the
    quotient (2 or 3 depending on the add's operand order), a name for the
    shift (2), splitting the add into two statements (2), and six orders of
    the three initialisations (55/77/57/76/77/77 at the time).


func_8005ABA0 -- 175 differences at -2 (178/176). The HSV-to-RGB inverse of
    func_8005A98C, leaf, gp == 0. The instruction sequence is broadly right
    and the residue is one whole-function register offset plus two
    instructions.
    What paid, in order:
      - **`u8 arg4`, not `s32`.** The listing loads the fifth argument's
        stack slot with `lbu`, so the parameter is a byte; declared `s32` it
        becomes an `lw` that lands in `$a2` and forces a copy of `arg2` out
        of its argument register, which shifts every register after it.
        functions.h's PROVISIONAL prototype was the `s32` one and is now
        fixed (func_8005AE68, its only decompiled caller, is unaffected --
        rechecked at 22).
      - `u32` for the `s & 0xFFFF` selector, for retail's `sltiu`.
      - `(sa * 2) * L` written through a named `d = sa * 2;`: as
        `sa * 2 * L` gcc reassociates to `sa * (2 * L)` and emits
        `sll $v0,L,1` where retail has `sll $v0,sa,1`.
      - a second name for the third clamped byte only -- `e2 = r2;` --
        which m2c's own naming had spotted. Giving the first one the same
        treatment is a further -1, so it is that one variable.
    What is left: retail computes `&c[m]` before the load that uses it and
    keeps a copy of the first packed byte right after its shift; we fold both.
    Measured and dead: a `u32 *pm = &c[m];` before the `if` or in both arms
    (-3 either way), a `u32 *pa` for each arm's early store target (-5), a
    second name for the first clamped byte (-3), and splitting the first
    shift into `r0 = c[0] + 0x800; r0 = r0 >> 12;` (-4).


func_8002A9C0 -- 6 differences at equal length (125/125): three scratchpad
    addresses materialised in the order 310, 308, 318 where retail has 310,
    318, 308. A sprite blit that fills four scratchpad points and calls the
    GTE quad routine twice.
    Everything else came from four levers, in the order they paid:
      - **base locals for every scratchpad block.** Written as literals
        inline -- `((u8 *)0x1F8002A0)[3]` -- gcc folds each field's offset
        into its own `lui`/`ori` constant (0x1F8002A3, 0x1F8002A7, ...) and
        the function is nine instructions long. One local per block, used
        with displacements, is +9 to -3. The *call arguments* stay literals:
        retail materialises 0x1F800300 twice, once for the base and once for
        the call, which is the two-materialisations rule.
      - a separate name for the frame countdown. `v = p[0x60] - 0x10;` reused
        for the later u16 arithmetic gives the subtraction and the
        sign-extend different registers; its own name puts both in `$v0` and
        is -1 and 111 differences to 125/125 and 34.
      - names for the 0x55555555 constant and the `p[0xC]` read, placed
        before the byte stores that follow them: 29 to 21.
      - a name for the `p[0x14]` call argument, read BEFORE the
        read-modify-write that precedes the call. Without it gcc hoists the
        RMW's load above the two byte stores and fills the call's delay slot
        with the `addiu $a3,1` instead of the RMW's store: 14 to 6.
    Measured and dead on the last six: all 24 orders of the four base-local
    assignments (best 6, then 8, 8, 10) and all 24 orders of their
    declarations (all 6).

func_80048D08 -- one register-to-register copy, everything else exact.
  131/131 instructions, 59 differences, and the whole residue is two things:
  retail's `addu $a0,$a1,$zero` -- a second register for the D_8009B45C
  pointer across the unaligned struct copy -- and the $t3/$t4 pair (outer
  counter vs the 0x44C giv) exchanged, which is almost certainly downstream of
  the missing copy.
  What paid, in order: the symbol is the BARE form, not the aggregate --
  gp=0 in this listing, so recipe branch 1 applies and `as -G0` with the real
  scalar declaration gives retail's per-reference `lui $r,%hi / lw $r,%lo($r)`
  through one register, where `_IS_AGGREGATE` lets gcc hoist the %hi out of
  the loop as an invariant and every reference collapses to a single `lw`.
  A local `p = D_8009B45C;` for the first group of field reads (0x43C, 0x440,
  0x444) with the last two reads left inline is what reproduces retail's mix
  of one load and two reloads.  The tpage arithmetic
  `((a % 2) * 0x19000 + 0xD810) >> 4` must be written **inline in each arm**
  of the k-test rather than shifted into a variable before the loop: as a
  statement it emits the srl where the products are, inline it is a loop
  invariant and gcc hoists it into the preheader, which is where retail has
  it (one of the two lands in the second `jal`'s delay slot).  That single
  change was 84 differences to 59 and fixed the whole prologue.
  `(u32)(...) >> 4` for the srl (a signed `>>` is `sra`), `u16` locals for the
  two halfword values so the redundant-looking `andi 0xFFFF` appears, and
  `(s32)` on the sum so the `addu` comes out index-first.
  Measured and dead, for the copy: `p2 = p`, `c = p` reusing the name of the
  0x43C pointer, `p2 = D_8009B45C` re-read before *and* after the struct copy
  (both +2, because a bare symbol re-read is two instructions and the copy is
  one), the second group written inline, a named scaled offset, and a named
  local for the 0x43C pointer.  gcc coalesces every source-level copy here;
  the copy is an allocation decision, which is the permuter's class.
  sweep_try: 59 is the best of the table, and `-G0`/`-G2` on the assembler
  tie -- the function has no `%gp_rel` at all, so the threshold is free.

func_80047480 -- one register, thirty sites: the system-block pointer is $a1
  where retail has $a2, and every store in the twenty-store initialiser block
  is off by that one register.  111/111 instructions, 60 differences, and the
  whole residue is that rotation plus two scheduling positions.
  Needs `-fno-strength-reduce` (recorded in build.py) and `as -G0`.  Without
  the flag the last nested loop is *reversed* -- `addiu $v1,$zero,31` /
  `addiu $v1,$v1,-1` / `bgez` where retail counts up against `slti 32` -- and
  the outer `sll $a3,6` becomes a `addiu $a0,$a0,64` giv; that is one
  instruction short and 7 differences worse.  gp=0, so the assembler
  threshold is free and the bare form gives retail's per-reference
  `lui $r,%hi / lw $r,%lo($r)`; the first loop reloads the symbol five times
  per iteration and that is what the bare form reproduces.
  What paid: `(D_8009B45C + i)[0x40C]` rather than `D_8009B45C[i + 0x40C]`
  for `addu base,index` on the byte stores, and `(i + i)` rather than `i * 2`
  for the same on the halfword ones -- a named scaled index does it too, the
  two are identical.  `u16` for every 0xFFFF store, because `s16` folds it
  into an `addiu -1` and retail shares one `ori $zero,0xFFFF`.  A local `p`
  for the initialiser block (one load for twenty stores, where the first
  loop's inline symbol reloads).  A second name `r = p` for the fill loop
  plus an explicit `if (n != 0) { do … while }` -- the guard reads through
  `p` and the body through `r`, which no `for` can express, and that is
  retail's `addu $a0,$a2,$zero` in the preheader.  An explicit byte offset
  `k` in the last loop for retail's `addiu $v1,$v1,2`.
  Measured and dead: base locals for 0x801E2800 and 0x801E4000 (much worse,
  -2 and 108 -- keep them as literals), four positions for the `i = 0`,
  four declaration orders, dropping `q` or `r`, `-fno-schedule-insns` with
  and without the strength-reduce flag, `-O1`, `-fno-function-cse`.  A
  permuter run of 125 iterations from this base found nothing.
  The two scheduling positions that are not the rotation: retail materialises
  0x801E4000 whole (`lui`/`ori` adjacent) before loading the pointer, where we
  leave the `ori` twenty instructions later filling an `lhu`'s delay slot; and
  retail keeps the 0x442 store where it is written, where we hoist it next to
  the 0x388 store because both consume the same 0xFFFF.  Retail's 0xFFFF
  therefore lives in `$a0` across the block, which is the extra long-lived
  value that pushes `p` to `$a2` -- so the two are the same problem.

func_8004A518 -- 113 built against 112, and the single extra instruction is a
  callee-saved register: gcc hoists the loop-invariant `0x63` out of the
  middle loop into $s6, where retail materialises `addiu $v0,$zero,0x63`
  inside the body and reuses that register for the 0x40 two instructions
  later.  That costs a `sw`/`lw` pair in the prologue and epilogue and shifts
  every following instruction by one, which is the whole of the 73-difference
  count -- loops 1 and 3 and the tail align instruction-for-instruction
  either side of it.
  Needs `as -G0` (recorded in build.py): gp=0, and retail reloads D_8009B458
  through `lui $r,%hi / lw $r,%lo($r)` once per iteration of loop 1, which is
  the bare form.
  What paid: three separate offset variables, one per loop -- one shared `o`
  makes the pseudo live across the middle loop's two calls, so it goes
  callee-saved in *all three* loops where retail uses $a0, $s2 and $v1
  (105 -> 105 with the count still wrong, but it is a prerequisite); three
  separate record pointers `e1`/`e2`/`e3` for the same reason (78 -> 73);
  and a *rotated* `for (; i < *(s16 *)(p + 0x510); )` for the middle loop
  rather than an explicit `if` plus `do`/`while` (116/+4 -> 113/+1, 105 ->
  78) -- the explicit guard loads the count into a register that dies, and
  the rotated form lets the bottom test's load carry round the back edge into
  the next iteration's `addu $v1,$v1,$s2`, which is what retail does.
  Measured and dead on the 0x63: a shared name for the two QImode constants
  (`c = 0x63; … c = 0x40;`, which is exactly retail's one-register-two-values
  shape) is -4 and 108, because gcc constant-propagates `c` before the loop
  pass ever sees two sets; borrowing `o1`, `o3` or `m` as the name; moving the
  store to the end of the byte group; `acc = acc | m`; a pre-loop local for
  the 0x40 halfword constant; indexing the table instead of a cursor;
  `do { … } while (0)` round the store group and round the two calls; and
  `-fno-schedule-insns`, `-fno-schedule-insns2`, `-fno-strength-reduce`.
  The permuter's best output (score 740 against a base of 1050) re-scores at
  **73**, and its *fourth*-best (score 875) re-scores at **27** -- another
  instance of the ranking inverting.  Reading what it changed: it declared
  `o2` as `unsigned char`.  That is semantically wrong (0x180 does not fit,
  and the built code has `addiu $s2,$zero,128` and an `andi $v1,$s2,255`
  retail does not have), but it is a pointer at the mechanism: **one more
  instruction in the loop body is enough to stop the hoist**.  Retail's body
  is 27 instructions and ours is 26, so the missing `addiu $v0,$zero,0x63` is
  also the thing that would prevent its own hoist -- which is why no
  source-level spelling of the constant reaches it.  The instruction retail
  has and we do not is more likely to be somewhere else in the body.

func_8003BF00 -- 111 built against 112, 35 differences (was 42), and the single missing
  instruction is `addu $a0,$a2,$zero`: retail holds the record pointer in $a2
  and copies it into $a0 for the one call, where we hold it in $a0 and need no
  copy.  Everything else is the two standalone arms' scheduling.
  A `jr $v0` jump-table switch on 0..5 with cases 0, 1 and 4 sharing a tail and
  cases 2, 3 written out; gp=0 and at=1, so `as -G0` with the default
  `volatile s32 D_8009B0F4` gives retail's `lui $at` store and its per-arm
  reload.  **The full build is what proves the table landed** -- try_func
  normalises the symbol.
  What paid, in order: a named local for the mask -- `m = 0xFFDCFFFF;` then
  `v = D_8009B0F4; v &= m;` rather than `v = D_8009B0F4 & 0xFFDCFFFF` -- 95 to
  90, and it is the same lever that was worth 20 on the sibling func_80043328;
  a second name for the record pointer, `e = p;` at the top with every store
  through `e` and the *call* through `p`, which is what makes the parameter
  copy survive at all (90 to 62); the mask assignment written before the arm's
  first store (62 to 58); and naming the call's second argument at the top of
  its arm so the argument setup precedes the last store and reorg can fill the
  `jal`'s delay slot (58 to 53, then 42 with case 3's read named and its
  `+ 0x800` written in place).
  Measured and dead: routing the call through `e` as well (the copy then
  coalesces and it is 90 again); borrowing `p` as the name for a global read in
  case 3 or case 5 (-2, 107); `v`/`q` as the borrowed name for case 3's read;
  five separate mask names rather than one (92); `0xFFDCFFFF & D_8009B0F4`;
  `u32` for the mask.
  A permuter run of 54 stored outputs re-scored: the best correct-length one is
  86 against the then-base of 90, and its mechanism was an *unreachable*
  `new_var = p;` between two cases -- semantically an uninitialised read, but
  it is what pointed at the two-names-for-the-pointer lever above.
# ---

  **2026-08-28, `tools_src/sweep_arm_split.py`: 42 -> 35, length error unchanged.**
  Two names for the D_8009B118 read, one in arm 3 and one in arm 5.  The -1
  is untouched.

func_80043328 -- 115/115, 12 differences, all of them in the `mode == 0` arm:
  the 0x18000 constant's `lui`/`ori` pair is split (the `ori` scheduled into a
  load's shadow) where retail keeps it adjacent, and two stores sit before
  their load rather than in its shadow.
  A three-way comparison-tree dispatcher with `goto` arms in retail's address
  order and a tail shared by cases 1, 2 and 3.  `one = 1;` as a real local is
  required: retail materialises it into $s1 for *both* the `mode == 1` test and
  the shared tail's `sb $s1,0x46`.  gp=0, at=3, `as -G0`.
  What paid: naming the D_8009B118 read in each arm that uses it -- 90 to 38,
  because retail evaluates the call arguments at the top of the arm and the
  store into the load's shadow; a named mask local per the func_8003BF00 entry
  -- 38 to 18; **borrowing `q`**, the shared tail's pointer variable, as the
  name for m0's D_8009B118 read -- 18 to 13, m0 being mutually exclusive with
  every arm that reaches the tail; and **borrowing `b`**, the name used for the
  reads in m2 and m3, for m0's 0x18000 -- 13 to 12.  Both are the
  func_8002596C lever: a name whose live range is only in an arm that can
  never run at the same time.
  Measured and dead: separate `b` names per arm (40, much worse -- the
  borrowing is the point); `c` as a fresh name for the 0x18000 at either
  position; `one` or `m` as the borrowed name; four statement orders in m0 and
  three more targeted ones; `v` as the borrowed name (21, 22).
  A permuter run from this base produced four stored outputs; the best
  re-scores at 15.
# ---

  **2026-08-28: the whole D_8009B0F4-family rewrite is WRONG here, and by a
  lot.**  Four functions matched the same hour on `v = D_8009B0F4; ...
  D_8009B0F4 = v & m;` with named locals, and applying that shape to this
  function is 12 -> 22; adding the family's `b = 0x18000;`-first ordering on
  top is 26 to 28.  The compound `D_8009B0F4 &= m;` this candidate already
  uses is right, and `b = 0x18000;` moved to the top of m0 -- which is where
  retail's `lui` in the dispatcher's delay slot says it belongs -- is 15
  either plain or under a `do`/`while`.  So the family recipe is not a
  recipe for every member; read the arm before transferring it.
func_8002DDFC MATCHED 2026-08-30 by PORTING Unchiga's C (docs/MERGE_UNCHIGA.md);
  the history below is what this tree had reached on its own.
func_8002DDFC_WAS -- 76/76, 56 differences, and every one of them is that retail
  holds the record pointer in $a2 while we hold it in $a0, with retail's
  `addu $a2,$a0,$zero` in the first branch's delay slot where we put the
  `sw $ra`.  Identical residue to func_8003BF00 before the two-names fix.
  This function did not exist until this block: splat had merged it into
  func_8002DD74 because nothing in .text branches to it -- its only reference
  is as a *callback argument* from func_8002DF2C, which is why it was declared
  `extern u8 D_8002DDFC[];` in variables.h.  Splitting it meant truncating the
  listing at splat's own `alabel`, writing the tail out with its own
  glabel/endlabel and size, adding the INCLUDE_ASM line, moving the symbol
  from variables.h to a prototype in functions.h, and updating the one caller.
  The build stayed byte-identical through all of that, which is the proof the
  split is right.
  A three-way if-chain dispatcher with no shared tail.  gp=0, at=3, `as -G0`.
  The named-mask lever pays here too (58/+1 to 56/equal).  The `e = p;`
  two-names lever does **not**: copy propagation folds it away because `mode`
  stays live in $a1 across the whole if-chain, where func_8003BF00's jump table
  frees $a1 early.  That is the discriminator between the two, and it is the
  reason the same residue needs different source there and here.

func_80046768 -- 125/125, 46 differences, and all but a handful are a register
  shift through the three unaligned struct copies: retail uses $t0-$t3 where
  we use $a3,$t0-$t2, so retail has one more register occupied there and we
  have not found what occupies it.
  Two things paid and are worth keeping:
  * **The clear loop's base is a literal, not the symbol splat prints.**
    Retail is `lui $a1,0x801E` / `addu $a2,$a1,$zero` / `ori $a2,$a2,0xA7FF`:
    gcc materialises a constant whose low half is zero with a *single* `lui`,
    and then CSEs that `lui` into the `ori` that builds 0x801EA7FF. Written
    against `D_801E0000` it is `lui`/`addiu` plus a separate pair for the
    limit -- 122 differences to 60 on that one change. splat symbolises
    `lui $a1,%hi(D_801E0000)` and `sw $zero,%lo(D_801E0000)($a1)`, and both
    assemble to the same bytes as our `lui $a1,32798` / `sw $zero,0($a1)`, so
    three of the remaining "differences" are try_func reading text rather than
    bytes.
  * **D_8009B0F0 and D_8009B120 need `lui $at` while the four-byte pointers
    beside them keep %gp_rel, and the widths are equal.** No real threshold
    exists, so the SIZED arms declare eight bytes -- still inside cc1psx's own
    -G8, so the bare symbol survives -- with `as -G4`. Same inflate-to-eight
    recipe as func_8002FD10 and func_800175A0.
  Also: naming the last `D_8009B45C` read before the 0x50C store (so it
  schedules into the `addiu %lo` slot rather than needing a `nop`) closed the
  length error, and naming the D_8009B458 read beside it was two more.
  The call `func_800494F4(0x801E1670)` takes ONE argument even though the call
  site sets $a0-$a3: $a1 is the 0xFF constant used by four `sb`s, $a2 is a
  field read used by a `sh`, and $a3 is a reload of D_8009B45C used by the
  store in the delay slot. The existing prototype is right; this is the
  "look a few instructions back" tell.
  **The permuter's best two outputs re-score at 5 and 7 and are both
  semantically wrong in the same way: they read an UNINITIALISED local.**
  output-25-1 puts `if (D_8009B45C || new_var)` round the clear loop's body
  with identical arms; output-220-1 stores through `new_var` one line before
  assigning it. Decomposed, the plausible halves of output-220-1 -- hoisting
  `(arg0 & 0xFF) != 0` into a local before the 0x4A store, and `u16 c` instead
  of `s32 c` -- are worth **exactly nothing**, alone or together. So the whole
  gain is the uninitialised read, and what it does is give the function one
  pseudo that is live at entry. That is the mechanism the register shift
  needs; no plausible source shape for it has been found, and the function has
  one caller passing one argument, so a second parameter is not it.
# ---

func_8005A6A8 -- re-examined; still 1 difference, `subu $v1,$zero,$v1` where we
  emit `subu $v1,$zero,$v0`. Retail negates the *copy* in place; every
  spelling we have tried lets gcc's copy propagation rewrite `-v` as `-w`,
  because `v = w` dominates the negation and the two are provably equal.
  New negatives this pass: `if (w < 0)` rather than `if (v < 0)` as the guard
  (unchanged at 1), swapping which of the two names carries the value through
  the clamp (9), and `v = 0 - v` (1). The permuter has now been run on it and
  produced no stored output at all.
# ---

func_80024E58 -- re-examined; still 2, and the residue is which of
  `andi $s0,$v0,255` and `addiu $s0,$s0,-1` lands in the `jal func_8002C604`
  delay slot. Retail has both before the call and reorg takes the second;
  we compute the decrement after the call, so reorg takes the mask.
  Writing the decrement before the call **does** fix the delay slot and costs
  something else every way it was tried: `n = (v & 0xFF) - 1;` merges `v` and
  `n` into one register where retail keeps `v` in $v0 and `n` in $s0 (4);
  `n = v & 0xFF; n = n - 1;`, `n--` and `n -= 1` all hoist the `andi` above
  the `sb` to D_8009B364 that retail emits first (3); an intermediate name for
  the masked value is 4; putting the mask before the store is 3. Five
  spellings, three distinct scores, none better than leaving it at 2.
# ---

func_8005A98C -- re-examined; still 2, `mflo $v0` / `sll $v1,$t1,13` against
  our `mflo $v1` / `sll $v0,$t1,13`. The `addu $v0,$v1,$v0` that consumes them
  is identical, so it is purely which pseudo gets $v0.
  Eight spellings of `n = n / df + (hi << 13)` measured this pass: operands
  swapped (3), split into two statements against `n` (2), a named shift
  hoisted out (29 both ways -- much worse, the shift must stay inline), a
  fresh name for the sum with a copy back (4), a fresh name for the quotient
  (2 and 3), and the quotient accumulated then copied (6). Three-plus
  spellings at 2 says the axis is elsewhere.

func_800339D0 -- 134/134, THREE differences, and all three are one value's
  register: retail holds `&D_800EB0F8` in $s0 and copies it into $a0 for the
  `func_80035B7C` call, filling that call's delay slot; we hold it in $a0
  directly and leave the slot a `nop`. Same residue as func_8003BF00.
  gp=2, at=1, and the two symbols are equal widths, so `D_8009B140_SIZED8`
  (eight bytes) with `as -G4` is what gives retail's `lui $at` store while the
  one-byte D_8009B2F8 keeps %gp_rel. `D_8009AF76_IS_AGGREGATE` and
  `D_8009B34D_IS_AGGREGATE` for the two %hi/%lo byte reads.
  What paid, in order: **naming the D_8009B2F8 read** before the 0x633E
  read-modify-write, so the two chains interleave and both load-delay slots
  fill -- 119 differences and +1 to 14 at the right length, the single biggest
  step; a **separate cursor name** for the first copy loop's destination
  rather than reusing the name that holds the func_80035C38 result (14 to 11,
  the phase rule); and then **reusing the first loop's destination name for
  the second loop's record cursor** (11 to 3). That last one is the phase rule
  read backwards and it is the second time it has paid: retail carries `$a0`
  through both loops for one role and `$v1` for the other, so the second
  loop's two cursors want the *same two names* as the first loop's, not fresh
  ones. A `u16 *` name for the second loop's other cursor is what makes the
  pair work; using the first loop's `u8 *` source name for it instead is 30.
  Measured and dead on the last three: borrowing the func_80035C38 result's
  name for the address (14), two locals both assigned from the address (14),
  a copy `e = r` at the call (3, unchanged), `&D_800EB0F8[0]` at both sites
  (3), computing the address before the preceding call (-1 and 65), and three
  declaration orders.
  A permuter run produced two stored outputs; they re-score at 7 and 10.
# ---

func_8001F364 -- 126/126, 14 differences, and the residue is the dispatcher's
  delay slots: retail fills `beq $v0,$zero,L3` from L3's own first instruction
  (`addiu $v0,2`) and the next branch from the fall-through's `return 1`,
  where we leave the first a `nop` and hoist the m0 arm's `addiu $a0,0x10`
  into the second. Four instructions, plus m0's two constants landing in $v1
  where retail uses $v0.
  Three things paid. The **0x48000 split**: `D_8015C424 + idx * 0x1C + 0x48000`
  written as one expression folds the constant into the symbol's %hi/%lo and
  is two instructions short; a base local first -- `r = D_8015C424;` then
  `g = r + idx * 0x1C + 0x48000;` -- keeps retail's separate `lui`/`ori` of
  0x48000 and the 0x36B4 displacement on the load. Same shape as
  func_8001B938. A **nested `if`** in the `mode == 1` arm rather than an early
  `return 1`, which stops gcc cross-jumping that return into the function's
  first exit block. And **`do { … } while (0)` wrapped round the arms**: the
  permuter found it, and it is worth 82 differences to 14 on its own. The
  block runs from the dispatcher's last `return 1` through the `mode == 2`
  arm, with the `goto` labels inside it -- which is legal C and reads as the
  macro WORKFLOW says to look for.
  Also here: `D_800F284A` is a real linker symbol (it is in
  config/undefined_syms_auto.txt) two bytes into D_800F2848; declaring it and
  writing `D_800F284A[0]` removes two false differences that were only
  try_func reading text rather than bytes.
  Measured and dead on the dispatcher: a `switch` instead of the `if` chain,
  inverting the `v == 0` test, testing `>= 2` first, naming the fourth or the
  fifth argument at the top of the m0 arm, and a second `do`/`while (0)` round
  the tail of the dispatcher.

func_80071CB0 -- 130/130, THREE differences, and all three are the order of
  three instructions in the main loop's preheader: retail emits the stack
  array's address, then `best = 0` from `$zero`, then `i = 0` CSE'd *from
  best*; we emit `best = 0` CSE'd from the loop counter, then the array
  address, then `i = 0` from `$zero`. Same instructions, three positions.
  What paid, in order:
  * **A base local plus the index form for the second scan's table.** The
    loop reads two fields of the same record, `+6` and `+2`. Written against
    the symbol, gcc hoists `D_801AB2A0 + 6` out as an invariant and the
    function is +4; written as an explicit cursor stepping 0xC, gcc gives the
    cursor a *bias* of +2 and it is +1; written as `e = D_801AB2A0;` before
    the loop and `e + i * 0xC + 6` / `+ 2` inside, it is exactly retail's one
    cursor with two displacements. 83 differences to 26.
  * **Flipping which side of each comparison the candidate sits on**, so the
    candidate's field is loaded before the incumbent's (26 to 20, and again on
    the final comparison).
  * **Merging the zeroing loop's counter with the main loop's counter** --
    one variable, not two. Retail carries both in `$t1`, resetting it to 0
    between them. 20 differences to 6, the largest single step, and a third
    instance of the phase rule running backwards.
  * Writing `best = 0;` immediately after `k = 0;` rather than after the two
    table-base locals: 6 to 3.
  Measured and dead: `-fno-strength-reduce` (+5); a named pointer for the
  stack array used at all four access sites (-3); `i = best` in the `for`
  init, which is semantically equivalent because `best` is always 0 there but
  gcc cannot prove it (+6); three other preheader orders.
  A permuter run produced eleven stored outputs; the best re-scores at 4.
# ---

func_80071700 -- 146 built against 137, and the whole +9 is loop-invariant
  motion that retail does not do: gcc hoists the constants 4 and 5 into
  callee-saved registers and the `(u32)(mode - 3) < 3` range test into a
  *stack slot*, and the spill then pushes two more values out of registers.
  Retail materialises 4 and 5 inline in branch delay slots and recomputes the
  `addiu -3` / `sltiu 3` pair inside the loop every iteration.
  `-fno-strength-reduce` is recorded in build.py and is worth 4: without it
  the record cursor picks up a second, biased register (`$s0 = $s3 + 4`,
  reading `2($s0)` where retail reads `6($s3)`), which is the third user of
  that flag for exactly that shape.
  What is right: `one = 1;` as a real local -- retail uses `$s5` for the
  `mode == 1`, `fp == 1` and `sel == 1` tests *and* as the shift amount in
  `sllv $v0,$s1,$s5` for the `* 0xC`; the `(u32)(mode - 3) < 3` spelling,
  which is gcc's own fold of `mode == 3 || mode == 4 || mode == 5`; and the
  cursor form for the record.
  Measured and dead: writing the whole guard as one `&&` chain rather than
  nested `if`s with `goto next` -- byte-identical output either way, so the
  condition's spelling is not the axis. The axis is register pressure, and
  the same class as func_8004A518's hoisted 0x63: retail's build had one more
  pseudo in play and therefore hoisted less.

func_80021598 -- 161 built against 158, and the residue is gcc *eliminating
  two induction variables that retail keeps*. The loop walks five cursors: a
  Rec20 record, that record + 1, two into D_801D5608, and the D_8009B1E8
  pointer carrying a running total at +0x2C. Retail strength-reduces exactly
  one of them -- the D_801D5608 pair, `$s2 = $s4 + 0x78` with negative
  displacements, which is gcc's own giv and appears in our build too -- and
  leaves the other two as plain biv cursors, `addu $s0,$a0,$zero` with
  `44($s0)` / `48($s0)` and `addiu $s1,$s3,1` with sixteen displacements from
  0 to 0x17. We get biased givs instead: `$s0 = $a0 + 44` and `$s1 = $s3 + 6`,
  so every displacement in the body is shifted and the preheader stores move
  to a different base.
  `-fno-strength-reduce` removes the biases *and* the giv retail has, which is
  -4. Writing the second D_801D5608 cursor explicitly (`u = t + 0x1E;`) and
  taking the flag gets to -1 with 134 differences, but that source shape --
  a second cursor into the same array at a fixed +0x78 -- is not something
  anyone writes, so it is recorded as a measurement rather than installed.
  Measured and dead at default flags: moving `f += 0x20;` from the middle of
  the body to the end, which should move the bias from +5 to 0 by the
  bias-follows-the-last-use rule and moves nothing at all.
  The rest is right first try: one `v` for every loaded field (retail passes
  the loaded register straight to the call and stores the same register), the
  `lb`/`lh`/`lbu` widths per field, and the accumulate written as
  `*(s32 *)(p + 0x2C) = *(s32 *)(p + 0x2C) + func_80021558(n, v);` so the
  reload lands where retail has it.
  A permuter run's best correct-length output re-scores at 27 against our
  154, and what it changed is one expression: `v = (*(new_var = &f))[8];` --
  taking the address of the cursor, into a variable that is then dead. That is
  a **seventh** instance of the not-installable permuter class and a new
  sub-kind: not an uninitialised read this time but an address-taken local,
  which is precisely what stops gcc replacing the biv. Same lesson either way
  -- the mechanism is now named, and no plausible spelling of it has been
  found.

func_80013154 -- 131/131 with `-fno-strength-reduce` (recorded in build.py;
  without it gcc builds a biased giv for the +0x5150 store and the function is
  +5), and 117 differences, nearly all of them a register rotation downstream
  of two loop-invariant hoists retail does not have: the `2` stored at +0x5110
  and the loop bound `arg0 + 0xA2C0`. Retail materialises both *inside* the
  loop -- `addiu $v0,$zero,2` and `ori $v0,$zero,0xA2C0` / `addu $v0,$s4,$v0`
  every iteration -- and keeps five callee-saved values; we hoist both, need
  seven, and every register after them shifts.
  This is the fourth instance of the standing open problem (func_8004A518,
  func_80071700, func_80021598 are the others): gcc hoists small loop
  invariants where retail does not, and the permuter only reaches it through
  shapes that cannot be written.
  Probed directly on this one, since the length is right and the mechanism is
  visible. Three things that do NOT change it: adding named temps inside the
  loop (more pseudos), naming the `2` as a local, and `(s32)`/`(u32)` casts on
  the bound comparison -- all 117, unchanged. One thing that does: writing the
  bound as `p - arg0 < 0xA2C0` stops the *bound* being hoisted and takes the
  count to 79, but costs an instruction (+1) and leaves the `2` hoisted, so it
  is a measurement rather than a candidate. So the bound and the constant are
  hoisted by the same pass but respond to different things, and neither
  responds to pseudo pressure.
  The addressing is right and was the interesting part: SIX one-byte symbols
  (D_8009B142/3/4 and D_8009B14A/B/C) need the bare `lui $at` store while
  SEVEN one-byte symbols beside them (D_8009B0A0..D_8009B0D0) keep %gp_rel.
  Equal widths, so no real threshold -- the `_SIZED` arms declare two bytes and
  the unit assembles at `as -G1`, which is the same knob as D_8009B318 and
  the first time it has been needed for a whole group rather than one symbol.
  D_8009B0A3 needed a new `_IS_SCALAR` arm for the same reason in reverse.
  Also right: `six = 6;` as a local (retail's $s3 serves the D_8009B0A1 store
  and both +0x5124 / +0x514C stores), and the 20-byte unaligned copy at the
  end as a `struct { u8 b[20]; }` assignment.

func_80040DD8 -- MATCHED 2026-08-21. Kept here only for the two levers it
  established, both of which apply elsewhere:
  * Name a GROUP of loads before the group of stores that consumes them. gcc
    cannot hoist a load through one `u8 *` above a store through another, so
    retail's batched loads have to exist in the source. Two sites here took it
    from 170/164 and 121 differences to 164/164 and 22.
  * **One name for two sequential constants.** The last 22 were the eight
    `- D_8009B146` / `- D_8009B148` subtractions: retail loads each offset with
    `lh` into the SAME register `$a2`, one after the other. Two named locals
    `dx`/`dy` gets the `lh` (that alone is invisible in the count -- still 22 --
    and is the reason ten earlier spellings all read as "wrong axis"); ONE name
    reused for both, assigned again between the two groups, is 22 -> 8. The
    residue was then the four X statements written in the target's *store*
    order rather than its *load* order; writing them 8 / 0x10 / 0x18 / 0x20 is
    a MATCH.
  Its sibling func_80041068 (12-byte stride, 182 instructions) MATCHED the
  same afternoon by adapting this source field-for-field -- the two levers
  above, `as -G0` (it has no `%gp_rel` at all), and one declaration:
  `D_800EFE42` needs to be its own symbol, because `D_800EFE40[1]` relocates
  as `%lo(D_800EFE40+2)`, which is the same address under a different name and
  reads as two differences. That was the whole residue.
  The third sibling, func_8004158C, carries the same block at 472 instructions
  and is still open.

Permuter re-score sweep, 2026-08-21 (score_permuter_outputs.py over the whole
park). Six stored outputs beat what was installed; every one was read before
being trusted, and the split was three not-installable, one decomposed to a
real one-line lever, and two open.
  * func_800434F4 32 (from 35) -- DECOMPOSED and installed: the only change is
    `*(s32 *)(p + 0x1C) = 0x800;` moved between the two D_801AF000 stores.
    It transfers to func_80020BE4 and func_8003B808, and it is the seed of the
    whole run of improvements to that family recorded above.
  * func_80032184 27 -> 22 -- NOT INSTALLABLE, and worth reading anyway. The
    change is `new_var = 1;` in arm m0 and `p[0x46] = new_var;` in arm m2, so
    m2 reached without m0 reads an uninitialised local. The plausible spelling
    is already available -- this function has `s32 one; one = 1;` -- and
    `p[0x46] = one;` is **27, no change**, so the gain is specifically a pseudo
    that is *defined in the other arm* rather than one that is merely equal to
    1. Fifth instance of the borrowed-name class and the first where the
    honest version is this close to hand and still does not fire.
  * func_8002C6C8 58 -> 25 -- NOT INSTALLABLE: it moves `q[0] = q[0] | 0x40;`
    to after the `return`, i.e. deletes a store the function makes. Its other
    half is legitimate and was measured -- hoisting `r = D_800F2848;` out of
    the loop -- but the candidate is -1 instructions and the hoist makes it
    +2, so the two counts are not comparable and the hoist is not a win. The
    -1 is the thing to chase in that function, not the permuter's number.
  * func_80045208 25 -> 17 -- NOT INSTALLABLE: `if (e) { A } else { A }` with
    two identical arms round the switch's `default`. The `do { … } while (0);`
    that stood in for the same shape on func_8005F3B8 does **not** reach it
    here -- both placements are 25. So the duplicate-arm idiom and the
    do/while(0) idiom are not interchangeable; whatever gcc does with two
    identical arms it does not do with one loop node.
  * func_80046768 46 -> 5 and func_80021598 154 -> 27 -- both already recorded
    above as the uninitialised-read and address-taken-local classes.
  * func_8003B744 18 -> 8 -- already recorded as semantically wrong (it casts
    an index the target does not mask).

func_80040588 -- 3 differences, first written 2026-08-21. 163 instructions, the
  fourth member of the func_80040DD8 sprite-list family (same D_800EFE48 record
  array, same 0x70 stride, same `bltz`/`do`-`while` outer loop) but a different
  packet layout and a three-way tail.
  What is right, and most of it came straight from func_80040DD8: `g`, `q`, `h`
  and `tb` as locals assigned inside the guard; a `goto again;` back into the
  middle of the loop body for the `D_8009B424 != 0` retry (retail's
  `bnez $v0, .L800405F4` jumps past the record-address computation, so it is a
  label, not a loop); `idx = e[0x17];` named as the loop body's *first*
  statement, which spreads the table read's `lbu`/`sll`/`addu` into the three
  load-delay slots of the store group (+2 and 109 differences to -1 and 105);
  and `c66`/`n` named for `e[0x66]` and `*(u16 *)(e + 0x14)` so the `a3`
  constant is materialised late rather than in the entry branch's delay slot
  (105 -> 9).
  Two declarations carry the rest:
  * `D_8009B424_IS_SCALAR` -- a new guard arm. As the default unsized array
    cc1psx emits a %hi/%lo pair, gcc hoists the %hi out of the loop into a
    callee-saved register, and the function is five instructions long with one
    saved register too many. Retail reaches it `%gp_rel`, which only the scalar
    gives. (A `_IS_VOLATILE` arm was added at the same time and measured: it is
    worth nothing here, 3 either way.)
  * `D_8009B146_SIZED` plus `as -G4`. Retail keeps each of the two `%hi`/`%lo`
    pairs adjacent; under the unsized-array arm cc1psx hoists both `lui`s to
    the front of the block. Eight bytes at -G4 makes each reference a single
    indivisible pseudo, and the 4-byte D_8009B424 beside it stays gp-relative.
    9 -> 4 on that one line. This is func_8002E470's rule, second user.
  The residue is 3, all one instruction: retail emits `lhu $a2,6($s1)` before
  `sw $zero,D_8009B424` and we emit it after. Eleven shapes were measured and
  every one is 3 or worse -- naming all three argument loads (which is what
  took it from 4 to 3), the three loads in two other orders, the store moved
  above and below the group, both sums named, `(s16)` casts moved onto the
  named reads, two `do { … } while (0);` groupings, and `volatile` on the
  global. The full flag sweep is 3 at the installed row and 81+ everywhere
  else. Permuter running.

func_800408D0 -- 3 differences. A permuter run from the 3-difference base
  saturated at 4921 iterations with ZERO outputs, so that base is a local
  optimum for the permuter and the residue needs a source-shape idea, not more
  search. (13 when first written, 2026-08-21; taken to 3
  the same day by decomposing a permuter output -- see the coupled pair
  below). 202 instructions,
  the fifth member of the func_80040DD8 sprite-list family: same 0x1F800320 /
  0x1F800344 / 0x1F800378 scratchpad packets, same D_8009B146 / D_8009B148
  offsets, same func_80041F90 / func_80042188 / func_80082840 tail, but it
  draws one sprite in horizontal slices with a `do`/`while` over a remaining
  count rather than walking the object list.
  Declarations: `D_8009B146_SIZED` plus `as -G4` (as func_80040588) and
  `D_8009B424_IS_SCALAR`.
  **The 13 -> 3 was a COUPLED PAIR, and neither half is installable alone.**
  Its permuter output re-scored 8 against 13, and what it had done was three
  things plus a junk `volatile long long` return type. Decomposed: the junk
  type is the whole of its 8 and is not installable; the `fl` borrow for the
  0x1F800378 address and the `0 >= call` flip are both worth nothing. But
  taking its `g[3] = 9` inline (with the load named instead: `v =
  *(s32 *)(q + 0x14); g[3] = 9; *(s32 *)(g + 4) = v;`) is **-1 and 116** on
  its own, and swapping the loop's last two statements to
  `q[0xE] = q[0xE] + q[8]; rem = rem - *(u16 *)(q + 8);` is **also -1 and
  116** on its own -- and the two together are 202/202 and 3. Each half
  deletes an instruction the other restores, which is exactly the shape that
  a one-at-a-time hand sweep cannot find and the permuter can. Read a
  permuter win for its *parts* even when the part that scores is unusable.
  Residue: retail issues the `lhu $a2,6($s0)` for `u6` before the call's
  fourth-argument `addiu $a3,$s2,32` and the `sw $zero,d_8009b424`, and we
  issue it after. All 23 permutations of the four statements in that block
  were measured (3 is the floor, 4 and 5 the rest), as were a named
  fourth argument (3), a named third-argument sum (7) and a named
  `*(s16 *)(e + 0x1A)` (7). The three-way tail is one loop with two entry
  points in the listing (`.L80040AE8` loads 0x40, `.L80040AEC` stores it) and
  both entries carry the same 0x40, so it is one `do { *(u16 *)(q + 8) = 0x40;
  … } while (rem != 0);` and the second label is only the delay slot of the
  test above it.
  Three levers took it from 156 to 13, all already in WORKFLOW:
  * `fl = *(u16 *)(e + 8);` named above the `*(u16 *)(q + 0xE)` store, so
    retail's two loads are adjacent and the store sinks into the branch's
    delay slot (156 -> 119).
  * the three func_80041F90 argument loads named (119 -> 118).
  * `v = 9;` -- borrowing the *dead* local that held `*(s32 *)(e + 4)` for the
    `g[3] = 9` constant. Without it the delay-slot filler takes the `addiu 9`
    and the function is an instruction short; a fresh name does not do it, and
    neither does naming the `q + 0x14` load at three positions. 118 and -1
    became 18 and the right length. `v = 9; … v = 0x2C;` for both constants is
    19, so here it is the borrow and *not* the one-name-for-two-constants
    rule -- the two levers are separable and this function wants only one.
  * declaring `a3` before `base` (18 -> 13): retail holds `base` in $s4 and
    `a3` in $s3, we had them swapped, and declaration order is what decides it.
  The residue is 13 in three clusters, all allocation or scheduling:
  the `lhu $a2,6($s0)` / `sw $zero,D_8009B424` order (the same three
  differences func_80040588 is parked on -- solve one and both close), a
  $v0/$v1 exchange around `lw 20($s0)` / `addiu 9`, and the order of the
  `lbu 0xE` / `lhu 8` pair at the bottom of the loop. Eleven shapes were
  measured against the last two and every one is 13, 14 or -1; the permuter
  ran 4900 iterations from the 13 base and stored nothing.

  **2026-08-28, still 3.**  Retail emits the `u6` halfword read BEFORE the
  `h + 0x20` address and the `D_8009B424 = 0` store; gcc sinks it past both.
  Six shapes measured: `do`/`while` round the read (9), round the store (9),
  round both together (9), a named `hh = h + 0x20` (3), the store moved above
  all three reads (4), and the `u6` read moved above `k18` (3).

  **2026-08-29: the owed `sweep_borrow.py -n 400` run is a NON-ANSWER, not a
  negative.**  270 substitutions tried, **96 failed to compile**, 0 better --
  and the tool's own warning fired, which is what that failure counter is for.
  The failures are its documented limitation: on the outermost block, which
  spans the declarations, renaming `a` to `b` leaves two declarations of `b`.
  With thirteen locals this function is almost all outer-block pairs.  Do not
  read this as "the borrow lever does not apply here" -- it was not measured.

func_8002E128 -- 8 differences, first written 2026-08-21. 146 instructions,
  gp=0 at=0, default flags, no addressing work at all.
  A card-artwork setup routine: decodes a BCD card id
  (`((n >> 4) & 0xF) * 10 + (n & 0xF)`) into an index, then reads a per-card
  flag byte from one of two tables -- D_80090C00 with six-byte entries for ids
  >= 0x200 and D_80090BA8 with two-byte entries below -- and calls
  func_8002E060 for each bit set. Both symbols were added to variables.h.
  Three things were needed and all three are existing rules:
  * `s32 arg1`, not `s16`. Retail has `addu $s1,$a1,$zero` with no sign
    extension, so the `< 0` test is on the raw word; the narrow declaration
    adds `sll`/`sra` and is +1 and 144 differences.
  * **the else branch's flag byte needs its own name.** Reusing `n` for
    `o[0]` there -- which looks right, because retail reuses `$s1` for it --
    is 49 differences, and a separate `m` is 22. Every one of those 49 is a
    register, and the split fixes the *whole function's* allocation, not just
    that block. Note this is the opposite of what the listing's register reuse
    suggests, which is the standing warning about reading register reuse as
    name reuse.
  * `p[0x10] = 1;` written *above* the two read-modify-writes on the object
    rather than below them (22 -> 19), and a base local for D_80090BA8 in the
    else branch (19 -> 18).
  * **the else branch's index wants its own name AND an `s16` declaration**,
    and the second half is the real lever. The sequence is worth writing out,
    because the first reading of it was wrong:
      - inline in the address expression: 18
      - a fresh `s32 k`: 19 (worse)
      - borrowing `q`, the mutually exclusive arm's `s32` local: 12
      - a fresh **`s16 k`**: 8
    So the borrow was a partial proxy for the narrowing, not the mechanism.
    The permuter reached 9 by declaring the borrowed `q` itself `short`, which
    is **not installable** -- `q` also holds `*(s32 *)(p + 0x28)` in the other
    arm and a 16-bit truncation of that is a wrong address -- and the two
    plausible spellings that reproduce it, a fresh `s16 k` or `s16 q` with a
    separate `s32 r` for the pointer, are both 8, i.e. better than what it
    found. Read a permuter win for its *mechanism*: here the semantically
    illegal candidate pointed straight at a legal one that is better.
    The `u16 *` pointer local and the `&=` it also carried are worth nothing.
    Where the `* 2` sits also matters: inside `q` is 12, moved to the
    indexing (`&b[q * 2]`) is 19, because retail materialises the table base
    *before* the scaling.
  The residue is 8 in one cluster, and it is two instructions' *placement*
  rather than any instruction being wrong: the RMW block itself is
  instruction-for-instruction right, but retail materialises the `1` for
  `p[0x10]` between the `sw 4` and the `sh 8` and does not store it until four
  instructions later, after the *6 index scaling, while we emit the `addiu`
  and the `sb` together at whichever end the source puts them. Thirteen
  placements and spellings were measured -- the statement at five positions
  among its neighbours, `one` as a local at three positions, the RMWs as
  compound assignments, a `do { } while (0);` round the pair, and both reads
  named -- and they are 11, 15, 18, 19, 21, 22, 23 or 30 -- the four placement
  probes were re-run against the 8 base after the `s16` landed, in case the
  levers were coupled, and they are not. Permuter rerunning from the 8 base.

func_8004BE88 -- MATCHED 2026-08-21. Kept for the one lever that is new and
  general:
  **A switch that retail compiles to a comparison tree and you compile to a
  jump table wants a CASE RANGE sharing the default label.** gcc 2.8 takes the
  table when `count >= 4` and `range <= 10 * count`; five cases over
  0x2F..0x59 is 42 against 50, so it took it. Retail's tree opens `bltz $a1` /
  `slti $a1,0x10`, which are the lower-bound tests for a case node at 0
  spanning to 0xF -- so the source is `case 0 ... 0xF:` immediately above
  `default:`, the switch's range is 89, and gcc builds the tree. Semantically
  a no-op; 127 differences to 86 and the dispatcher byte-for-byte right. The
  function is a MIDI meta-event dispatcher and 0x00..0x0F is the text-event
  range, so the original wrote it to document that.
  The rest was existing rules: the divide split into two statements against
  one name (86 -> 57, and it also stops gcc sinking the D_8009B458 load and
  store past the `divu`), the scalar arm of D_8009B458 plus `as -G0`
  (57 -> 48, the func_8002CEE8 delay-slot lever), two names for its two reads
  (48 -> 44), an explicit guard plus `do`/`while` with `off += 0x2C;` before
  `i++` (44 -> 6), and `off = 0; i = 0;` in that order (6 -> 2).
  The last 2 came from the permuter and decompose to one line: `z = 0;` at the
  top of the arm, with `off = z; i = z;`. Retail zeroes one register and
  copies it into the other, and a literal 0 in both places gives two
  independent zeroings whichever order they are written in. Twelve spellings
  had been measured against that residue first, so this is the shape to reach
  for when two initialisations to the same constant come out as two
  materialisations and the target has one.

func_8002DA1C -- 6 differences, first written 2026-08-21. 135 instructions,
  gp=9 at=1. A sibling of func_8002CEE8: the same D_8009B26C / D_8009B26E
  state pair, the same `x & 0xF` goto dispatcher, the same addressing recipe
  (`as -G1` with `D_8009B26C_IS_SCALAR` so the two one-byte flags stay
  gp-relative, plus `D_8009B0C0_SIZED` -- the existing `[2]` arm -- so its
  bare store goes through `$at`).
  Its own shape: the mode-0 arm can jump *backwards into the middle of the
  init block* (`beq $a2,2, .L8002DA64`), so the init block carries a label and
  the arm ends `goto init;`.
  Four levers, in order:
  * `r = (u8 *)D_800EB0F8;` as a base local (48 -> 13). Written inline, gcc
    folds the +0x34 into the `%lo` and then has to *subtract* it back to pass
    the base to func_80035B7C.
  * `f = func_8003F70C();` -- borrowing the local that held the D_8009B26E
    read, which is dead by then (13 -> 10). Retail copies the call result into
    `$a2`, which is exactly where `f` lives.
  * `f = D_801D0534 % 5;` -- the same borrow again, one arm over (10 -> 9).
    A fresh name is 9 as well and `v` is 12, so here the borrow and a fresh
    name tie and only `v` is wrong.
  * `t = D_80090B50;` as a base local assigned after the remainder (9 -> 6),
    which is what emits the table's address *before* the `sll` that scales the
    index. Writing the symbol inline, `(u8 *)`-casting it, or adding it as the
    last term are all 9.
  The residue is one instruction's position: retail materialises the `0` that
  is func_80035C38's first argument *after* the `multu` of the reciprocal
  divide, and we materialise it before the whole block. Eight shapes were
  measured -- a named `z = 0;` for the argument, a named read of D_801D0534 at
  two positions, the D_8009B26E store moved and wrapped in `do { } while (0);`,
  the base local moved -- and every one is 6, 13 or 23. `-fno-schedule-insns`
  is +1 and `-fno-schedule-insns2` is 35. Permuter running.

func_80071924 -- +12 instructions and 125 differences, first written
  2026-08-21. 144 instructions, gp=0 at=0, default flags. Parked on a single
  mechanism and recorded mainly for that.
  The whole gap is **gcc's loop-invariant motion hoisting three comparison
  constants that retail recomputes inside the loop**. The body tests a
  loop-invariant `b` against 1, 4, 5 and against the range [3,5]; retail
  materialises `addiu $v0,$zero,4` and `addiu $v0,$zero,5` twice each, in
  branch delay slots, and computes `addiu $v0,$s3,-3` / `sltiu $v0,$v0,3`
  inside the loop. We hoist 4 and 5 into `$a2` and `$fp` and the range check
  into a stack slot -- five preheader instructions -- and the two callee-saved
  registers they take then spill two other values, which is the other half of
  the +12.
  Nine source spellings and eight flag rows were measured against it and
  **every one is +12 or worse**: the `&&` chain as nested `if`s, the `||`
  chain as separate `if`s with a shared label, the range check as
  `(u32)(b-3) < 3` / `b >= 3 && b <= 5` / `b == 3 || b == 4 || b == 5` / a
  named intermediate, one name for the table base and the record cursor,
  `-fno-cse-follow-jumps`, `-fno-cse-skip-blocks`, `-fno-rerun-cse-after-loop`
  and `-fno-expensive-optimizations` (all four exactly +12/125, which is the
  usual tell that CSE is not the mechanism). `-fno-strength-reduce` is the
  only row that moves it, to +8, and a `switch (b) { case 1: case 4: case 5:
  }` for the first test is also +8 but builds a different comparison tree.
  What IS right and worth keeping: `sllv $v0,$s1,$s6` in the preheader is
  **gcc's own** constant-1 register for the `* 12` index scaling, not a source
  local -- writing `one = 1;` and using it for the three `== 1` comparisons
  scores identically to using literal 1s everywhere, so the `one` reading is
  unnecessary. The three-way tail (`a == 0` / `a == 1` / else compare the two
  fields) is instruction-for-instruction right, and so is the loop's
  `do`/`while` with the address-taken `sp14` re-read from the stack each
  iteration.

func_80048A28 -- 65 differences at the right length (121/121), first written
  2026-08-21. The permuter took it 103 -> 65 and it decomposes to ONE line:
  `w2 = (u16)(arg2 + 0x80);` hoisted to the top of the `if (t != 0)` block
  with the second range check written `if (w2 < 0x80)`. Naming the *first*
  range check the same way as well is -2 and much worse, so it is that one
  test and not the pair. Every instruction is in the right place; the whole residue is
  one register rotation across the function, which is why the count is so
  high against so little wrong.
  What is right: `D_8009B45C` as the **scalar** arm plus `as -G0` (gp=0, so
  any -G is free). Under the aggregate arm cc1psx emits a `%hi`/`%lo` pair,
  gcc hoists the `%hi` out of the loop into a callee-saved register and every
  one of the five reads becomes a `lw` off it; the bare form is one
  pseudo-instruction, nothing to hoist, and retail's `lui`/`lw` per use comes
  back. 111 differences to 104 and the length from +1 to 0. Same lever as
  func_8003CCD8 and func_8002CEE8, third user.
  Also right: `f = arg0 & 0x100; f = f != 0;` against one name for the
  `((arg0 & 0x100) != 0) << 6` index term -- written as one expression gcc
  emits a *branch* instead of retail's `sltu $zero`/`sll 6`. That is
  func_80048920's rule and this is its neighbour in the binary.
  Measured and NOT the axis: naming `i * 2` (-4), naming `0x80 - t` and
  `t + 0x80` (+3 each), `-fno-strength-reduce` (-3), `-fno-schedule-insns`
  (102 at the right length, i.e. no better), four index-ordering spellings,
  reusing `arg0` for the re-read halfword (which IS right -- retail keeps both
  in $s2 -- but worth nothing on its own), and `s8`/`u16` parameter types.
  `s32 arg0` is worth 1. Permuter running from this base.

func_8005D994 -- 5 differences at the right length (132/132), first written
  2026-08-21. gp=5 at=0, all one-byte flags, default flags, no addressing work.
  A sound-position setup: it builds two eight-byte packets on the stack, wraps
  two angles into (-0x800, 0x800] and hands both to func_8005F91C, which is
  the function this project matched earlier the same day -- so its two `u16
  a[4]` / `u16 b[4]` stack objects are the two lwl/lwr records that function
  copies, and declaring them as two separate arrays rather than one `u16 v[8]`
  is the honest reading (it scores the same, 17 either way).
  Three levers took it from 71 to 17:
  * **branch polarity, twice.** `m = -1; if (arg0 <= 0) m = 1;` where the
    obvious `if (arg0 > 0) m = 1; else m = -1;` inverts the branch; and
    `if (arg0 <= 0) x -= 0xC00; else x -= 0x400;` for the same reason. Retail
    falls through into the `-1` and the `-0xC00`, which is step 3's rule read
    off the delay slots.
  * `t = arg2 + 0x1000;` as its own statement before the `if`, then `t = t -
    x;` after it. Written as one expression `(arg2 + 0x1000) - x` gcc
    reassociates to `arg2 - (x - 0x1000)` and folds the constant into the
    other arm's `addiu`; the two statements against one name keep retail's
    `addiu $a2,$s5,0x1000` in the branch's delay slot. 70 differences to 17.
  A fourth lever came from the permuter and is the **opposite** of the one
  that matched func_80058434 the same hour: the first wrap wants its
  subtraction written INLINE, `b[1] = (t - x) - (t - x) / 0x1000 * 0x1000;`,
  not as `t = t - x;` first. 17 -> 11. So "name the value the division
  consumes" is not a rule about naming in general -- in func_80058434 the
  named value is the whole sum and the partial sum must not be named; here the
  whole difference is the *outer* subtraction, and naming it costs six.
  Read which register the target gives the intermediate before choosing.
  A fifth lever, again from the permuter and again one line: **name the
  read-back of the stack slot for the FIRST correction only.**
  `r = b[1];` after the store, with `if (*(s16 *)&b[1] >= 0x801) b[1] = r -
  0x1000;` and the second correction left reading `b[1]`, is 11 -> 5. That is
  what forces retail's store-then-reload-twice: the `lh` for the compare comes
  from `b[1]`, the `lhu` for the subtraction from `r`. A *fresh* name is 5 and
  the permuter's borrowed `q` is 7, so here the fresh name wins -- the
  opposite of func_8002E128, where the borrow was worth 7 over a fresh name.
  The permuter's best output wrapped the store in `if (arg0) { A } else { A }`
  and also scores 5; the named read is the plausible spelling of it.
  Historic residue description follows.
  The residue was two clusters. The larger: retail stores the first wrapped
  angle to the stack slot and then **reloads it twice** (`lh` for the compare,
  `lhu` for the subtraction), where we keep it in a register and store it in
  the branch's delay slot -- and the *second* angle, twenty instructions
  later, retail does keep in a register, so the two blocks genuinely differ in
  the source. Eight spellings were measured against it (an lvalue-cast store,
  a named intermediate with a fresh name and with `t`, a value cast instead of
  the lvalue cast, an `s16` array, an intervening statement, and splitting the
  divide from the multiply) and every one is 17 or worse. The smaller cluster
  is `$s4`/`$s5` exchanged between arg1 and arg2, both used once and
  adjacently. Full flag sweep is 17 at the default row. Permuter next.

func_80012E5C -- 132/132, 24 differences (31 by hand; 26 and then 24 off two
  separate permuter reads -- see the second one below)
  change was moving `i--` to after the call in the loop body). Scheduling and one load order, after
  a long chain of levers that each paid.
  Addressing: gp=12, at=1, and the whole function is decided by one assembler
  `-G4` plus three guard arms.  The gp side is D_8009B0B4 (4 bytes),
  D_8009B0A0/A8/AD/D0 (1), D_8009B098 (2), D_8009B0AC; the bare side is
  D_800FE048, D_800E9D90/D28, D_800A5768, D_8009B4A8 (unsized arrays,
  cc1psx's own pair), D_8009B142/143/144 (`_IS_AGGREGATE`), D_8009B141 and
  D_8009B318 (`_SIZED8`), and D_8009AFA2 (`_SIZED8`, the `lui $at` store).
  **The two `_SIZED8` arms are the load-bearing ones and they are the
  CSE lever, not an addressing lever.** Both flags are read twice with a
  call or a branch between the reads, and retail re-materialises the
  `%hi`/`%lo` pair each time.  Under `_IS_AGGREGATE` cc1psx emits its own
  pair, gcc CSEs the `%hi` half into a callee-saved register, and the
  function grows a *fifth* saved register ($s4) that retail does not have --
  which rotates every callee-saved allocation downstream and reads as a
  loop problem.  Under `_SIZED8` at `as -G4` the reference is a single bare
  symbol, there is nothing to CSE, and the pair comes back twice.
  D_8009B141 alone was -1/89 from +1/126; D_8009B318 alone was -1/119; the
  two together only work once the rest of the block is right (they were
  +1/126 early on and 132/132/41 at the end).  **A `_SIZED8` arm that scores
  worse today is worth re-trying after every other lever lands.**
  The rest, in the order they paid: batch-naming the four bytes that are both
  stored into D_800FE048 and passed to func_80085500 (135 -> 131, 129 -> 115),
  moving `func_800862C0(e)` to *after* the `D_8009B0B4 =` assignment so the
  masked selector stops being callee-saved (that is the $s0 the fifth saved
  register was stealing), `qb = D_800E9D90; q = qb + 0xC;` in two names to
  stop `+12` folding into the `%lo` (retail has `addiu %lo` then `addiu 12`),
  `bp = &D_8009B0A0;` assigned *after* the func_80085320 call rather than
  before it (35 -> 36 -> 31 with the next one), and `ab = D_800A5768;` as a
  base local, which is what puts the `& 0xFF` in retail's `$v1` instead of
  `$a1` (35 -> 31).
  Residue is three clusters, all scheduling: the ten loads in the first `if`
  come out in a different order (same count, same set of registers bar one);
  `n = 0x514C` is materialised at the top of the selector block where retail
  has it in the func_800862C0 delay slot; and the loop's rotated copy is the
  index computation where retail's is `addu $a0,$zero,$zero`.
  Measured and worthless: `z = 0;` as the loop body's first statement (the
  two zero arguments are already CSEd), `do { } while (0);` round the body
  head or the whole body, the index written last, five orderings of the
  first block's reads, `u8` for the four batch names (+4), a named mask,
  `(u8)v` for the mask, splitting the a5768 add in two so the base comes
  last (36), a base local for D_8009B4A8 as well (43), and four placements
  of `n = 0x514C`.  sweep_try confirms default compiler flags with `as -G4`
  (31; the next best row is `as -G2` at 95).

func_8005BFC8 -- 139/139, **17** differences (22 by hand; two separate
  permuter reads took it to 18 and then 17, both of them borrowings the hand
  sweep had no reason to try). The first, worth 4: `t2 = 0x1B000;` -- the
  0x1B000 constant held in the SECOND timestamp's name, which is dead by then.
  A fresh local for the same constant is worth NOTHING (22), and borrowing
  `t`, `cnt`, `lim` or `cap` instead is 26, 26, +1 and 24 -- so it is that one
  name's allocation and not the naming. The second, worth 1: a `u16 *` local
  for the address `sp14 + 0x12`, assigned before the `e = D_8009B498 +
  0x40000;` line, with both halfword reads going through it.
  The original 22-difference notes follow, and the first half is exact.
  gp=19, at=0, no addressing work at all: default flags.
  Decoded shapes worth keeping: `one = 1;` is a real local -- retail holds 1
  in $s3 across three calls for the three `sb $s3` stores of D_8009B063 and
  D_8009B064, and it is assigned in the `if (arg0 != 0)` test's own delay
  slot, so it is written before that test.  func_800902A0 and func_800784D0
  each *look* like they take one more argument than they do: $a2 holds
  D_8009B498 because the base was needed for the `addu` that builds $a1.
  `D_8009B066 = (D_8009B066 + 1) & 1;` must be written with **no local at
  all** -- the second read is what produces retail's `andi $v0,$v0,255`
  (the QImode-to-SImode zero-extend), and a `u8 m` local does not, because
  gcc knows `(x+1)&1` fits (that one line was -1 and 58 -> 41).
  `e = D_8009B498 + 0x40000;` twice, with the stores as plain `0x2424`,
  `0x2426`, `0x242C`, `0x242E` displacements, is retail's two
  materialisations; one `e` is +2 and worse.
  The last big step was two names in the final block: `f = D_8009B060;`
  read before the 0x2426 store, and `a = sp10;` hoisted as the call's first
  argument -- 41 -> 38 -> 22 and the length error from +3 to 0, because
  without them both loads sit in delay slots retail fills.
  Residue: $s0 and $s1 are exchanged (the retry counter and the second
  timestamp), and two load positions -- retail reads D_8009B498 *before* the
  D_8009B066 read-modify-write and reads sp14 before D_8009B498 in the final
  block.  Every attempt to name either read coalesces away.
  Wrong axes, all measured: three declaration orders, `cnt = 0` moved above
  the first func_80074170 call (30) or duplicated at the top, `do { } while
  (0);` round the initialisation (23) or round `cnt = 0`, a dead `t2 = 0;`
  first use, `cnt = t2;`, an explicit `goto reset` matching retail's branch
  structure, the `&&` spelling, and four spellings of the func_800902A0
  argument (an embedded assignment, a `bb` local at two positions, a
  pre-biased `bb`, and an `(s32)` cast sum) -- all 22 or worse.

func_80058624 -- 133/133, ONE difference, first written 2026-08-21.
  A panning/attenuation setter: two `func_80086E50` square roots over the pair
  of 12-byte vectors at D_800F56F0, a distance-derived volume clamped to
  [0x10, 0xFF], and a signed pan clamped to [-0x7F, 0x7F].
  gp=0, at=0, and sweep_try says every row from `-G0` to `as -G4` gives the
  same 1 -- so the addressing is not the axis and default flags are installed.
  Five things decided it, and four are recorded levers firing exactly as
  written:
  * **The two locals are one `u8 st[2]` whose address is taken.** Retail
    stores zero to both slots before the first call and then overwrites both
    a few instructions later; gcc deletes the first pair as dead unless the
    object is address-taken, and it also materialises `addiu $s4,$sp,0x11`
    into a *callee-saved* register. `s8 *pp = (s8 *)&st[1];` assigned **above
    the first call** gives both -- assigned later it folds into the store's
    own addressing and lands in $a2, which is -3 and a whole register class
    wrong. This is the address-taken-local rule (WORKFLOW step 4) used
    deliberately rather than diagnosed as a problem.
  * **The `- 0x2BC` on the first square root belongs after the SECOND call**
    -- func_8005A6A8's lever, second instance. Written next to the call gcc
    fuses it into `addiu $s0,$v0,-700`; moved across the second
    func_80086E50 the raw result has to survive in a callee-saved register
    and retail's `addu $s0,$v0,$zero` / `addiu $s0,$s0,-700` pair appears.
    That one move was -1/86 to 133/133 and 16.
  * **The quotient reuses the name of the distance** (`r`), whose live range
    ended at the `r > 0` test -- the mutually-exclusive borrow. A fresh `q`
    puts it in $a0 where retail has $s0. 16 -> 5 with the next one.
  * **`st[1]` is read `lb` at the calls and `lbu` would be wrong**: the array
    is `u8` because element 0 is, so the pan read needs the lvalue cast
    `*(s8 *)&st[1]`, not `(s8)st[1]`. 8 -> 3.
  * The clamp chain is **three assignments and two names, not one**:
    `w = r / 16; v = w; if (w < 0) v = -v;` … `w = v; if (r <= 0) w = -w;`
    -- the third assignment reusing `w` is what puts the second copy back in
    retail's $v0. One name throughout is 10; `w`/`v`/`u` as three names is 3.
  Residue: retail negates `$v1` in place (`subu $v1,$zero,$v1`) and we negate
  the copy's source (`subu $v1,$zero,$v0`). Same layout, same delay slot, same
  registers otherwise -- gcc has folded one copy through. Ten spellings
  measured, all exactly 1: testing `v` instead of `w`, `0 - v`, `v -= v + v`,
  an `if`/`else`, a ternary, `v = -w` explicitly, `do { } while (0);` round
  the pair and round the `if` alone, a redundant `v = v;`, and a trailing
  `w = v;`. Six borrowed names for `v` were also tried (`dx` 26, `dz` 20,
  `a` 3, `b` 3, `c` 16, `ex` 7) -- none is better than the plain name.
  This is a permuter case: one instruction, one operand.

# func_8004D914 MATCHED 2026-08-21, same day it was written. See the commit
# message; the last lever was `o = 0;` written as a statement before the
# function's first guard.

  **2026-08-28, still 1.**  The one difference is the abs expansion:
  retail's `subu $v1,$zero,$v1` negates the COPY and ours negates the
  original (`subu $v1,$zero,$v0`), because gcc propagates `w` back into
  `v = -v;` when it knows the two are equal.  Seven more shapes measured and
  every one is 1: `do { v = w; } while (0);`, the test written on `v` instead
  of `w`, a `do`/`while` round the negation, `v = 0 - v;`, both ternary
  spellings of the abs, and `v = w = r / 16;`.  411 permuter iterations from
  the current base found nothing either.  The mirror block twenty
  instructions later has the same shape and matches, and its test is on `r`
  rather than on the copy's source -- which is the difference, and no
  spelling reproduces it.
func_80038EB0 -- 164/164, **29** differences (147 when first written; three
  permuter reads and one hand lever took it to 37, 36, 34 and 29 over two
  days). First written 2026-08-21. 164
  instructions, a byte-stream command dispatcher: it reads two bytes from a
  cursor selected by `p[0x58]`, looks the command up in the three-entry
  D_800EB010 table (Rec4C, 0x4C bytes each) and either updates a slot or
  creates one. gp=4, at=0, one `%hi`, so no addressing work.
  **The count is inflated and says almost nothing** -- the FIRST difference is
  the prologue, because we save a sixth callee-saved register that retail does
  not, and every instruction after it is shifted by one. Read the first
  difference here, not the 147.
  What is right, and was worth finding:
  * **The two byte reads are `p[0x58]` read twice, not a cached cursor.** The
    `*c = e + 1` store may alias `p[0x58]`, so gcc reloads -- writing it twice
    is what the target does and it comes for free.
  * **The first byte needs two names.** Retail has `lbu $a1,0($v0)` … later
    `addu $s3,$a1,$zero` -- the caller-saved-to-callee-saved copy at the
    definition, func_800386B8's tell. `a = e[0]; … cmd = a;` produces it; a
    single name does not. Worth 15 differences. The *second* byte's copy comes
    for free and splitting it explicitly changes nothing.
  * **The second read must NOT have a `u8 **c` local.** With `c` the pair
    `lui`/`addiu %lo(D_800EB010)` is emitted after the address computation and
    the `lb`'s load-delay slot takes a `nop`; written inline
    (`e = *(u8 **)(p + p[0x58] * 4); … *(u8 **)(p + p[0x58] * 4) = e + 1;`)
    the scheduler straddles the pair across the `lb` exactly as retail does.
    That was +1 to 0 on the length -- and it is the whole reason the earlier
    134-at-+1 reading looked better than the correct 149-at-0. **Do not
    compare counts across length errors**; this function is the cleanest
    instance of that trap in the file.
  * The `>= 0x41` / `< 0x41` split, the `goto have;` join after the three-way
    table lookup, and `goto create;` out of the `fl & 0x20` arm are all
    retail's own layout.
  **The sixth saved register is gone, and the lever is worth knowing.** gcc
  was sharing `cmd >= 0x41` between the `create` block and the test AFTER
  `func_80039F44`, so the comparison result lived across the call and took a
  callee-saved register -- the sixth, which shifted every `sw`/`lw` in the
  prologue and inflated the count to 147. Retail recomputes `slti $v0,$s3,0x41`
  fresh after the call. **No spelling of the same comparison breaks that:**
  `cmd > 0x40`, `!(cmd < 0x41)` and `0x40 < cmd` are all canonicalised back and
  score identically. What breaks it is an ARITHMETIC on `cmd` before the
  compare -- `cmd + 1 >= 0x42`, `(cmd + 1) > 0x41` and `cmd - 1 >= 0x40` all
  give 147 -> 37, because the compared value is then a different expression and
  there is nothing to share. The permuter found `(cmd + 1) > 0x41`; all three
  spellings are equally contrived and equally correct, so the installed one is
  a judgement call and the MECHANISM is what to remember: when a comparison is
  shared across a call and the target recomputes it, add and subtract one.
  Splitting the second byte into two names is then worth one more (37 -> 36);
  on the 147 base it was worth nothing, which is the "arms that score worse in
  isolation" rule again.
  Two more after that, both small and both worth recording for their kind:
  * **`b = cmd + 1;` as a NAMED local, borrowing the second byte's dead
    name** -- 36 -> 34. A FRESH name for the same expression is worth nothing
    (36). Third instance in three days of the same shape: the permuter's win
    is a borrow, and the fresh-name control proves it is the allocation and
    not the naming (func_8005BFC8's `t2` and its `u16 *`, now this).
  * **`do { } while (0);` round the single `*(s16 *)(q + 0x40) = v;` store**
    -- 34 -> 29. gcc sinks that store below the two constant stores that
    follow it and retail emits it first; the loop node pins it. Round the
    whole `v = 0x178; if …; store` block scores the same, so it is the store
    that needs pinning.
  Residue at 29: register naming in the two byte-read blocks, one `addu`
  operand order on read 2 (base-first in retail, index-first in ours -- and
  read ONE comes out base-first from the same spelling, which is the puzzle),
  and the `create` block now sharing the `cmd + 1` form. Measured and
  worthless on the 29 base: `&p[...]`, the negation trick and a named scaled
  index for read 2's address; `cmd - 1`, `cmd + cmd` and `-cmd` for the
  post-call test (34, 34, 45); `cmd | 0` folds straight back to 147.
  The old residue, for the record: Measured and worthless: `cmd > 0x40` and
  `!(cmd < 0x41)` at either site or both, an `if`/`else` for `k` (148), five
  placements of `q = (u8 *)D_800EB010;`, three names for the reused `v`
  (147 -- the func_800154E4 rule fires but only just), and four compiler flag
  rows including `-fno-schedule-insns`. This is the register-allocation class:
  run the permuter on it.

func_800580D4 -- 123 target, 120 built (-3), 44 differences, written
  2026-08-21. A light/matrix packet builder: it picks a 0x50-byte record,
  builds two MATRIXes on the stack, copies one to the other, and fills the
  caller's out-parameter with three `0x1000 - x` angles.
  **The stack layout came out exact on the first draft**, which is the part
  worth keeping: gcc 2.8 allocates locals at increasing offsets in
  DECLARATION order starting after the 16-byte outgoing-args area, so writing
  them in the order the listing's `sp+NN` constants appear reproduces the
  frame with no iteration at all. Here that is pkt(0x10) sv60(0x60) m68(0x68)
  sv88(0x88) sv90(0x90) m98(0x98) svB8(0xB8), and every offset in the built
  code matched immediately.
  It also needed a new type. `include/types.h` now has `Matrix` -- 3x3 `s16`,
  two bytes of pad, three `s32` -- because the 32-byte by-value copy expands
  as eight aligned `lw`/`sw` only at alignment 4, and that expansion is the
  tell (WORKFLOW's Rec1C rule read forwards). The 8-byte copy at the end is
  `SVector` and expands `lwl`/`lwr`, which is alignment 2 and confirms the
  pair of types rather than one.
  What else is right: `y = sv60.vy;` named once and used for both the
  `pkt.m0.t[1]` store and func_8008E3D0's fourth argument -- retail reads that
  halfword ONCE and we read it twice, 93 differences to 50 on that line -- and
  writing D_800F2C40 inline rather than through a base local (50 to 44).
  Residue, and the two halves look coupled: retail carries arg2 in $s3,
  D_800F2C40 in $s2 and arg3 in $s1 -- which is exactly usage-frequency order,
  most-used in the lowest register -- and we have that rotation inverted. And
  retail RELOADS `*(s32 *)(D_800F2C40 + 0xD18)` three times where we load it
  once and keep it in $v1, which is the -3. The reloads are most likely
  register pressure rather than aliasing: get the rotation right and they may
  follow. Measured and worthless: `volatile` on any subset of the three reads
  (it moves in steps of TWO, a load plus a `nop`, and never lands on the +3
  needed), the byte-address cast, a local copy of arg2, a base local used only
  for the 0xD18 reads, and `*(s32 *)arg3 = 0;` hoisted to the top (-4).

func_800580D4 -- the blocker retraction that preceded the draft above: the standing notes said it "needs PsyQ MATRIX/SVECTOR types". It
  does not. The listing has **zero** GTE/COP2 instructions (`wc2|rtps|mfc2|
  mtc2|cop2|lwc2|swc2` all match nothing), gp=2, at=0, and all seven calls go
  above 0x80073840 into the PsyQ library, where an implicit declaration is
  enough. `SVector` already exists in include/types.h. The real difficulty is
  the stack frame: ~0xB8 bytes of locals that m2c renders with overlapping
  offsets (sp10/sp14/sp28..sp5C), i.e. arrays it could not size, plus a
  pointer to a stack local stored into the out-parameter at the end. It is the
  smallest open candidate (123) and it is open, not blocked.


func_8002BD0C -- 140 target, 140 built, 19 differences, written 2026-08-21,
  reworked 2026-08-28 from -1/113.  The seventh member of the D_8009B0F4
  dispatcher family and the first with a real jump table. gp=0, at=5,
  `as -G0`, and the default (volatile scalar) arm of D_8009B0F4 for the
  back-to-back read-modify-writes in arms 0 and 2.
  **The dispatch and the jump table came out exact on the first draft**, which
  is the hard part for this class: seven cases whose arms sit in memory in the
  order 0, 1, 2, 4, {3,5}, 6 -- read straight off `jtbl_80010208` in
  `asm/data/800.rodata.s`, since the table's `.word`s give the case-to-arm map
  and the arm addresses give the layout. Written in that order with
  `case 3: case 5:` sharing, plus two `goto` join labels (arm 0 jumps to the
  one arm 4 falls into; arm 1 jumps to the one arms 3/5 fall into), it
  reproduces retail's block layout exactly.
  Three levers closed the -1 and took 113 to 51, and **all three were already
  in the file as measured negatives**, because each is worth nothing until the
  other two are in:
  - **arm 2's `d` had to be a second name.**  Arm 2 and the join1 arm both end
    `sw 8; addiu 0x800; sw 0xC`, so with one name they get one pseudo, one
    register, and the final jump pass cross-jumps arm 2's tail into join1 --
    two instructions short.  Retail keeps arm 2's pointer in `$v1` and
    join1's in `$v0`, and that register difference is the ONLY thing blocking
    the merge.  A second name is +2 and restores the length.
  - **`do { n = 0x20000; } while (0);` in arm 0.**  Without it gcc fills arm
    0's `j` delay slot by duplicating join1's `addiu $v0,0x800` and hoists the
    `lui` for 0x20000 up into the middle of the arm; retail has the `lui` in
    the slot.  -1 to 0, and 117 to 66.
  - **a named read of D_8009B0F4 above the `+6` store in arm 0** (66 -> 56)
    **and a SECOND, differently named one in arm 2** (56 -> 51).  The old
    entry recorded the arm-0 read alone as "+6 worse", which it is; it only
    pays once the length is right.  One name shared by both arms is 56, so
    this is the func_8003B808 rule ("do not share the name across two switch
    arms") a second time.
  Then the one-name-per-arm lever from the sibling func_80057544 took 51 to
  32, in three steps and with one surprise in each:
  - **arm 4's `0x100` is not arm 2's.**  Both arms build the same four-
    halfword packet, so one `hun` reads as obvious; retail holds arm 2's in
    `$s0` (it survives the call and is stored again afterwards) and arm 4's in
    `$v1` (dead at the call).  One name forces the callee-saved register on
    both.  51 -> 41, and giving arm 4 its own `g` as well is worth nothing.
  - **`n` wants one name per JOIN GROUP, not per arm.**  Arms 0 and 4 reach
    join1 and arms 1, 3, 5 reach join2, so the value really does cross arms --
    `n2` for the join2 group is 41 -> 35 and a third name for arm 2's own
    `n` is 41 -> 38, but the two together are 38, not 32.  They interfere.
  - **`d` wants splitting at the same boundary** (a third name for the join2
    group): 35 -> 32 with the join-group split, and 32 either way, so the
    installed candidate carries the two-name `n` and the two-name `d` and
    leaves arm 2 alone.
  What is left is 32 register differences with the instructions
  instruction-for-instruction right: retail holds the mask `m` in `$a0` and
  `n` in `$v1`, and we have `n` in `$a0` pushing `m` to `$a1`.  All six
  declaration orders of m/n/d score identically, which is the usual tell that
  the axis is elsewhere.  745 permuter iterations from the 32 base found
  nothing better than 32.  From the earlier 51 base it reached 47 by writing
  join2's two stores as `d = (*(s32 *)(p + 8) = D_8009B118);`, which emits
  them in the opposite order to retail's; not installed.

  **2026-08-28 evening, 32 -> 19**, on three edits from the two siblings that
  matched the same hour:
  - **one name per distinct mask VALUE** (the sibling rule): 32 -> 27.
  - **arm 4's two constants are ONE name.** Retail holds 0x26810 in `$v1`,
    stores it to `p+0x30`, then puts 0xA000 in the same `$v1`.  Written as a
    literal store plus a separate `n = 0xA000;` the two get different
    registers; `n = 0x26810; *(s32 *)(p + 0x30) = n; n = 0xA000;` is 27 -> 24.
    This is func_80020BE4's one-name-for-two-sequential-constants rule, third
    instance.
  - **arm 2's `n` is not the join groups' `n`.** It is the only 0x1C value
    that reaches no join label, and a third name for it is 24 -> 19.  Measured
    as +3 (worse) earlier in the same function's life, which is the coupled-
    levers trap again.
  The residue is 19, all of it `m` in `$a1` where retail has `$a0` because `n`
  took `$a0` first, plus arm 2's pointer/constant pair exchanged.  The
  permuter reaches 14 by wrapping join1's two stores in `if (d2) { A } else
  { A }` on an UNINITIALISED `d2` -- the func_8005F3B8 class -- and none of
  the three plausible `do { } while (0);` shapes with the same effect
  reproduces it (two of them are -3, because the do/while lets gcc cross-jump
  join1's tail into arm 2's).

func_80020988 -- 151 target, 150 built (-1), 72 differences, written
  2026-08-28. A menu appear/animate/dismiss handler returning a status code
  (0, -1, 1, 2). Third out of the dup-%hi pool. **The whole first half -- the
  block that creates the two widgets, 75 instructions with two calls each
  taking eight arguments -- is exact on the first draft**, so what follows is
  a small residue in a small tail.
  Addressing: the gp side is D_8009B152 (1 byte) and D_8009B188/D_8009B18C/
  D_8009B1CC (4 bytes each), and the two bare-wanting symbols are D_8009B394
  and D_8009B398, both `u16`. The bare ones are NARROWER than the widest gp
  one, so no threshold exists and this is WORKFLOW's third dup-%hi branch:
  inflate. D_8009B398_SIZED_VOLATILE already existed for exactly this; a
  matching D_8009B394_SIZED_VOLATILE arm was added, and the unit assembles at
  `as -G4`. Both need the volatile as well -- D_8009B394 is read twice in one
  block and D_8009B398 twice across calls, and retail reloads every time.
  Four globals had no declaration at all and are new in variables.h.
  What is measured so far: `e` borrowing `q`'s name is worth 2 (74 -> 72), the
  two being in mutually exclusive branches and retail carrying both in $s3.
  Residue, and the -1 is part of it: retail leaves the `(D_8009B398 & 0xE0)`
  test as `beq` falling into the body with the zero return value in its delay
  slot, and we emit the inverted `bne` plus a `j`, which is +2 there and means
  something else is -3. Three tail shapes measured (the body inside the `if`,
  a `goto zero;` label, the plain early return) and all are -1 at 72/73.
  **The -1 is now located exactly**: retail's `addu $v0,$v1,$zero` copy of the
  `e[0x21] + e[0x60]` sum, and the tail is otherwise instruction-for-
  instruction. But it is NOT the two-names tell, and this is the first time
  that reading has failed. Retail masks the copy 0x7F for the branch and the
  original 0xFF **in that branch's delay slot**; we compute the 0x7F mask,
  branch with the STORE in the delay slot, and mask 0xFF after -- one
  instruction fewer because one register serves both masks. So the real
  question is what fills the delay slot, not how many names the sum has.
  Measured and worthless, all at -1: a second name `w` for the sum at five
  declared types (s32/s16/u16/u8/s8), the same copy borrowed into `one`, `b`
  and `c` (92/95/98 -- much worse), `u8` for the sum itself,
  `do { } while (0);` round the store and round the sum-plus-store, and the
  store moved above the `|= 4`.
  Retail also reads the flags halfword at `+8` BEFORE the two bytes it sums,
  where we read the bytes first.
  The permuter's only exact-length output is semantically wrong -- it holds
  the pre-write flags value in a `volatile` local and reuses it for the
  `& 0xFFFB`, where retail re-reads after the write. Its correct length is an
  accident of the volatile's own store-and-reload.


func_8004803C -- 157 target, 157 built, 140 differences, written 2026-08-28
  (-4/151 on the first draft the same day).  Fourth out of the dup-%hi pool.
  A sound-voice setup: it looks a 16-bit id up in one table, derives two
  volumes, applies an optional pan, and writes eleven fields of the
  D_8009B45C block before two library calls.
  gp=0, at=0, `as -G0`, and D_8009B45C is re-materialised in nearly every
  block -- gcc has no global CSE, so writing the symbol inline is right and a
  base local would be wrong everywhere except the first block, which does
  share one load across several accesses.
  Two decode notes worth keeping: the `sra $v0,$t3,1` has no bias, so it is a
  SHIFT and not a divide (the `/ 128` further down does have the `bgez`/
  `addiu 0x7F` bias and is a real divide); and `sllv $v0,$s1,$v0` with `$s1`
  holding 1 across two shifts is the `one = 1;` named-constant tell, the same
  shape as `and` versus `andi` for a mask.
  arg5 and arg6 are `u8` on the callee's own evidence -- their stack slots are
  read with `lbu`.
  **The -4 was three things and none of them was the `andi` lead the first
  entry named.**  Retail's `andi $a1,$a3,0xFF` is reproduced by the obvious
  `v = arg3 & 0xFF;` as soon as `v` stops being reused for the pan block's
  products -- that reuse was the whole of it, and the "we multiply $a3 raw"
  reading was wrong.  The other two: every index sum is written **index
  first** (`n + *(s32 *)(b + 0x444)`, not the reverse), and `idx` is a
  **`s16`**, not an `s32` -- with `s32` gcc knows the `lhu` already bounds the
  value and folds the second `& 0xFFFF` away, so the `andi` retail re-emits
  at the tail block has nowhere to come from.  Declared `s16`, gcc keeps the
  raw halfword and re-masks.  157/157 with that.
  What is left is 140 differences of pure allocation: retail has arg0/arg1/
  arg5/arg6 in $s3/$s0/$s5/$s4, the id in $s2 and D_8009B45C in $a2 (with
  `addu $t2,$a2,$zero` saving arg2 out of the way), and we hold D_8009B45C in
  $t0 and need no such copy.  The count is inflated: the diff is positional
  and the prologue's store order differs, so almost every later line is
  marked.
  Measured and worse: splitting the base local per block (-1/153, and the
  three-name split is what *causes* the duplicate `lhu` to disappear, so the
  two interact), splitting `n` into two names (-1/157), naming arg2 (141), and
  a `w = arg4;` copy for the second pan test (worth exactly 1).
  1431 permuter iterations from the 140 base found nothing better than 139.


func_80057544 -- 155 target, 155 built, 26 differences, written 2026-08-28.
  The EIGHTH member of the D_8009B0F4 dispatcher family and a near-clone of
  func_8002BD0C: same `$s2` parameter block, same jump table shape, same
  `and`/`or` pairs on D_8009B0F4, six cases instead of seven and every arm
  ending in its own `j` rather than sharing a join.  gp=0, at=7, `as -G0`,
  default declarations.  D_80010008, D_801DD800 and D_800F5694 are new
  symbols; D_80010008 is the word 0x80180000, which is the overlay address
  docs/DISC.txt is about.
  **The lever that matters here is one name per arm for the D_8009B0F4
  read, and it is worth 53 -> 29.**  A single `t` used in five arms is one
  pseudo with a live range spanning the whole switch, so the global allocator
  gives it `$a0` and pushes the mask `m` -- which really IS one global pseudo,
  `$a0` in every arm of retail -- up to `$a1`, and every arm's registers
  rotate behind it.  Five names are five short live ranges, each local to its
  block, each landing in retail's `$v0`.  The mirror was measured the same
  minute and is what makes the claim sharp: splitting `d` per arm is 29 -> 34
  and splitting `m` per arm is 29 -> 42, because retail really does carry
  those in one register across the whole function.  So the question is never
  "one name or many" -- it is which values the target keeps in one register
  across the arms, and the answer here differs per variable within one
  function.
  Two more that were needed to get to exact length:
  - arm 4 re-reads D_8009B0F4 for the `|` after storing the `&` result, and
    retail hoists that reload ABOVE the `p+0x1C` store to fill its own delay
    slot.  A named read (`t4 = D_8009B0F4;` written before the constant) is
    +1 to 0 and 68 -> 54.
  - `do { m = 0xFFDDFFFF; } while (0);` in arm 4, which stops the scheduler
    splitting the `lui`/`ori` pair (54 -> 53).
  Measured and worthless: naming the `p+0x1C` constants (shared is -3, because
  arms 2 and 3 then cross-jump; per-arm is also -3), a `do`/`while` round arm
  5's single store, and hoisting arm 3's `&a` into a pointer local (+4).
  The residue is 29 differences, all of them `$v0`/`$v1` exchanged around the
  named read and the constant beside it -- the same residue func_8002BD0C
  has, in the same family, which by the two-independent-functions rule means
  it is systematic and not local pressure.

  **2026-08-28 evening, 29 -> 26 on one declaration.**  Arm 5's only statement
  stores 1 into D_800F5694, and retail does it as `lui $v1,%hi` /
  `sb $v0,%lo(D_800F5694)($v1)` -- through an ORDINARY register, which under
  this function's `as -G0` is the unsized-array form and not the scalar one
  (a scalar at -G0 stores through `$at`).  `extern u8 D_800F5694[];` and
  `D_800F5694[0] = 1;`.  The tell is the register in the store's address:
  `$at` means scalar, anything else means cc1psx's own pair.
  Also tried and worth nothing: the chained-assignment spelling of the two
  D_8009B118 stores that closed func_8002FB78, and one name per distinct mask
  value (29 either way).


func_80046294 -- 151/151, 5 differences, written 2026-08-28.  A queue
  compaction pass over the D_8009B45C block: it walks 0x30-byte records from
  +0x80, removes the ones whose command byte is in a set, decrements the count
  at +0x4C, and then does a small amount of flag work on +0x7C and +0x40.
  gp=0, at=0, `as -G0` -- retail reads D_8009B45C through a `%hi`/`%lo` pair
  at every one of its eleven sites and the default -G8 makes it gp-relative,
  which is -10 instructions on its own and was the whole of the first draft's
  length error.
  **Both jump tables were recovered from `asm/data/800.rodata.s` before a line
  of C was written**, which is the cheap part of this class:
  jtbl_800106C0 is base 0x20 with 41 entries -- index 0 to the `case 0x20`
  arm, 34/35/37/38/40 (0x42 0x43 0x45 0x46 0x48) to the copy arm, the rest to
  default -- and jtbl_80010768 is base 0x42 with 7.  Six written cases over a
  range of 41 clears gcc's `count >= 4 && range <= 10 * count`, so the first
  switch really is a table and not a tree.
  Three levers, in the order they paid:
  - **the loop is `while (1)` with an `if (…) goto after;` head test and two
    `break`s at the bottom.**  A `do { … } while (i < n && i >= 0);` with the
    same head test inside is -10 and then +2: gcc rotates the head test to the
    bottom and merges it with the loop-back, so the head's `lh`/`beq` pair
    disappears.  Writing the head test with a `goto` label instead keeps it
    but loses the loop entirely -- the jump-table base stops being hoisted
    into the preheader (+1).  `while (1)` keeps both: 151/151 and 13.
  - **`(bp + off)[0x80]` rather than `bp[off + 0x80]`** for the command byte,
    which is the documented `addu base,index` versus `addu index,base` (13 to
    12).
  - **`do { i = 0; } while (0);` written ABOVE the entry guard** -- 12 to 6 for
    the pin (it moves `i` from `$t0` to retail's `$a3` and `boff` the other
    way) and 6 to 5 for hoisting it above the `if`.  All six declaration
    orders of i/off/boff score identically, which is the usual tell that the
    axis is the assignment and not the declaration.
  What is left is 4 instructions of preheader order -- retail emits the
  hoisted jump-table base BEFORE `boff = 0x30; off = i;` and gcc emits it
  after -- plus one `%lo(.rodata+168)` against `%lo(.rodata)`, which is the
  SECOND jump table's offset inside splat's single rodata blob and may well
  be a try_func artefact: only the full build can prove a jump table landed.
  Measured and worthless on the preheader: a `do`/`while` round both
  statements (12), round `boff` alone (12), and swapping their order (5, no
  change).


func_80044608 -- 140/140, 38 differences, written 2026-08-29.  A small
  state machine: two nested comparison trees on D_8009B43D and D_8009B450,
  each arm ticking a byte counter and calling one of three `func_8008B3xx`
  routines through func_80043D48 before returning -1.
  gp=35, at=0 -- **default flags and no addressing work**, which is what the
  step-0 survey is for.
  Three readings, and two of them are about which copy of a shared tail is
  canonical:
  - **`#define D_8009B450_IS_SCALAR`.**  The default unsized-array
    declaration is non-small, so every reference is a `%hi`/`%lo` pair where
    retail has one `%gp_rel` load.  This is the FIRST time this session that
    the addressing went the other way -- the last two functions needed the
    symbol pushed OUT of small data and this one needs it in.  With the empty
    `case 2:` below, +6 to +3 and 114 to 74.
  - **`case 2:` written with an empty body.**  Retail's inner tree tests
    0, 1, 2 and 3 with 2 falling to the same place as `default`, so the case
    is spelled out in the source -- the documented "the case range follows
    what is written, not what is reachable".
  - **`sll 24` rather than `andi 255` needs the decrement NAMED**: `c =
    D_8009B43C - 1; D_8009B43C = c; if ((s8)c != 0)`.  A `(s8)` cast on the
    global's read-back still gives `andi 0xFF`, because the read-back is a
    `u8` load; only a local carrying the SImode value gets the shift.
  - **A `goto` into another arm's block is what puts the canonical copy where
    retail has it.**  Case 2's tail and case 1's zero-path tail are the same
    three statements; written out in both, gcc keeps case 2's copy (the
    later) and jumps to it from case 1, where retail keeps case 1's and jumps
    forward from case 2.  Labelling case 1's block and writing `goto
    shared340;` in case 2 is +3/74 to 140/140 and 38.
  What is left is 38, and it is the SAME question one level up: arm 3 of the
  inner tree and case 1's `== 2` path share the D_800F2AF0 tail, gcc keeps
  case 1's and retail keeps arm 3's.  The mirror fix does NOT work there --
  labelling arm 3 and writing `goto shared3A0;` in case 1 is -1 and 64,
  because retail reaches that copy with a `bgtz` that duplicates the `lui`
  into its delay slot and an explicit `goto` cannot.  Also measured: case 1's
  zero path written as an out-of-line `goto` arm at the end of the case
  (-1/65).

  **Permuter, 1229 iterations: 38 -> 33, and the shape is the implausible
  one.**  It wrapped one arm's decrement in `if (D_8009AF7C) { A } else
  { A }` -- both arms identical, so it is a legal no-op, the func_8005F3B8
  class.  The plausible `do { } while (0);` at either decrement site is 38,
  and the other half of its output (a named local for the `D_800F2888`
  argument, which is also what `D_8009B444` is assigned) is worth nothing at
  either placement.  Not installed.

func_80037DA4 -- 160/160, 61 differences, written 2026-08-29.  A sequence
  opcode reader: it pulls a byte off a per-depth cursor at `p + p[0x58] * 4`,
  dispatches on its low nibble and three flag bits, decodes a field out of
  `D_801D4244[D_8009B338 - 1]`, and either pushes a new cursor from one of
  three offset tables or calls func_80036C14.
  gp=6, at=0 -- **default flags**; the two gp-relative symbols are one byte
  and one halfword and the three tables are all far larger than eight bytes,
  so nothing needs a threshold.  m2c drafted the whole control flow correctly
  including the `goto` into the push block.
  One lever took it from -2 to exact length, and it is the fold rule twice in
  one function: **`+ 0x17` must be its own statement against the same name.**
  `a1 = ((t >> 22) & 0xF) + 0x17;` written whole puts the `addiu` before the
  range test where retail has it in the test's delay slot; and in the arm
  below, `a1 = ((t >> 18) & 0xF) + 0x17; if (a1 == 0x17)` lets gcc prove the
  comparison is `(t >> 18) & 0xF == 0` and fold the constant away entirely --
  retail materialises 0x17 twice, once as the addend and once as the
  comparison operand.  Split against one name both times, -2/103 to 160/160
  and 75.
  What is left is a whole-function register rotation: retail holds the opcode
  byte in `$a0` and the flag in `$a2`, we have `$a2` and `$a3`, and the two
  cursor registers are exchanged with it.  Measured and worthless: declaring
  the opcode first, moving `a2 = 0;` before the cursor store, and a
  `do { a2 = 0; } while (0);` pin.  Also measured and WORSE: the `+ 0x17`
  moved above the range test (-1), and a named `m = 0xFFFF0000;` for the mask
  retail materialises in both arms of the table lookup (+1).
  **Permuter, two runs: 75 -> 61 on a `do { } while (0);` round the labelled
  `lookup:` statement** -- a label inside a do/while reached by a `goto` from
  outside it, which is legal C and reads as source, so it is installed.  A
  second run from the 61 base, 1544 iterations, found nothing better.
  Still open after that: the prologue's two cursor registers are exchanged
  ($v0/$v1), and case 1's `+ 0x17` sits in the `j`'s delay slot where retail
  has it in the `beq`'s.  Measured and worthless there: swapping the `cp`/`c`
  declaration order, and a `do`/`while` round either the `if` body or the
  whole `if` (both -1).


func_8002BAB4 -- 150/150, 25 differences, written 2026-08-29
  2026-08-29.  A four-way dispatcher on `D_800EA1E8[0] & 0xF`; three arms are
  a single call each and the fourth steers a camera value at
  `D_800F2848 + 0x20` toward the overlay's D_80181002/D_80181012, ticks a
  counter, and copies eight words out of D_800F2848 into the buffer
  func_800591FC returns.
  gp=0, at=0, `as -G0` -- **the two overlay halfwords are the reason**: they
  are `s16` scalars defined by `config/undefined_syms_auto.txt` and at the
  default -G8 they come out gp-relative where retail reaches them `%hi`/`%lo`.
  New: `Words8` in types.h for the 32-byte copy, and `extern s16 D_80181002;`
  / `D_80181012` in variables.h -- the first declarations this project has
  taken in the overlay address range, and they are ordinary externs because
  the linker defines them as absolute addresses.
  The residue is +1 and it is a single `nop`: retail materialises the `lui`
  half of the `/ 24` reciprocal constant BEFORE the two `lh` loads and fills
  the second load's delay slot with the `ori` half, where we emit the whole
  pair after the subtraction and get a `nop`.  Measured and worthless: a
  named local for the first halfword read, and splitting `v / 24 + 1` into
  two statements (+2).
  The other visible difference is the prologue: retail reads
  `D_800EA1E8[0]` before the frame setup and we read it after.

  **2026-08-29, +1/106 -> 150/150 and 25**, on three edits:
  - **the abs written as a TERNARY inside the division**: `v = (d < 0 ? -d :
    d) / 24 + 1;` rather than `v = d; if (d < 0) v = -v; v = v / 24 + 1;`.
    That is the whole length error -- with the `if` spelling gcc emits the
    reciprocal constant's `lui`/`ori` pair after the subtraction and leaves a
    `nop` in the second `lh`'s delay slot; with the ternary it straddles the
    two loads the way retail does.  Also measured: two named halfword reads
    (+3) and `v = -d;` in the `if` (+1, no change).
  - **`one = 1;` assigned before the switch is WRONG here**, which is the
    opposite of func_8002F4C0 where the same shape was the lever.  Retail
    does hold 1 in `$s3` across the whole function, but it gets there from
    the switch tree's own comparison; writing the local costs 4.  The
    permuter found it.
  - the declaration order of `p` and `one` (29 against 31), now moot.
  What is left is 25: the `D_80181002` read lands in `$a0` where retail has
  `$v1`, the counter decrement in `$v1` against `$v0`, and retail puts
  `d = 2;` in the func_8008E590 call's delay slot where we put the argument.
  Measured and worthless: naming the call's result, and both orders of the
  `p`/`one` assignments.


func_80027508 -- 161 target, 162 built (+1), 25 differences, written
  2026-08-29.  A duel-AI move chooser: four guarded calls, each of the form
  `if ((rand() & K) == 0) { if (f() == 0) return 0; }`, then it picks a card
  record out of D_801A7AD8, reads a field out of D_801D4244 and fills the
  command block at D_800EAE88 one way or the other.
  gp=1, at=0 -- default flags.  `func_8008E590` is `rand()` and takes no
  arguments; m2c renders some of its call sites with four, which is register
  noise, and the listing settles it.
  **The one lever so far: the `% 5 + 0xB` must be written INLINE in the byte
  store, not through an `s32` local.**  With `c = *(s8 *)(e + 0x18) % 5 +
  0xB; D_800EAE88[0] = c;` gcc computes the modulo in QImode -- because the
  local's only use is a byte store -- and re-narrows with a second
  `sll 24`/`sra 24` pair after the `subu`, twice.  Written inline it stays
  SImode and matches retail.  +5/115 to +1/92.
  The residue is +1 and it is `t[idx - 1]`: gcc folds the `- 1` into the
  load's displacement (`lw -4`) where retail computes `addiu -1` then
  `sll 2`.  Five spellings measured -- a named index (+8), an `(s32)` cast on
  the halfword, the pointer form `(t + i)[0]`, the explicit
  `*(s32 *)((s32)t + i * 4)`, and dropping the base local for the symbol
  (+3) -- and none of them stops the fold.
  Also open: the `lbu 0x18` read is emitted after the `[6]` store where
  retail has it before the `[1]` store, which is the func_80026DC8
  interleave shape; the obvious named read is +2 from here.

  **2026-08-29, +1/92 -> +1/25, and the interesting part is that a 161/161
  state on the way was a FALSE ZERO.**  Two faults were cancelling in the
  length: the `t[idx - 1]` fold is -2 (retail computes `addiu -1` then
  `sll 2` at both sites, gcc folds the -1 into the load's displacement) and
  the second block's un-interleaved `% 5` chains are +2.  A permuter output
  reached 161/161 and 95 with both still wrong, and by the standing
  `(abs(length_error), differences)` order that beats the corrected +1/25 --
  which is why the order needs the caveat: **it assumes the length error is
  a single fault.  When you can SHOW it is two faults cancelling, the
  corrected shape at +-1 is the better base**, and here it is 95 differences
  against 25.
  The two fixes:
  - `i = *(s16 *)(e + 0xC); i = i - 1;` against one name, then `t[i]`.  That
    is the one-name-across-two-statements rule blocking a fold, and it is the
    only one of eight spellings that does: a named index in one statement, an
    `(s32)` cast, `(t + i)[0]`, `*(s32 *)((s32)t + i * 4)`, the `<< 2` form, a
    `s16` index local, and dropping the base local for the symbol are all
    still folded.
  - `c = *(s8 *)(e + 0x18);` at the TOP of the second block, with the modulo
    still inline in the store (`D_800EAE88[0] = c % 5 + 0xB;`) so it stays
    SImode.  Naming the read is the func_80026DC8 interleave lever; naming
    the whole modulo would bring the QImode narrowing back.
  The residue is +1 and 25: the first block wants the same interleave and
  every position for it there is worse (44), and the `set8:` block
  re-materialises `%hi(D_800EAE88)` where retail still has the base in a
  register -- a base local live across both blocks is +2 and 100.


func_80045C98 -- 169/169, 9 differences, written
  2026-08-29.  A pan/zoom updater on the D_8009B45C block: three near-
  identical clamp steps (a signed delta at +0x1588/+0x1582/+0x512 added to a
  position at +0x1586/+0x1580/+0x510 and clamped against a byte limit at
  +0x158A/+0x1584/+0x49), then two chained `(x * (pos + 1) & 0xFFFF) >> 8`
  scalings feeding func_80049F10, and func_80044DC0 at the end.
  gp=0, at=0, `as -G0` -- **thirteen `%hi`/`%lo` reads of D_8009B45C**, which
  the default -G8 turns into single gp-relative loads.  That alone is -13 to
  -4 and 159 differences to 64, and it is the same first move as
  func_80046294, func_800164FC and func_8002BAB4.
  Each clamp step reloads the symbol twice, which the inline spelling gives
  for free: gcc has no global CSE and a store through the pointer kills the
  cached load.
  The -4 is all in the SECOND scaling: retail keeps the `if`/`else` as two
  arms with a `j` to a join and then sign-extends the joined value with
  `sll 16`/`sra 16` before the call, and gcc collapses our `if`/`else` into
  an unconditional `b = 0;` plus a conditional overwrite -- the func_80026BA4
  shape -- and then folds the `(s16)` away because after `andi 0xFFFF` and
  `srl 8` it can prove the value is 0..255.  The FIRST scaling has the same
  shape and comes out exactly right, so the difference is the cast at the
  call.  Measured and worthless: a named `s16` local for the call argument,
  a `do { } while (0);` round the `else`, and writing it as
  `b = 0; if (a) b = …;`.
  `func_80049F10` has no prototype in functions.h and `src/func_80049F10.c`
  matches with `(s32, s32)`, so the sign-extension is the source's own cast
  rather than a conversion.

  **2026-08-29, -4 to -1 on one declaration: `s16 b`, not `s32 b` with an
  `(s16)` cast at the call.**  The cast folds -- after `andi 0xFFFF` and
  `srl 8` gcc can prove the value is 0..255 -- and the narrow declaration
  makes the truncation part of the assignment instead, which restores three
  of the four missing instructions (the `j` to the join, the `else` arm's
  `addu`, and one of the shifts).  Measured and worse: `u16 b` with the cast
  (-1, same), an `s16`-parameter prototype for func_80049F10 (-4), and
  `(s16)(b & 0xFFFF)` at the call (-4).
  The last -1 is the sign-extension itself: retail does `sll $a0,$v0,16` /
  `sra $a0,$a0,16` at the join and we copy the already-narrow `b` into `$a0`
  with one `addu`.  So retail's `b` is an SImode value extended AT the call
  and ours is a 16-bit value extended at the assignment; no spelling found so
  far gives the first without folding the extension away.

  **2026-08-29, -1/63 -> 169/169 and 9, from the permuter: the second clamp's
  upper test assigns its LOAD into `b` inside the condition.**
  `if ((b = *(s16 *)(D_8009B45C + 0x1580)) >= D_8009B45C[0x1584])` -- `b` is
  the call argument three blocks later and is dead here, so this is the
  assign-to-a-not-yet-live-local hint (func_8002A4A8's class), but spelled as
  an ordinary C idiom rather than a bare no-op.  It is the whole -1 AND
  fifty-four differences.
  **The exact spelling matters**: `b = (x >= y);` and
  `if ((b = x >= y) != 0)` -- assigning the COMPARISON rather than the load --
  are both 45.  The permuter's parenthesisation is the thing to read, and a
  paren-stripping normaliser hides which of the two it found.
  What is left is 9 register differences in the two scalings: retail carries
  the masked value in `$a0` and the multiply result in `$v0`, we have `$v1`
  and `$a2`.  Declaration order (three permutations) and a redundant re-read
  of `D_8009B45C + 0x44` inside the test are all 9.

  **2026-08-29, 9 -> 4, three edits, two of them from the permuter.**
  `u16 a`, not `s32 a`, for the first scaling's result -- the value only ever
  feeds a mask, a test and a multiply, and the narrow declaration puts it in
  the register retail uses (9 -> 7).  It also makes the `a = a & 0xFFFF;`
  line that followed it dead, and dropping that line changes nothing, so it
  is gone.  Then the two shifts want to be their own statements against the
  same name -- `a = (u32)((h * (…)) & 0xFFFF); a = a >> 8;` is 7 -> 5, and
  the second scaling's `+ 1` wants the name `h` borrowed from the first
  scaling's dead load, `h = *(s16 *)(D_8009B45C + 0x1586) + 1; b = (u32)((a *
  h) & 0xFFFF) >> 8;`, which is 5 -> 4 on top.  Each alone is 5 and 6, so
  they compose.
  Measured and worse at the second scaling: carrying `h` through the mask as
  well (7), through the mask into `b` (5), one name `b` for all three steps
  (+2 and 59), and splitting the `+ 1` into its own statement (8).  `u16 b`
  is -1 and 63, and `u16 h` and `s16 h` are 7 and 8.
  The last 4 are one register pair in the second scaling only: retail has
  `addiu $v0,$v0,1` / `mult $a0,$v0` / `mflo $v0` / `andi $v0,$v0,0xFFFF`,
  one register throughout, and we get `$v1` for the `+ 1` and `$a2` for the
  `mflo`.  The first scaling has the identical shape and matches.
  Also measured and worth nothing: the assign-inside-the-condition idiom at
  the OTHER two upper clamp tests (+0x1586/+0x158A and +0x510/+0x49) -- both
  are 9 against a 9 base, i.e. no change at all.  The lever is specific to
  the one site whose loaded value is the call argument.

# ---
func_80023D08 -- 173/173, 69 differences, written
  2026-08-29.  A card-cursor mover: `arg1` is a direction bitmask (bit 0
  picks the column axis, bit 1 the negative direction), the record at
  `arg0 + 4` is the cursor sprite, and each arm sets up a slide by writing a
  target coordinate and a per-frame delta into it, then arms the countdown
  D_8009B162.  gp=7, at=0, default flags -- `tools_src/sweep_try.py` says the
  default row is the best of all 24.
  Three things the first draft got right and are worth reusing:
  * `func_8002348C()` is called with NO argument register set at all, and
    `src/func_8002348C.c` takes `u8 *arg0`.  That is the original's missing
    prototype: `$a0` still holds this function's own `arg0` at that point, so
    an implicit declaration emits nothing.  Do not add a prototype.
  * both `div`s are by the variable `n` (`break 7` in the listing), and the
    second arm's SHIFT is `sllv` by the same `n` -- so that arm is
    `(x << n) / n` with `n` a variable, while the first arm is `(x << 8) / n`
    with a literal.  gcc 2.8 has no global constant propagation, so `n = 8`
    assigned before the call does not reach the use after it.
  * `*(u16 *)(m * 4 + D_8009B1D5 * 0x50 + D_80090800 + 2)` folds the `+ 2`
    into `%lo(D_80090800+2)`; a base local (`tb = D_80090800;` assigned right
    after the call) is what keeps retail's `%lo(D_80090800)` with `2($v1)` on
    the load.  func_800300C8's rule, second instance.
  **The open question is the store-then-reload in the second arm.**  Retail
  has `sh $v0,0x2C($s1)` / `lh $v0,0x2C($s1)` / `nop` -- it stores the table
  value into the record and immediately reads it back signed.  gcc forwards
  the store and emits `sll 16`/`sra 16` on the register instead, which is one
  instruction fewer and cascades.  `*(volatile u16 *)(p + 0x2C) = …` for that
  one store reproduces retail exactly (-1/97 -> 173/173 and 69) and is the
  only thing that does.  Measured and worthless: a volatile READ (+2), a
  named local for the read-back, `&p[0x2C]`, a struct-view type on the read,
  a struct-view type on the store, and the whole 24-row flag sweep.  A
  genuinely aliasing store between the two -- `D_8009B162 = 0x40;` moved up,
  or a dummy byte store -- also brings the reload back, which is what
  identifies the mechanism as cse's memory invalidation; but both put the
  global store in the wrong place.  The volatile is installed as a
  placeholder and the plausible spelling is still open.
  What is left is 69 register differences in two clusters, both in the first
  arm: the five-argument `func_80022D94` setup (retail computes
  `((i * 2 + arg0[0x18] * 8) + D_8009B1D5 * 0x10) + D_800907AC` with the sum
  in `$v0` and we get the same tree with `$a0` as the destination, so
  expand_binop swaps the last two `addu`s), and the position of
  `D_8009B162 = 0x50` relative to the division's basic-block split.
  Wrong axes, each measured as a set of identical scores: six spellings of
  the fifth argument (a named index, an `(s32)` cast on the base, the base
  written first, three regroupings) are ALL 69, and four spellings of
  `t = arg1 & 2` (declared first, declared last, `s16`, plus a second name
  for `arg1 & 1`) are ALL 69.  One name for the two arms' `i` is 102 -> 97
  and is installed.  Next move is the permuter.

  **2026-08-29, 4 -> 2, from the permuter and it is the SAME edit twice.**
  The second scaling wants its mask and its shift split exactly the way the
  first one does, but against `a` rather than `b`:
  `a = (u32)((a * h) & 0xFFFF); b = a >> 8;`.  So both scalings are
  `x = mask(...); y = x >> 8;` and the intermediate is the value being
  scaled, reused as its own destination -- which is what puts the `mflo` in
  `$v0` instead of `$a2`.  The two remaining differences are `h`'s register:
  retail has `addiu $v0,$v0,1` / `mult $a0,$v0` and we get `$v1`.
  Measured and worse at that site: `h = load; h = h + 1;` and `h += 1;` as
  two statements (3), a fresh name for the `+ 1` (3), `s16 h` (+2 and 60),
  borrowing `b` for the `+ 1` (+2 and 59).  Measured and identical: `u32 h`,
  and writing the multiply as `h * a` rather than `a * h`.

# ---
func_80056250 MATCHED 2026-08-29, and the last two differences were the
  loop counter's initialisation moved ABOVE the guard in front of it --
  `i = sum;` written before `if (sum < p[0xE1B])` rather than as its first
  statement.  The permuter found it in 1524 iterations from the 2 base.  It
  is the lever func_8004D914 named (a loop variable assigned before an early
  exit numbers its pseudo first and shifts the whole allocation), and
  func_800727C0 matched on the identical edit the same hour -- so treat
  "move the counter's init above the guard" as a standing first try whenever
  a residue is nothing but register names.  What follows is the history.

func_80056250 -- was 170/173 (-3), 147 differences, written
  2026-08-29.  The duel-screen rebuild: it calls three setup routines, walks
  the 0xE1B-entry hand summing 0x14 or 0xC per card depending on a bit in the
  0xBEC bitmap, writes the two totals to +0xDF0/+0xDF4, re-initialises every
  sprite in the 0x1E0 pointer table, then dispatches on the byte at +0xE16 to
  one of three func_8005A468 calls and finally recolours the two VRAM bands.
  `arg0 * 0xE20 + D_800F2C40` is written inline FOUR times -- retail
  re-materialises the `%hi`/`%lo` pair and the whole 0xE20 multiply at each
  of the four phases, and only the third phase (across the func_800590DC
  call) keeps it in a callee-saved register, so that one is a local and the
  other three are not.  gp=0, at=0, default flags.
  Three things that are already right: `func_8004CB0C`, `func_8005A4C4`,
  `func_800590DC` and `func_800582C0` have no prototypes in functions.h and
  must not get one (`func_800590DC` is called with `$a1`/`$a2` still holding
  the previous loop's leftovers, which is what a one-argument implicit
  declaration gives); the second loop reloads `*c` for every one of its six
  statements, which is just the source writing `*c` inline; and
  `*(s32 *)(p + 0xDF0) = v; v += …; *(s32 *)(p + 0xDF4) = v;` against ONE
  name is retail's `addu $v0,$v0,$v1` where a fresh name is a second
  register.
  The -3 is three separate missing instructions, all identified:
  * the first loop's entry guard.  Retail reads the count, then
    `slt $v0,$a2,$v1` against the ACCUMULATOR's register (which holds the
    zero) and copies it into the counter -- three instructions where our
    `for (i = 0; i < p[0xE1B]; i++)` folds the constant and emits
    `beq $v0,$zero` plus the copy.  `for (i = sum; …)`, an explicit `z = 0;
    sum = z;` third name, and a hand-rotated `while` are all identical, so
    the shared-zero spelling is not it.
  * the arm ORDER of the switch.  Retail lays the arms out 0x23, 0x3E, 0x3C
    with 0x3C falling into the shared `jal`, so 0x3E needs its own
    `addu $a1,$a2,$zero` and ours does not.  Writing the cases in that order
    is 149 -> 147 and does NOT restore the instruction: gcc puts `v` straight
    into `$a1` where retail keeps it in `$a2` and copies.
  * the copy itself, `addu $a1,$a2,$zero`, which retail executes
    unconditionally in the first `beq`'s delay slot.  Measured and worthless:
    `w = v;` before the switch with `-w` in the 0x3C arm, `w = -v;` inside
    it, `w = v; w = -w;` inside it, and `v = -v;` in place.  A `goto`-join
    rewrite of the whole switch is -7 and much worse.
  Next move is the permuter; the shape is otherwise instruction-for-
  instruction right.

  **2026-08-29, -3/147 -> 173/173 and 17, six edits and every one of them
  named in the entry above as an open question.**  In order:
  * **the first loop is an explicit guard plus `do`/`while`, with the bound
    read INLINE in the guard and NAMED inside it.**
    `if (sum < p[0xE1B]) { n = p[0xE1B]; i = sum; do { … } while (i < n); }`
    -- the guard's inline read is what stops gcc folding `0 < n` to `beqz`
    (a named bound in the guard is -4), and the second read inside is CSEd
    to a plain `addu $t0,$v1,$zero`, which is retail's invariant copy.  Both
    halves are needed: named in both places is -3, inline in both is -2.
    -3/147 -> -2/90 -> 173/173.
  * **the switch's argument is a variable assigned BEFORE the switch, and
    each arm keeps its own call.**  `w = v;` then `case 0x23: w = 0;
    func_…; case 0x3E: w = v; func_…; case 0x3C: w = -w; func_…;` is
    -2/90 -> 173/173 and 55.  Writing ONE call after the switch cross-jumps
    the three arms' `addu $a0` together and is -6; passing `v` directly is
    -1, because gcc then gives `v` the argument register and the 0x3E arm
    needs no copy.  The case ORDER is 0x23, 0x3E, 0x3C -- read off the arm
    addresses in the listing, not sorted.
  * **a base local for the symbol in phases 3 and 4** (`b = D_800F2C40;`
    then `arg0 * 0xE20 + b`), which moves the `%hi`/`%lo` pair ahead of the
    0xE20 multiply.  Phases 1 and 2 keep the symbol inline.  55 -> 47.
  * **the 0xFFFF the loop compares against is a NAME**, assigned between
    `i = sum;` and `n = p[0xE1B];`.  32 -> 31.
  * **the second loop's counter is its own name** -- the two loops are two
    phases and one `i` for both costs a register class.  31 -> 23.
  * **and so is the second base local**: `b` in phase 3 and `b2` in phase 4,
    23 -> 17, even though the two never coexist.
  What is left is 17: `sum` and `i` are in `$a1`/`$a2` where retail has them
  the other way round, and the `*(s32 *)(p + 0xDF4) = v;` store is sunk into
  the `bne`'s delay slot where retail materialises the ternary's 0x800
  there instead.  Wrong axes, all four identical at 17: the operand order of
  `v = sum + *(s32 *)(p + 0xDE0)`, `i = 0` instead of `i = sum`, the
  declaration order of `i` and `sum`, and `do { i = sum; } while (0);`.
  Next move is the permuter.

# ---
2026-08-29, guard sweep, and BOTH hits are worth writing down as method.
  `sweep_guards.py` over 205 parked candidates and 84 guarded symbols
  returned two rows, and one of them is a **false positive of exactly the
  kind the score-ranking caution describes**.  func_8003BF00 goes from
  111/112 and 35 to 112/112 and 55 under `D_8009B118_SIZED` -- the LENGTH
  comes right, which by the usual ordering makes it the better base.  It is
  not: the guard changes `extern s32 D_8009B118` into `extern s32
  D_8009B118[2]`, so every `b = D_8009B118;` in that file silently becomes
  the symbol's ADDRESS rather than its value, and the listing plainly has
  `lw $v0,%lo(D_8009B118)($v0)`.  The instruction that "came back" is the
  `addiu %lo` of an address computation the function is not supposed to do.
  So: **a `_SIZED` or `_IS_AGGREGATE` hit on a symbol the candidate reads as
  a bare scalar is an array-decay bug, not a lever** -- check every
  unsubscripted use before believing the count.  func_80030D5C's hit is
  genuine (it already writes `D_8009B0F4[0]`), and is 29 -> 28 at -1 on
  `D_8009B0F4_SIZED_VOLATILE`; installed.

  **2026-08-29, 17 -> 2, and the whole drop is ONE embedded assignment plus
  one un-split store.**  The permuter's output was
  `v = (*(s32 *)(p + 0xDF0) = *(s32 *)(p + 0xDE0) + sum);` -- the DF0 store's
  own value becomes `v`, which is func_8002FB78's chained-assignment idiom
  and is ordinary C.  17 -> 5.  Then the DF4 store wants the addition INLINE,
  `*(s32 *)(p + 0xDF4) = v + *(u16 *)(p + 0xE02) * 4;`, not `v += …;` on a
  line of its own: with the chained assignment above it, `v +=` allocates the
  sum a second register and lets the store sink into the `bne`'s delay slot.
  5 -> 2.  Measured and worth 5 (i.e. nothing): a name for the multiply,
  `v = v + …`, naming only the halfword, and a `do { } while (0);` round the
  store.  Measured and worth 9 from the 17 base but subsumed:
  `v = *(s32 *)(p + 0xDE0); v = v + sum;` split against one name.
  The last 2 are the preheader order: retail has `addu $a1,$a2,$zero`
  (`i = sum`) before `ori $t1,0xFFFF` and we have them the other way round.
  Nine spellings measured, all 2 or 3: every ordering of the three preheader
  statements, the 0xFFFF written inline, and `do { } while (0);` round each
  of `i = sum`, `m = 0xFFFF` and the pair.

# ---
func_8001BAF0 -- 149/150 (-1), 98 differences, written
  2026-08-29.  A hand-reshuffle: it snapshots five byte slots out of
  `D_8009B1C8[i + 0x1A]` into a stack array, clears the entries named by the
  low bytes of D_800EAE88, and then for each remaining D_800EAE88 entry
  >= 0x10 finds the first free slot, swaps a six-byte record with the one the
  D_801AB000 entry points at, reloads the sprite through func_800249E0 /
  func_80018004 / func_8004036C, and writes the slot back.
  gp=3, at=0, default flags.  Things established and worth keeping:
  * the six-byte records live at `(u8 *)D_801AB000 - 0x31E0` and the
    subtraction must be its own `addiu`: written as one expression it folds
    to `%lo(D_801AB000+-12768)`, which is one instruction short.  A base
    local (`bb = (u8 *)D_801AB000; b = (Rec6 *)(bb - 0x31E0);`) is what
    splits it -- func_800300C8's rule again.
  * the record type is SIX BYTES WITH ALIGNMENT 2: retail copies it with
    `lwl`/`lwr` for bytes 0..3 and `lh`/`sh` for 4..5, which is exactly
    gcc's block move for size 6 at alignment 2.  Alignment 1 would be byte
    moves and alignment 4 aligned words.
  * the field at +2 is `s8`: the func_800249E0 call site sign-extends it
    (`lb`) because it becomes an SImode argument, while the two byte-to-byte
    copies of the same field come out `lbu`, which is what a QImode use of
    an `s8` member gives.  One declaration covers all four sites.
  * the function swaps field +2 individually and THEN swaps the whole
    six-byte record, so byte 2 ends up where it started.  That is what the
    listing does; do not "fix" it.
  * **the outer loop's exit test is SIGNED (`slt`) on a pointer**, and no
    pointer spelling gives that -- gcc compares pointers with `sltu`.
    `(s32)e < (s32)(D_800EAE88 + 5)` does, and so does an `s32` cursor with
    `*(u8 *)e` (identical scores).  Writing it as `for (i = 0; i < 5; i++)`
    over `D_800EAE88[i]` also gives `slt`, via strength reduction -- but
    then gcc puts the giv's final value in the PREHEADER, where retail
    re-materialises the whole `%hi`/`%lo` pair plus `addiu 5` in the latch
    every iteration.  So the index loop is -2 and the cast is the shape.
  * `while (1)` with `e++` and an explicit `if ((s32)e >= …) break;` at the
    bottom is what removes the folded-but-not-deleted entry guard
    (`addiu $v0,$zero,1` / `beq $v0,$zero`) that the `for` spelling emits:
    +1/106 -> -1/98.  A `do`/`while` peels the first `lbu` and is +2.
  What is left, in three pieces: the `+ 5` folds into `%lo(D_800EAE88+5)`
  where retail keeps `addiu $t2,%lo(D_800EAE88)` and `addiu $v0,$t2,5`
  separate; D_800907CC is hoisted out of the OUTER loop by us and into the
  inner loop's preheader by retail; and D_801A7AD8 is hoisted into that same
  preheader by retail and left in the body by us (a base local for it is 111,
  i.e. worse).  All three are placement, not shape.

  **2026-08-29, -1/98 -> 150/150 and 84 on the loop limit's spelling.**
  `(s32)e - (s32)D_800EAE88 >= 5` is the one form that does not fold the
  `+ 5` into `%lo(D_800EAE88+5)`; it emits `subu`/`slti` where retail has
  `addiu $t2,%lo` / `addiu $v0,$t2,5` / `slt`, which is the same instruction
  COUNT and not the same instructions -- so this is a placeholder that fixes
  the length, not the answer.  Measured and all -1/98: `(s32)e >= (s32)
  D_800EAE88 + 5`, `!((s32)e < (s32)&D_800EAE88[5])`, `(s32)(&D_800EAE88[0]
  + 5)`, `(u8 *)((s32)D_800EAE88 + 5)`, and a two-induction-variable `for`
  (that one is -2 and 137).
  What retail wants is a REGISTER holding D_800EAE88 with `addiu 5` applied
  to it, re-materialised in the latch every iteration -- three instructions
  where every spelling above gives two.  A source local for the base would
  be hoisted, so it is not that either; the mechanism is not established.

# ---
2026-08-29, negatives worth not repeating.
  func_8003CEB8 MATCHED 2026-08-29 on ONE declaration: `s16 crc`, not
  `s32`.  It is a CRC-16, so the narrow type is what the source obviously
  had; the extra `andi 0xFFFF` at the return and the whole shift chain come
  out right once each iteration truncates.  The permuter found it in 699
  iterations paired with a named `0xFFFF` mask that is worth NOTHING on its
  own -- decompose before installing, as always.  The four zero-init shapes
  recorded here an hour earlier were the wrong axis, and the reason is worth
  keeping: the residue was `addu $a3,$v1,$zero` against retail's
  `addu $a3,$zero,$zero`, which reads as a shared-zero question and was a
  TYPE question.
  func_8002E730 (2 differences): retail sets up func_8002E00C's argument
  (`addu $a0,$s1,$zero`) BEFORE the `sw $s0,D_8009B280` store and we set it
  after.  Measured: a fresh name for the argument assigned right after
  `t = D_800EAE98;` is 2 (no change), borrowing the function's own dead `u`
  is 2, moving the D_8009B280 store below the `*(s32 *)(e + 4)` update is 8,
  and folding that update into the same `do { } while (0);` is 4.  So the
  argument-hoist lever does not reach it and the axis is still unknown.

# ---
2026-08-29, `score_permuter_outputs.py` over ~150 directories, three finds.
  This is the sweep the WORKFLOW says to run after any batch of permuter
  work, and it paid again -- every one of these had been sitting unread.
  * **func_80021598: +3 and 154 -> 158/158 and 27**, on ONE statement:
    `fp = &f;` where `fp` is a `u8 **` that is never read again.  Taking the
    address of the record cursor is what fixes the whole allocation, and it
    fixes the LENGTH too.  Position does not matter (at the top of the
    function and immediately before the `v = f[8];` the permuter found score
    identically).  It is **not** that `f` goes to memory: `u8 *f[1]` with
    `f[0]` everywhere is +15 and 163, and `u8 * volatile f` is +20 and 167 --
    both put it in memory and both are much worse.  So the lever is the
    address being TAKEN, not the storage class, and no plausible spelling has
    been found.  What is left is one bias: retail keeps `$s0` = `p` and
    writes `44($s0)`, we hoist `p + 0x2C` into `$s0` and write `0($s0)`.
    Four spellings of that accumulator (`((s32 *)p)[0xB]`, an `(s32)` cast,
    the constant written first, and the whole 24-row flag sweep) are all 27.
  * **func_8002BD0C: 19 -> 14**, on `if (d2) { A } else { A }` round the
    join1 arm's two stores -- semantically a no-op that gcc merges back, and
    the gain is that `d2` (the OTHER arm's name for the same global) is READ
    there, which extends its live range past the branch.  Plausible spellings
    that do NOT reach it: `do { … } while (0);` round the same two stores
    (-3 and 68), `d2 = d;` before them with the stores through `d2` (-1 and
    65), the same with the stores through `d` (19), and `d2 = d;` after them
    (19).  Installed as a placeholder; the spelling is open.
  * **func_80020988: 72 -> 46**, and this one does NOT decompose.  Its output
    is three edits at once -- a named `0x2000`, an `if (1)` round one store,
    and `if (D_8009B152) { A } else { A }` round the tail.  The named
    constant alone is 72, and the plausible reading of the third (writing
    `D_8009B152 |= 0x40; return 0;` out in both arms of the `0x2000` test) is
    -1 and 76.  Not installed.

  **The full run: 65 candidates checked, 18 with a better stored output, and
  one of them was a MATCH nobody had opened.**  func_8005D994 was parked at 5
  and its `output-0-1` directory held a zero score.  Decomposed, only ONE of
  its three edits is load-bearing:
  `first = *(s8 *)&D_8009B07A; x = first;` -- borrowing the name of a local
  whose own live range starts further down, which is the func_8002A4A8 class.
  The other two (`0x80 | arg0` instead of `arg0 | 0x80`, and `a[2] +=`
  instead of `a[2] = a[2] +`) are worth nothing alone and are NOT installed.
  MATCHED 2026-08-29.
  The remaining fifteen are unread and are the seam to work next; each is
  `build/permuter/<f>/output-*-1/source.c` re-scored against the installed
  park, and each still has to be decomposed before it goes in:
    func_8002C6C8  58 -> 25      func_80047480  60 ->  9
    func_80037DA4  61 -> 54      func_8004803C 140 -> 98
    func_8003B744  18 ->  8      func_80048A28  65 -> 60
    func_80044608  38 -> 33      func_80048D08  59 -> 19
    func_80045208  25 -> 17      func_8004A518  73 -> 27
    func_80046768  46 ->  5      func_8004C84C  25 -> 14
    func_8004D58C  13 -> 12      func_8004D75C  40 -> 21
  Two cautions that already apply to this list: func_8003B744's and
  func_8004C84C's stored outputs are the two the WORKFLOW records as
  SEMANTICALLY WRONG (one casts an index the target does not mask, the other
  deletes a store the target has), so their numbers are not candidates.
  func_80046768's 46 -> 5 is the uninitialised-read class and is not
  installable either -- its entry above says so.

# ---
2026-08-29, working the score_permuter_outputs list.  Five taken, one MATCH.
  * **func_80047480 MATCHED**, and the permuter's win decomposes to ONE
    moved store: `*(s32 *)(p + 0x438) = 0x1010;` written after the
    `p + 0x43C` store instead of before the `p + 0x388` one, which is 60
    differences to 9.  The named zero it came paired with is worth nothing.
    The last 9 were the counter-init lever twice over: `i = 0;` hoisted out
    of the outer `for` clause AND `j = 0;` hoisted out of the inner one,
    with `k = i << 6;` between them.  `i` alone is 6, `j` alone is 3, both
    is the match -- so this is a coupled pair of the same lever, and doing
    one and stopping would have read as a near-miss.
  * **func_80048D08: -1 and 59 -> 131/131 and 19**, on `d = D_8009B45C;
    p = d;` -- borrowing the name of a local whose own assignment is two
    lines further down, the func_8002A4A8 class.  It fixes the length too.
    The `do { } while (0);` it came paired with is worth nothing.  What is
    left is `j` and `k` exchanged in `$t3`/`$t4` plus one copy's position;
    measured and all 19: both counter-init hoists, the declaration order of
    `j` and `k`, and reading `+0x444` through `d` rather than `p`.
  * **func_8004D75C: 40 -> 21** on `if (r || t) { A } else { A }` round one
    store -- the same implausible identical-arms hint as func_8002BD0C, and
    again `do { … } while (0);` round the same store is worth NOTHING (40).
    Reading `r` and `t` at that point is the mechanism.  Installed as a
    placeholder.
  * **func_8004A518: +1 and 73 -> 112/112 and 27 on `u16 o2`** -- and this
    one is a FALSE ZERO worth spelling out.  The +1 in the `s32` version is
    gcc hoisting `addiu $s6,$zero,99` (the `e2[3] = 0x63` store's constant)
    out of the loop where retail materialises it per iteration.  The narrow
    declaration does not fix that; it adds an `andi $v1,$s2,0xFFFF` retail
    does NOT have and separately suppresses the hoist, and the two cancel.
    `u8 o2` scores the same and is semantically WRONG (`o2` starts at 0x180).
    Installed at `u16` because the equal length makes the diff readable, but
    the `andi` is a known lie -- the honest problem is the 0x63 hoist, and a
    named constant assigned inside the loop, `for (o2 = 0x180; …)`, and
    moving `o2 = 0x180` above `i = 0` are all +1 and 73.
  * **func_8002C6C8's 58 -> 25 is REJECTED**: its output moves
    `q[0] = q[0] | 0x40;` to after the `return`, i.e. deletes a store the
    target has.  Third instance of that class after func_8003B744 and
    func_8004C84C.  The one plausible half, a base local for D_800F2848
    hoisted out of the loop, is +2 and 46 against a base of -1 and 58 --
    worse by the length rule.

# ---
2026-08-29, the rest of the score_permuter_outputs list.  Six worked, no
match, and one finding that is worth more than any of them.
  **`if (x) { A } else { A }` is a LIVE-RANGE hint and nothing else, and
  there is no plausible spelling of it.**  Four instances now -- func_8002BD0C
  (19 -> 14 on `if (d2)`), func_8004D75C (40 -> 21 on `if (r || t)`),
  func_80045208 (25 -> 17 on `if (e)`), func_80044608 (38 -> 33 on
  `if (D_8009AF7C)`).  In every one, `do { … } while (0);` round the SAME
  statement is worth exactly nothing.  The mechanism was pinned down on
  func_80045208, the smallest of them: a plain dead read (`p8 = e;`, `q = e;`,
  at either position) is also worth nothing, because gcc deletes it before
  allocation.  A BRANCH on `e` cannot be deleted -- cross-jumping the two
  identical arms happens after register allocation -- so the branch is the
  only thing that survives long enough to make `e` live at that point.  That
  is why the idiom works and why every legitimate spelling of "read this
  value here" fails.  All four are installed as placeholders.
  * **func_8004D58C: 13 -> 12** on moving `ff = 0xFFFF;` one line down, below
    `p3 = (u8 *)0;`.  The `new_var` for `t + 0xDF0` and the `!= 0` dropped
    from the test are both worth nothing.  What is left is the master
    register of five zero copies (`$t3` in retail, `$t5` in ours); twenty of
    the 120 orderings of those six initialisations were swept and none beats
    12.
  * **func_80037DA4: 61 -> 54** on a SECOND NAME for the opcode --
    `op2 = op;` assigned right after the read, used only by the
    `(op2 & 0x80)` test.  Using it for the `switch (op2 & 0xF)` as well
    scores the same, and using it ONLY for the switch is worth nothing, so
    the 0x80 test is the site that matters.
  * **func_80048A28: 65 -> 60**, two edits that compose: a name for
    `(arg0 & 0x1F) * 2` (65 -> 64) and writing D_8009B45C INLINE at the two
    sites in the `arg2 - 1 < 0x80` block instead of through the `b` local
    (61 alone, 60 together).
  * **func_8004803C: 140 -> 137**, and its 98 is REJECTED for length.  The
    two `arg1 & 0xFF` masks the permuter added are semantically free -- the
    function already does `arg1 = arg1 & 0xFF;` before them -- and they are
    worth 3 on their own at equal length.  Reaching 98 needs the
    identical-arms hint on top, and HERE that hint costs an instruction (+1),
    unlike the four cases above.  `u8 arg1` in the signature, with or without
    the explicit mask statement, is -1 and 154, so the byte parameter is not
    the reading either.

# ---
2026-08-31, FOURTEEN parked candidates replaced by Unchiga's, because his base
is closer. The counts are ours -> his, all measured through our own toolchain
with HIS unit flags (the flags are in each file's header; try_func's defaults
report a different number, which is the func_800137E4 lesson again):

    func_8004B374   -3/72 -> 0/2      func_8004A518   0/27 -> 0/2
    func_8003BD14    0/42 -> 0/2      func_800260D0   0/25 -> 0/4
    func_8003BF00   -1/35 -> 0/4      func_80049920   0/21 -> 0/2
    func_8004B854   -2/34 -> 0/3      func_8005C5D4  -1/19 -> 0/2
    func_8004C420    0/15 -> 0/2      func_80043328   0/12 -> 0/2
    func_800734DC    0/10 -> 0/2      func_8003B808    0/8 -> 0/4
    func_8004A6F8     0/6 -> 0/2      func_8002CCE4    0/3 -> 0/2

  The prose above each entry is still ours and still worth reading -- it is
  the record of what this tree measured on its own, and several of those
  findings are cited elsewhere in this file. What changed is only which
  candidate sits in `parked/`, and the rule is the obvious one: the park holds
  the best known base, and his was better in every one of these.
  Every one is now at 2-4 differences at correct length, which is msearch
  range rather than permuter range.

# ---
2026-08-31, three axes closed EXHAUSTIVELY rather than sampled, by the new
sweeps. Recording the negatives because each one used to be a guess:
  * **Flags do not move the ported near-misses.** All six combos over the 60
    units his C brings within eight differences: 0 improved. The control
    matters more than the result -- func_800137E4 fed in at a synthetic base
    of 42 comes back MATCH under `-G0 -mno-split-addresses`, so the sweep can
    say yes and its zero is a real zero. His flag choices port correctly; what
    is left in those 60 is shape.
  * **Declaration width does not move them either.** Every local's width over
    the 36 units within two differences: best stays 1-2, none reaches 0.
  * **func_80058624 is characterised, not merely parked.** Ten spellings of
    the negate tail, twelve `v`/`w` type combinations and all 75
    single-declaration widths leave it at one difference -- on top of the ten
    hand shapes and 411 permuter iterations already recorded. The residue is
    still `subu $v1,$zero,$v1` against our `subu $v1,$zero,$v0`: retail
    negates in place and gcc propagates the original in. Whatever produces
    that copy, it is not the tail's spelling and not a declared width.
